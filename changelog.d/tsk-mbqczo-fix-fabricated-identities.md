### Fixed
- Fabricated sha256 values removed from `longmemeval_oracle.json` and `longmemeval_s_cleaned.json` in `benchmarks/data/README.md`; both now marked `NOT YET PINNED - no verified copy has been hashed`
- "Used by" sentence for `longmemeval_s_full.json` corrected to name all 9 real loaders from the gate output, removing the trailing catch-all `longmemeval_*` phrasing
- Identity blocks for datasets without verified checksums explicitly admit the hole rather than filling with fiction
- Reference to missing `benchmarks/scripts/clean_longmemeval.py` script removed; obtain step replaced with honest statement that no verified copy is available