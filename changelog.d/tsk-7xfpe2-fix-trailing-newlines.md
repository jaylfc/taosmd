### Fixed

- Restored the trailing newline on `benchmarks/data/README.md`, which lost its terminating byte in #348 (`17ea02a8`). The `tsk-vghh6c-unify-mib-mb-units` fragment is added here for the first time rather than restored: #360 carried it but was blocked and never merged.
