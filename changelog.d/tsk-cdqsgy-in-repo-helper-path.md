### Fixed

- `.claude/audit-cron-prompt.md` now invokes `scripts/resume_arm_time.py` at its repo-relative path in both places it spelled out the command, instead of naming an out-of-repo copy under `~/.taos-team/`. Since #354 the script derives its own location from `__file__`, so the in-repo file is the only copy that can be correct, and a second copy elsewhere is a document that drifts silently: one did, resolving for weeks to a checkout fifteen commits behind master, so the armed resume pair ran a pre-#354 script while every path involved still resolved successfully.
- `tests/test_docs_name_in_repo_helper_path.py` fails if any file under `.claude/` or `docs/` names `resume_arm_time.py` at a path other than `scripts/`. The scan reads files out of the repository and never inspects the filesystem it describes, so it is checkable in CI on a machine with no `/home/jay` at all. It is verified against the pre-fix document rather than only a synthetic fixture, and it asserts that the scan reaches that document, since a scan that read no files would report zero violations and read as a pass.
