# STATUS

Working snapshot for whoever picks this repo up next. Updated whenever meaningful state changes. Shipped changes live in [CHANGELOG.md](CHANGELOG.md); measured results live in [docs/benchmarks.md](docs/benchmarks.md); the task list of record is GitHub issues.

Last updated: 2026-06-23 ~19:45Z (MAC MINI RESTART PAUSE). **RESTART HANDOFF (read first): Jay is restarting the Mac mini, so THIS session + its session-only crons DIE (audit 795131e3, daily 74cba0e7, resume pair 4100656a/8318c52a). Disk, git, and memory persist. ON THE NEW SESSION: (1) re-arm the audit cron (`7,37 * * * *` running .claude/audit-cron-prompt.md), the daily sweep (`9:13`), and a resume pair for the live 5h window; (2) the GPU is Jay's until he frees it (check the bus + nvidia-smi before any claim). IN-FLIGHT WORK THAT SURVIVES THE RESTART (it runs on the BENCH host over SSH, NOT the Mac): the full-500 gemma4 head-to-head confirm = nohup chain PID 312668. FULL-500 CONFIRM DONE (stamp 20260623_171640): combo 53.8 Qwen / 61.4 llama vs MemOS 54.0 / 61.2 = PARITY (statistical tie on both judges; the n=150 +4.6 strict combo lead was sampling noise). The gemma4 generator upgrade is confirmed real regardless (combo 53.8 Qwen full-500 vs the shipped qwen3.5:9b 42.8). Docs framing FIXED to PARITY (not "beats"): benchmarks.md + research report rev 1.46. **GENERATOR-FLIP DECISION: NOT flipping the default.** gemma4:12b is TASK-DEPENDENT (wins LongMemEval +10pp but LOSES LoCoMo 0.63 vs 0.68 [N-007] and BEAM 46 vs 49 [rev 1.25]); the shipped default generator STAYS qwen3.5:9b (multi-session/long-context leader); gemma4:12b documented as a per-task option for single-fact QA. @taOS-dev sent the generator recommendation (bus 974) then the task-dependency caveat (bus 975). CASCADE STATUS: PR #176 merged, full-500 done, docs corrected (README + benchmarks + report, parity + task-dependency), @taOS-dev messaged. REMAINING: the 5 directory PRs (#47/#76/#10/#46/#26) still cite 74.6%, correct them carefully (spam-guard history, keep it factual). READ IT WHEN DONE: `grep -E "COMBO:|MEMOS:|DONE" benchmarks/results/e022_h2h_full500_SUMMARY.txt` (format: `COMBO: Qwen X/500, llama Y/500` / `MEMOS: ...` / `DONE`). DONE THIS SESSION (Jay greenlit "deal with all four"): PR #176 parser fix MERGED to master; research report CORRECTED (rev 1.45, N-017); README + benchmarks.md CORRECTED to remove the inflated 74.6%. STILL PENDING (do AFTER the full-500 SUMMARY lands, with its confirmed numbers): (a) flip the recommended generator default to gemma4:12b (recipes.py / agents.py / README tier tables, per ship-the-win); (b) correct the directory PRs #47/#76/#10/#46/#26 (they cite 74.6%); (c) send @taOS-dev the corrected numbers; (d) tighten the README/benchmarks gemma4 figures to the full-500 confirmed values. CONFIRMED NUMBERS: 97% Recall@5 is judge-free, UNAFFECTED, the true headline; the end-to-end Judge corrected to 42.8% Qwen / 51.2% llama (old qwen3.5:9b generator, full-500); gemma4:12b generator is +10pp (bake-off n=150) lifting end-to-end to ~54-57%; gemma4 head-to-head has the combo matching/beating MemOS-lossless (n=150: combo 57.3 Qwen / 64.7 llama vs MemOS 52.7 / 64.0; full-500 combo 53.8% Qwen, MemOS pending). The judge-parser bug (N-017) inflated ALL LongMemEval --llm Judge numbers since 2026-04-13; Recall@5 / LoCoMo / BEAM use different judges and are unaffected.** **JAY GREENLIT "deal with all four": (1) PR #176 parser fix MERGED to master (_parse_verdict on master); (2) full-500 gemma4 head-to-head CONFIRM RUNNING (HEALTHY, not stalled; the temporal front-load made it slow at ~35s/Q early, faster on easy types) = self-contained nohup chain PID 312668, at ~206/500 on Arm A at ~19:13Z, revised ETA ~01:00-01:30Z (Arm A then Arm B then 2 rescores), read `grep -E "COMBO:|MEMOS:|DONE" benchmarks/results/e022_h2h_full500_SUMMARY.txt`; DECISION (Jay asked wait-vs-proceed-on-n150): WAITING for the full-500 to do ALL public corrections in ONE clean pass on confirmed numbers (no rework, best story); the research report (rev 1.45) already carries the integrity correction so the record is straight while the marketing copy waits; (3) research report CORRECTED = rev 1.45, N-017 (the judge-parser bug + corrected numbers), flagged F-012/F-013 SUPERSEDED, resolved E-022; (4) generator-default flip to gemma4:12b and (5) public corrections (README 74.6% in ~4 spots + benchmarks.md + directory PRs #47/#76/#10/#46/#26 + @taOS-dev) are SEQUENCED for AFTER the full-500 confirms, so they carry the FINAL confirmed numbers + the good story (gemma4 beats MemOS) rather than interim placeholders. NOTE: README/benchmarks still display the inflated 74.6% pending that pass; the authoritative research report already documents the correction (N-017). ON NEXT RESUME / when SUMMARY.txt shows DONE: read the 4 numbers, then do the default-flip PR + the README/benchmarks/directory/@taOS-dev corrections with those numbers. PRIOR LATEST (Jun 23 ~00:45Z): ARCTIC RESULTS LANDED. Job1 full-500 F-013 re-measure on arctic = 410/500 = 82.0%** (up from 74.6% on minilm + the buggy harness; same runner / judge / 500-Q oracle, only arctic + the context-order fix changed; 500/500 processed, 0 errors). HEADLINE 82% empty-inflation RULED OUT (n=100 inspectable confirmation: 0/100 empties, 0 empties judged correct; the runner now persists predictions via TAOSMD_DUMP_PREDICTIONS, commit edf62a5 on bench/e022-head-to-head). NEW caveat surfaced: the judge (Qwen3-4B-Instruct) is SAME model FAMILY as the qwen3.5:9b generator and showed some paraphrase leniency (57/77 correct verdicts had no literal gold substring), so the ABSOLUTE 82% may be optimistic. The cross-family firm-up CAUGHT A CRITICAL SCORING BUG (not judge leniency). benchmarks/longmemeval_runner.py score_answer_llm parses the verdict as `"CORRECT" in judgment` after `.upper()`, and "INCORRECT" CONTAINS the substring "CORRECT", so EVERY INCORRECT verdict is scored as a PASS. Confirmed by CODE INSPECTION (master line 143, bench/e022 line 219; present since the initial commit 93d9ff0, 2026-04-13) AND empirical re-judge (n=100 stored 77 -> 33 corrected by the same Qwen judge; cross-family 25 gemma4:e4b / 37 llama3.1:8b; ~40pp inflation). The "Judge" score effectively measured non-abstention, not correctness (the abstain guard correctly failed IDK answers; everything else passed the broken parser, which is why better retrieval "raised" it). **AFFECTED (inflated):** every longmemeval_runner --llm Judge number = F-013 74.6% / 82% / 77%, E-021 ablation (baseline 64 etc.), E-022 head-to-head (taOSmd 64/77/81). **SAFE (unaffected):** Recall@5 97% is JUDGE-FREE (objective retrieval via fusion_shootout.py / embedding_comparison.py: `any(aid in retrieved_session_ids ...)`), so the PRIMARY headline stands. Bug pattern is isolated to this one file (grep-confirmed); BEAM (beam_runner.py) + LoCoMo use different judges, audit separately. NOTHING was published with the inflated numbers this session and nothing recorded to the report. **ESCALATED to Jay.** Proceeding with the SAFE, non-public, reversible parts while public corrections wait for Jay: (1) PARSER FIX DONE as a reviewable PR (NOT merged) = PR #176 (fix/judge-verdict-parser): a `_parse_verdict` helper (INCORRECT-first, fail-closed) + 5 regression tests; master still carries the bug until Jay merges. (2) RE-BASELINE LANDED (bench log e022_rebaseline_arctic500_20260623_042130.log, dump predictions_20260623_063813.json = 500 recs): **corrected full-500 arctic Judge = 214/500 = 42.8%** (Qwen3-4B-Instruct, FIXED parser), DOWN from the inflated 82.0% (410/500) = ~39pp of inflation confirmed. Consistent with the n=100 corrected 33% (the full-500 adds the easier question types). Cross-family llama3.1:8b full-500 rescore = 256/500 = 51.2% (rescore log rescore_llama_500_071615.log; dump predictions_20260623_063813.rescored_llama3_1_8b.json; flips: 60 Qwen-INCORRECT -> cross-CORRECT, 18 the other way). So the REAL arctic Judge sits in a **~43 to 51% band** (corrected Qwen 42.8% at the strict end, llama 51.2% at the lenient end; n=100 gemma4:e4b 25 was stricter still on the hard slice). NOT 82%. Notably, once the parser is fixed the same-family Qwen judge is the STRICTER judge, so the original same-family-leniency worry was inverted. The old minilm 74.6% was inflated the same way. RE-BASELINE COMPLETE. The objective Recall@5 97% (judge-free) is unaffected and remains the true primary headline. ALL PUBLIC CORRECTIONS STILL HELD for Jay (README 74.6% front-page, research-report F-013/E-021/E-022, benchmarks.md, directory PRs, @taOS-dev); parser fix is PR #176 (open, not merged). Jay asked "what is the point of the extras if MemOS gets 88 vs our 81"; answered honestly (BOTH 81 and 88 came from the broken judge so the real gap is unknown; the extras compete on auditability / local-first / provenance / multi-agent, not raw QA accuracy). CLEAN HEAD-TO-HEAD RE-RUN LANDED (Qwen inline, FIXED parser, n=100 hard slice; logs e022_combo_n100_fixed_20260623_081638 / e022_memos_n100_fixed_20260623_083456): **RESULT (n=100 hard slice, apples-to-apples, byte-identical question order, 0 empties): taOSmd-combo = 37 Qwen / 47 llama; MemOS-lossless = 43 Qwen / 46 llama.** Both collapsed ~44pp from the inflated 81/88 (both prior numbers were near-fiction). The corrected gap is JUDGE-DEPENDENT and within n=100 noise: MemOS +6 on the strict same-family Qwen judge, but taOSmd +1 on the neutral cross-family llama judge (it FLIPS). So once the judge is fixed MemOS does NOT robustly beat the combo = statistical DEAD HEAT. KEY: the combo (our verbatim-turn + rerank + self-verify, i.e. Jay's "our features + their representation") reached ACCURACY PARITY with MemOS-lossless WHILE keeping the differentiators MemOS lacks (zero-loss, active verify-and-gate, provenance, shared A2A coordination, local-first). That is the real answer to "what is the point of the extras": parity on the core task PLUS the stack they do not have. Caveat: n=100 hard slice (2 of 6 question types); a full-500 head-to-head would firm it up. Parser fix is committed on bench (730ac3a) + master PR #176. ALL public numbers (the inflated 81/88/74.6/82) still need correcting per Jay; honest public framing = Recall@5 97% (judge-free) + accuracy-parity-with-MemOS + the differentiator stack. GENERATOR BAKE-OFF IN FLIGHT (subagent af65d3af, Jay-suggested now that the judge bug is fixed): honest fixed-judge comparison of qwen3.5:9b (baseline) vs gemma4:12b (NEW, Jun 3 2026) + a qwen3/3.6 ~14B dense variant + phi-4-reasoning:14b, all fit-checked against the 12GB 3060 ceiling, representative n=150 across all 6 question types, generator-only variation = the clearest lever to raise the honest ~37-47% end-to-end number. (qwen3.6 27B / 35B-A3B confirmed too big for 12GB = hardware ceiling, not a bug artifact; phi-4-reasoning:14b SKIPPED = offloads at 13GB; no qwen3.6 dense <=14B exists, smallest is 27B.) **BAKE-OFF COMPLETE (n=150 representative seed=7, fixed judge Qwen3-4B-Instruct, generator-only): qwen3.5:9b baseline 65/150 (43.3%, abstain 20.7%, 9.3s/Q, peak 10.9GB); gemma4:12b 80/150 (53.3%, +10pp, 15.2s/Q, peak 8.5GB); qwen3:14b 74/150 (49.3%, +6pp, 14.1s/Q, peak 10.3GB).** BOTH challengers beat the shipped generator; gemma4:12b is the clear winner (+10pp AND lighter on VRAM than the 9b baseline, at ~1.6x latency). This is the clearest lever found: a generator swap raises the honest end-to-end from ~43% to ~53% with retrieval/everything else unchanged. NEXT (per discipline, do NOT flip the shipped default on n=150 alone): full-500 confirm of gemma4:12b + a llama cross-family re-judge of the winner; if it holds, update the recommended generator default (recipes.py / agents.py / README tier tables, per the ship-the-win rule) AND re-run the MemOS head-to-head with gemma4:12b (a stronger generator likely turns the dead-heat into a lead). phi-4-reasoning:14b skipped (offloads at 13GB); no qwen3.6 dense <=14B exists. GEMMA4 HEAD-TO-HEAD RESULT (n=150 seed=7, fixed Qwen judge, BOTH arms gemma4:12b generator): **taOSmd-combo 86/150 (57.3%) BEATS MemOS-lossless 79/150 (52.7%) = +4.6pp for taOSmd.** The prior dead-heat (old qwen3.5:9b generator: combo 37/47 vs MemOS 43/46 @n=100) FLIPPED to a taOSmd LEAD with the better generator = the honest version of the beat-MemOS goal, achieved. Combo+gemma4 57.3% is also our best honest end-to-end number to date (vs ~43% with the old generator). **llama cross-family DONE (rescore PID 303630, I salvaged it after the subagent stalled): combo 64.7% (97/150) vs MemOS 64.0% (96/150) = +0.7pp (a TIE on the lenient judge).** FULL PICTURE (gemma4:12b generator, n=150 seed7): combo 57.3 Qwen / 64.7 llama; MemOS 52.7 Qwen / 64.0 llama = taOSmd AHEAD on the strict Qwen judge (+4.6) and EVEN on the lenient llama (+0.7). vs the OLD-generator dead-heat (MemOS +6 Qwen / taOSmd +1 llama), the better generator swung the strict-judge result ~10pp in taOSmd's favor, so taOSmd is now ahead-or-even on BOTH judges (was behind on strict). HONEST VERDICT: with gemma4:12b the taOSmd-combo MATCHES MemOS-lossless and edges ahead on the discriminating judge; not a blowout (llama ties at ~64%), but a real, honest improvement. Logs e022_combo_gemma12b / e022_memos_gemma12b _20260623; rescored dumps .rescored_llama3_1_8b.json. CAVEAT: n=150; a full-500 would firm the +4.6/+0.7. This is the strongest honest story: 97% Recall@5 + best-honest end-to-end ~57-65% with gemma4 + matches/beats MemOS + the differentiator stack. Measurement only; public corrections + generator-default flip still HELD for Jay. **ALL PUBLIC CORRECTIONS STILL HELD for Jay's direction:** README (74.6% is front-page), research-report (F-013/E-021/E-022 entries), benchmarks.md, directory PRs (#47 cites 74.6% Judge), @taOS-dev. The objective Recall@5 97% headline is judge-free and stays correct. Firm-up committed a CORRECT rescore tool on bench/e022-head-to-head (8af894c: benchmarks/rescore_predictions.py, word-boundary parser, --judge-model). **Job2 combo (verbatim turns + rerank + self-verify, n=100 head slice) = 81/100**: +4pp over chunked 77 but did NOT beat MemOS-lossless 88 (gap narrowed ~1/3; +4 is near n=100 noise) so it stays the transparency-play thesis, not a score-beat. GPU now FREE (chain done). **PR #175 (taosmd reindex) MERGED** (squash 289fff5; per-agent clear + archive-driven re-embed under the current embedder, for the taOS minilm->arctic cutover @taOS-dev requested). NEXT: when the confirmation lands clean -> record F-013-arctic 82% + E-022 combo to the research report (with provenance) -> README/benchmarks cascade -> send @taOS-dev the validated numbers. **PRIOR: v0.4.0 RELEASED to PyPI (embedder-preflight hardening + claims layer + cockpit + controls); @taOS-dev notified (bus 937) to pin 0.4.0 + the re-index procedure. FAIR E-022 re-run = taOSmd 77/100** (the harness fix recovered ~13pp over the confounded 64: FTS-bloat context bug fixed on bench/e022-head-to-head 5fdae2a + arctic embedder). MemOS-lossless still leads **88 vs 77** = an 11pp verbatim-vs-chunked representation gap. **COMBO built to target it:** TAOSMD_VERBATIM_TURNS mode (ingest each turn verbatim like MemOS general_text, KEEP our rerank + self-verify + fixed context order), default-off byte-identical, CPU sanity PASS (one vector per turn, whole turns retrieved); commit f407069 (+ chain/sanity beb0590) on bench/e022-head-to-head. **GPU CHAIN LIVE** on the bench host, nohup wrapper PID 98146, sequential (ollama stop between jobs): JOB1 = full-500 F-013 re-measure on arctic (chunked / VERBATIM OFF, the headline Judge number; old 74.6% was minilm + buggy harness, expect higher), log benchmarks/results/e022_full500_f013_arctic_20260622_224740.log (~75-90min, started ~22:47Z); JOB2 = combo n=100 (VERBATIM ON), log benchmarks/results/e022_combo_verbatim_n100_20260622_224740.log (created when JOB1 ends) = the beat-MemOS test vs 88 and vs chunked 77. **READ each:** grep 'Overall:' <log> | tail -1 (or tail benchmarks/results/e022_chain_20260622_224740.out). **NEXT ON RESUME (integrity: INSPECT predictions before recording):** poll both logs; when done, record E-022 to the research report against its kill criterion (never publish a loss without inspecting the raw predictions first); then run the arctic-re-measure -> README -> benchmarks.md -> directory-PRs (#47/#76/#10/#46/#26) -> hardening-release cascade. **HARDENING DONE + PR'd:** PR #174 (feat/embedder-preflight) adds a setup-time dense-embedder preflight + a loud VectorMemory onnx-load-failure warning = the fix for the silent minilm fallback Jay hit when taOS provisioned an agent (recipe writes embed_model but nothing downloaded or checked it); MERGED to master (squash 0e1efd6). **RELEASED as v0.4.0** (commit 35ab09e, tag v0.4.0, GitHub Release -> publish.yml trusted-publisher -> PyPI confirmed live; CHANGELOG 0.4.0 section = claims layer + cockpit + controls + embedder hardening). **@taOS-dev NOTIFIED** on the integration channel (bus 937): pin taosmd==0.4.0, plus the embedder migration answers (canonical id = arctic-embed-s; re-index = clear vector store then `taosmd reconcile` archive-driven re-embed, KG/archive untouched; re-embed BEFORE flipping the LiteLLM 'taos-embedding-default' alias to avoid mixed-space). OPEN integration thread: taOS PR #1334 standardizes arctic; they query via a LiteLLM proxy alias (not taOSmd's local ONNX path), so 0.4.0's preflight guards STANDALONE installs while taOS-deployed agents need the alias pointed at arctic + the existing-agent re-index. POSSIBLE follow-up they asked about: a one-shot `taosmd reindex` command. Pi minilm->arctic re-index = operational follow-up. **PRIOR (rev 1.44 confounded):** E-021 RESOLVED -> N-016 (answer-prompt lever EXHAUSTED: no generator-prompt variant beats baseline; baseline 64 / self_consistency 59 / evidence_grounding 59 / persona 63, rev 1.43). E-022 local-first head-to-head FIRST RUN (taOSmd vs MemOS, n=100) RAN but is CONFOUNDED: raw taOSmd 64 vs MemOS-lossless 88 is NOT a real loss. A CPU-only retrieval inspection of the 28 disagreements shows taOSmd retrieves the gold 25/27 (= MemOS 27/27) then loses it to its OWN context assembly: an FTS archive_text block bloats context to ~2x MemOS's and TRUNCATES the gold out of the 16000-char prompt in 12/27; in 15/27 both have gold in-prompt and taOSmd still misses (noise dilution from the 2x bloat). Plus taOSmd's vector path ran on minilm not its recommended arctic, and the prompt budget was not matched. Only 2/27 are a genuine retrieval miss (high-fan-in aggregation). Recorded CONFOUNDED (rev 1.44, master caef3c4); do NOT cite 88-vs-64 as a MemOS win. REAL FINDING = a fixable taOSmd context-assembly bug (the FTS block evicts reranked/gold content past the 16000 cap) = THE score lever (E-021 proved the answer prompt is dead; context assembly is where the points are; ~10 of the 28 disagreements look recoverable). HIGH-VALUE NEXT (in progress / queued): build the context-assembly fix (cap/reorder the FTS block so it cannot evict reranked/gold past the cap) + run taOSmd on arctic, then the FAIR E-022 re-run. memos_tree (5h as-designed config) still running on the GPU; OpenMemory = the second half. BUS HANDLE RENAMED @taOSmd -> @taOSmd-dev (Jay convention, acked by @taOS-dev; repo a2a docs updated 6c87a7c). USAGE fresh window (5h 1% / 7d 42%); resume pair armed for the 03:10Z window (primary 3f988b23, retry 96d840ee); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (scrubber) + PR #173 (preset ladder).** **PRIOR LATEST (Jun 22 late evening): E-019 maximem empty-fix re-run did NOT converge; STOPPED + GPU pivoted to the higher-value E-021. The /no_think + empty-guard fix produced correct answers but could not get the empty rate reliably under the 10% gate (subset attempts thrashed 5 to 100%, a qwen3-thinking interaction with the maximem answer/extraction path). After three audits of non-convergence I STOPPED the runaway subagent (TaskStop), killed the bench processes, and freed the 3060 (245MiB/0%). The valid maximem number is DEFERRED pending a deliberate fix (an extraction fallback that reads the answer when the "Answer:" prefix is absent, plus a retry-on-empty), NOT more prompt roulette; E-020 (strict re-judge) is consequently BLOCKED on a valid E-019 prediction set. The inflation catch still stands (rev 1.39: the 95-97% was fake). Recorded research-report rev 1.41 (master 4db13af). PIVOTED the freed GPU to E-021 (the generator-prompt ablation on our OWN LongMemEval harness, which has no empty problem, the higher-value experiment): E-021 is LAUNCHED + RUNNING on the bench GPU: bench/e012-judge-harness 936ae03, all 4 arms (baseline / self_consistency / evidence_grounding / persona) smoke-tested CLEAN (real non-empty answers, judge labels both ways - our LongMemEval harness has no empty problem), full e021_ablation.sh (n=100, fixed strict Qwen3-4B-Instruct judge, sequential arms) under nohup PID 52113. LOG: /home/jay/taosmd/benchmarks/results/e021_ablation_20260622_185257.log (arm boundaries marked "==== ARM: <name> ====", each ends with "Overall: X/100 (Y%)"; no separate JSON, grep ARM:|Overall: for the 4 headline numbers). MORNING: read the 4 Overall lines + per-arm sample predictions, record E-021 against the kill criterion (an arm ships only if it beats the baseline beyond noise under the fixed judge + a llama firm-up; persona expected a dud), then free the GPU. E-021 arms were built + unit-tested (936ae03, 22 tests). LESSON: a subagent tasked with an open-ended FIX loop can thrash (4 worsening subsets) - prefer a tightly-scoped mechanical task with a STOP-if-blocked guard, and isolate bench-host subagents (the arm-builder left this Mac's working dir on the e012 branch, which I restored to master). USAGE 5h 42% / 7d 37%; resume pair armed for the 22:10Z window (primary 0dc65c31, retry c4c81f1e); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (scrubber) + PR #173 (preset ladder); the valid maximem number + E-020 await the deliberate extraction-fallback fix.** **PRIOR LATEST (Jun 22 morning): SHIP DECISIONS signed off + E-019 maximem run RESOLVED INVALID (a real integrity catch). (1) Jay signed off BOTH ship decisions (MC): prefer_verified flip ON, and E-012 = rerank-default + self-verify opt-in. Both were already the shipped bare default; the one fix was the preset ladder, now PR #173 OPEN (ship/preset-ladder): Minimal gate off (lean opt-out), Quality gate ON (recommended accuracy tier), Integrity = strict (drops unverified claims, small recall trade-off). resolve_config gained explicit-pinned-value support so Integrity writes strict. controls.py + profiles.py + README/INTEGRATION/CHANGELOG + tests; full suite 970 pass. The research-report F-011/E-018 status flip to signed-off follows on merge. (2) E-019 (maximem harness, full LoCoMo 1986 Q) RAN -> INVALID per its pre-registered kill criterion. As-judged 95.27% overall / 96.75% cat 1-4, BUT raw-prediction inspection tripped criterion (a): 213/1986 (10.73%) empty generations, over the 10% threshold (qwen3.5:9b emits only reasoning tokens stripped to empty = the E-016 failure family). Worse, the lenient maximem judge scored 208/213 empties "correct" incl. 127 non-adversarial, inflating cat-1-4 to 96.75% vs ~88.5% honest. NOT PUBLISHED; the inspect-raw-predictions discipline caught a 7-9pp inflation + the degeneracy. Re-run pre-registered (generation-robustness fix + empty-answer-equals-incorrect scoring guard + stricter judge). Recorded research-report rev 1.39 (master 73841a4). GPU FREED (orchestrator + taosmd serve killed, ollama models unloaded, 3060 at 245MiB/0%). (3) Daily sweep: qmd still v2.5.4 NOT tagged (stays v2.5.3, no rebase/republish owed); watchlist quiet (exo #1879 closed, qmd #692 unchanged). USAGE 5h 12% / 7d 30%, headroom; resume pair armed for the 17:10Z window (primary e5f50e45, retry 721472ee); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (time scrubber) + PR #173 (preset ladder); the E-019 valid re-run is queued behind the generation-robustness fix.** **PRIOR LATEST (Jun 21 evening 3): Jay directed four things, all done or in flight. (1) PR #171 MERGED (squash, 6cabf74); master now carries the full cockpit through Phase 2b. A gitar-bot perf finding on #171 was fixed first: memoized the Explorer's visible-edge set so selecting a node no longer re-runs the d3 layout or resets pan/zoom (6e18ba8). (2) TIME SCRUBBER (Phase 2c) BUILT -> PR #172 OPEN: kg.graph(as_of) reconstructs the graph at a past instant via the valid_from/valid_to window, plus t_min/t_max bounds; threaded api/service/remote + GET /graph?as_of= (400 on non-numeric); the Explorer gains a debounced time slider with a date label, an 'as of' header badge, and a Now button (native range input, aria-valuetext). Spec docs/superpowers/specs/2026-06-21-dashboard-phase2c-time-scrubber.md; full suite 973 pass. (3) MAXIMEM EVAL pre-registered as E-019 (research-report rev 1.38, master 190c07c) with a VALIDITY kill criterion (discard if >10% degenerate generations or the sample-data fallback fired; first valid run published, no best-of-N). The taOSmd Provider adapter is staged at /tmp/maximem-harness (subagent-built + wiring-validated: ingest->/ingest/batch, search->/search hits, text->memory / confidence->score / source+ts+meta->source_evidence; clear() a logged no-op since taOSmd is zero-loss). GPU RUN IN FLIGHT: Jay freed the 3060 (confirmed idle 227MiB/0%); a background subagent runs setup -> a 20-Q LoCoMo subset with the validity gate (inspect raw predictions for the E-016 stub-degeneration failure) -> then the full 1540-Q run under nohup only if the subset is clean. Answer qwen3.5:9b, judge llama3.1:8b (different families, local Ollama via OPENAI_BASE_URL, zero external spend). E-019 gets recorded ONLY after I inspect the raw predictions. (4) The two ship decisions explained with options + recommendations: prefer_verified default flip = recommend flip-ON now (tri-judge clean, verify is async so low serve cost, it is the product thesis; tier-aware only if the Pi write cost bites); E-012 74.6% = recommend rerank-ON in the recommended retrieve config + self-verify as an opt-in verified-answer mode in the Quality/Integrity profiles (an answer-gen pass the consumer owns). Both already plumbed through controls/profiles, so each is a one-line default flip; offered to prep the two PRs on Jay's word. Directory PRs (#47/#76/#10/#46/#26) + qmd upstream #728/#663 all still open, no maintainer movement (normal). USAGE: 5h 62% / 7d 23%, headroom. Crons armed: audit 795131e3 (7,37), daily 74cba0e7, resume pair primary 23dbae74 + retry 81153c46 for the 21:10Z window.** **PRIOR LATEST (Jun 21 evening 2): resumed after the 5h reset (fresh window). PHASE 2b LIVE RECALL BUILT on PR #171 (now 'Explorer: live recall + All/Current toggle', 3 commits, full suite 969 pass, all jaylfc). KEY: the 'watch the agent recall live' headline needed NO new event bus - the KG already bumps last_accessed_at on retrieval (query_entity track_access default True, called by context_assembler), so a new read-only GET /graph/activations (kg.activations, windowed) surfaces recently-recalled entities; the Explorer polls it every 3s and pulses those nodes (expanding halo, fades after the window, reduced-motion = a static ring). Plus the All/Current toggle (active facts vs reveal superseded). Verified in a real browser (screenshot sent). This effectively completes the Memory Explorer (galaxy + temporal toggle + live recall); only a full time scrubber ('memory as of T') remains as an optional smaller follow-on. STATE: dashboard cockpit Phase 0-2a MERGED to master (42719ce); PR #171 OPEN for review/merge. Crons re-armed post-reset: audit 795131e3 (7,37), resume pair primary 23dbae74 ~22:12 BST + retry 81153c46 ~22:32 BST for the 21:10Z window. USAGE: 5h 16% / 7d 18%, full headroom. PRIOR LATEST (Jun 21 evening): PR #170 MERGED -> the whole dashboard memory cockpit (Phase 0 + 1 + 1.5 + 2a) is on master (squash 42719ce): taOS dark/light theming + toggle, Home/Overview over GET /stats (growth, verification donut, categories, top agents/projects, recent-memory browse) with all/user/per-agent scope + GET /memories, semantic categories (taosmd/categories.py), and the Memory Explorer KG galaxy over GET /graph (bundled d3-force, click-to-detail). README + CHANGELOG document it. Small follow-up PR #171 OPEN: an Explorer All/Current toggle (focus on active facts vs reveal superseded), the first temporal step. NEXT: Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation over SSE/OTel + a full time scrubber) is the remaining heavy/architectural piece, HELD for usage headroom (it needs the retrieve path to emit activation events). USAGE: 5h reached 89% = SOFT WIND-DOWN (Jun 21 ~15:30Z); work PAUSED and the cron is monitor-only until the 5h window resets at 16:10Z. Clean state: master 543dd97, PR #171 (Explorer All/Current toggle) pushed and open, working tree clean. Resume pair armed: primary eb9a4616 ~16:12Z (deletes the retry, gates, resumes), retry 1871171c ~16:32Z. On resume the queued work is Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation over SSE/OTel + a time scrubber) plus optionally merging PR #171; nothing in flight to salvage. PRIOR LATEST (Jun 21 late-afternoon 2): DASHBOARD PHASE 2a BUILT (Memory Explorer KG galaxy) on PR #170 = the whole cockpit Phase 0 + 1 + 1.5 + 2a (15 commits, full suite 967 pass, all jaylfc). 2a: kg.graph() (nodes = entities sized by degree, edges = triples with an `active` flag from valid_to/superseded_by) + a read-only GET /graph; frontend dashboard/src/components/ForceGraph.tsx (d3-force bundled, ~20KB, offline; static settled layout, reduced-motion-safe; zoom/pan/hover/click; superseded edges faded + dashed; sr-only text fallback) + ExplorerView (an Explorer tab with a click-to-detail panel of an entity's relations). Spec docs/superpowers/specs/2026-06-21-dashboard-phase2-memory-explorer.md (6a27f3b). Verified in a real browser in both themes (screenshots sent). NOTE colour-by-type is wired but the seeded demo entities were all type 'unknown' so the galaxy looked monochrome; real typed entities will be multicoloured. NEXT (Jay's call): Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation streamed over SSE/OTel + a time scrubber; needs the retrieve path to emit activation events). PR #170 is now large (Phase 0 to 2a); offered Jay to merge as one or split Phase 2. USAGE WATCH: 5h at 76% and climbing within the window resetting 16:10Z (still under the 90 soft wind-down but close; watch next audit). PRIOR LATEST (Jun 21 late-afternoon): DASHBOARD PHASE 1.5 COMPLETE (1.5a scope + 1.5b categories) on PR #170 = the whole cockpit through Phase 0 + 1 + 1.5 (13 commits, full suite 964 pass; all jaylfc). 1.5b categories DONE: taosmd/categories.py = a fixed Memoire-style taxonomy (Identity & Preferences / Activities & History / Work & Learning / People & Social / Communication & Content / Sources & Access) + a deterministic LLM-free keyword classifier (classify + category_counts); api.dashboard_stats gains a `categories` field (read-time classification of the scoped texts, capped 2000); the Overview shows a Top-categories card above Top agents/projects ('1 inside 3'). The librarian-LLM (enriched) and KG-type classification are clearly marked `# upgrade-path:`, not silently faked. Verified in a real browser (screenshot sent to Jay). PR #170 awaits Jay's review/merge. NEXT (Jay's call): Phase 2 Memory Explorer (D3 KG galaxy + the live-recall animation Jay liked) or merge #170. USAGE: 5h 59% and climbing, still well under the 90 soft wind-down. PRIOR LATEST (Jun 21 mid-afternoon): DASHBOARD PHASE 1.5a BUILT on PR #170 (Jay's feedback: view individual agents AND the user's memory; categories were missing from the Overview). Spec docs/superpowers/specs/2026-06-21-dashboard-phase1.5-scope-and-categories.md (6ed5b19). Jay's design calls: categories = a Memoire-style semantic set classified by the librarian when LLM-enriched, KG-derived fallback when not, kept alongside agents/projects ('1 inside 3 with 2 as fallback'); user memory = a reserved `user` namespace (option 2, for the PiecesOS/share-to-collect personal corpus), surfaced as All/per-agent/user scopes with All as the interim default. 1.5a (scope) DONE on branch feat/dashboard-cockpit-phase01: scoped GET /stats?scope= + a new GET /memories browse endpoint + the reserved user namespace (api.dashboard_stats(scope) / api.list_memories + archive scoped helpers daily_counts/recent/top_by/scoped_total/list_memories), and the Overview gained a scope selector (All / User (you) / each agent) plus a Recent-memories browse list. Full suite 959 pass; verified in a real browser (screenshot sent to Jay). 1.5b (categories) is the remaining piece: taosmd/categories.py classifier + enriched/KG-fallback + a categories field on /stats + the Overview Top-categories card + a Memory-view filter; teed up, awaiting Jay's go. PR #170 now = Phase 0 + 1 + 1.5a (11 commits). PRIOR LATEST (Jun 21 afternoon): DASHBOARD PHASE 0+1 BUILT -> PR #170 (review, not self-merge). Branch feat/dashboard-cockpit-phase01. Phase 0 theming: the dashboard is re-skinned to taOS macOS graphite tokens (sourced from tinyagentos/desktop/src/theme/tokens.css), dark + light with a top-bar toggle (data-scheme, localStorage), minimal component churn. Phase 1 Home/Overview: a new read-only GET /stats (api.dashboard_stats + service/remote wrappers) over archive.stats/daily_counts/recent/top_by + claims.rate + projects, feeding a Home tab with stat cards, a memory-growth SVG chart, the verification-coverage donut (the hero), top agents/projects, and a recent-activity feed, all hand-rolled SVG, offline. Full suite 956 pass (+4); verified in a real browser in BOTH themes (screenshots sent to Jay). Plan: docs/superpowers/plans/2026-06-21-dashboard-phase0-1.md. GOTCHAS for the next session: (1) the aggregator is named dashboard_stats to avoid colliding with the existing per-agent service.stats; (2) do NOT prepend /usr/bin to PATH for python (it selects system py3.9 and breaks X|None collection) - the project python is 3.14 and the default profile PATH already has python3=3.14 plus curl/security. NEXT: Phase 2 Memory Explorer = the D3 knowledge-graph galaxy plus the 'watch the agent recall live' animated graph (nodes pulse/reflow per memory-activation, streamed over SSE/OTel) that Jay liked; spec it with the LinkedIn real-time-viz patterns + the ArcRift force-graph approach when Jay is ready. SPAM-GUARD: posted a factual clarification on PR #47; recommended a ~2-week cooldown on NEW awesome-list PRs under jaylfc. PRIOR LATEST (Jun 21 early-afternoon): DASHBOARD MEMORY-COCKPIT brainstorm advanced and two specs are committed to master. Jay supplied Memoire (mymemoire.ai) + ArcRift + PiecesOS as references; vision locked and decomposed. Committed: vision/roadmap `docs/superpowers/specs/2026-06-21-memory-cockpit-vision.md` (5e6220f) and Phase 0+1 design spec `docs/superpowers/specs/2026-06-21-dashboard-phase0-1-theming-overview.md` (324f30f). FRAMING (Jay): adapt-not-clone. Three capture lanes feed the one provable engine: (1) share-to-collect via a browser extension into source-typed collections with per-type enrichment (screenshot / text / transcript / metadata / comments-correction scan), (2) opt-in PiecesOS-style ambient capture (OCR active windows, per-app toggles, local + air-gappable) = the open/auditable/zero-loss answer to Pieces' black box, (3) the existing agent/conversation memory. Cockpit surfaces: Home/Overview, Sources, Memory Explorer (D3 KG galaxy, ArcRift-style), Documents, AI Assistants. THEME SOURCE RESOLVED: adopt taOS desktop tokens (`tinyagentos/desktop/src/theme/tokens.css`, macOS graphite + `data-scheme` light) so taOSmd and taOS feel like one product. Phase order 0 theming -> 1 Home/Overview -> 2 galaxy -> 3 share-to-collect -> 4 sources/permissions -> 5 documents -> 6 ambient. STARTED with Phase 0+1: Jay approved the design, the spec is written; AWAITING Jay's spec review before writing-plans -> subagent build. Phase 1 = a new `GET /stats` endpoint over archive.stats/daily_counts + access_tracker.stats + claims.rate + projects/agents, feeding a Home tab (stat cards, memory-growth SVG, verification-coverage donut [the hero], top agents/projects, recent activity), all hand-rolled SVG, offline. SPAM-GUARD: PR #47 drew the bot advisory again ("identical title to 30 repos" is INACCURATE; the real reuse was 5 repos on Jun 5, the rest is just PR volume); posted a factual clarification on #47 (Jay asked) and recommended a ~2-week cooldown on NEW awesome-list PRs under jaylfc. The 5 directory accuracy PRs stay open (#47/#26/#46/#10/#76). PRIOR LATEST (Jun 21 midday): memory-controls feature MERGED to master (802d3fa, PR #169 squash-merged with Jay's go, full suite 952 pass; branch deleted). DIRECTORY CAMPAIGN REFRESH: submitted 5 distinct accuracy/freshness PRs (good-faith updates to already-accepted entries, deliberately NOT the identical-title mass pattern that tripped the spam-guard): Zijian-Ni/awesome-ai-agents-2026 #47 (fix the live "97% end-to-end Judge" mislabel -> 97% Recall@5 + add 74.6% Judge + lead with the verify wedge), aristoapp/awesome-second-brain #26 (rokpiy hold LIFTED by Jay; profile to v0.3.0 + source-linked verifiable recall + labelled numbers, conservative framing kept), TeleAI-UAGI #46 (label Recall@5 + verify clause), DEEP-PolyU #10 (KG-first refresh + provenance + offline), IAAR-Shanghai #76 (table format-constrained; docs/benchmarks link replaces "No official website"). Campaign now 5/5 landed; watchlist + directory-listings memory updated. NEW THRUST (Jay): dashboard-polish initiative - make the standalone dashboard highly polished (taste + impeccable skills) with visual graphs + richer memory interfaces. Research done: the community wants white-box/transparent memory + interactive KG viz + fact-currency/invalidation views, which IS our provable-memory differentiator. Proposed direction: Overview/health (verification-coverage donut, extraction-error rate, ingest sparkline) + interactive temporal KG view + provenance memory browser (trace a fact to its source span) + a timeline + the shipped Settings; hard constraint offline/no-CDN self-contained (lean hand-rolled SVG charts + a light force layout, no Cytoscape CDN). AWAITING Jay's design-inspiration screenshot before brainstorm -> spec -> build. Also open: maximem "claimed vs observed" outreach (their open harness = maximem-ai/memory_and_context_eval_harness; strongest fit, draft-and-send with Jay); taosmd.taos.my own-site idea (hand a brief to @taOS-website-dev, closes the "No official website" gap). PRIOR LATEST (Jun 21 PM): MEMORY CONTROLS FEATURE SHIPPED on branch `feat/ship-validated-wins` (now MERGED, see above). Five components, full suite 952 pass (+21 new): (A) typed controls registry `taosmd/controls.py` = single source of truth for dashboard+API+config+docs; (B) `GET`/`POST /controls` API (settings+schema, presets, per-control writes, validation); (C) dashboard Settings panel (presets one-tap + Advanced per-control toggles, schema-driven, offline/ARIA; Playwright-verified the Integrity preset round-trips and persists); (D) README "Configuration and controls" section documenting every control with pros/cons/cost/scope + a drift-guard parity test; (E) `docs/INTEGRATION-memory-config.md` refreshed to the /controls contract for @taOS (corrected the stale ms-marco reranker + the prefer_verified-off default). KEY INTEGRITY FIX (F, the reconciliation): discovered the retrieval levers (reranker/fusion/adjacent_turns) were owned by the RECIPE and NOT read from the controls config the dashboard writes, so four of five "runtime" controls would have silently done nothing. Fixed: `api.search` now overlays persisted runtime controls on the resolved recipe per query (recipe stays the baseline, override only when explicitly set); `late_interaction` reclassified runtime->store (token vectors written at ingest = needs re-index); reranker choices trimmed to the implemented (off, bge-v2-m3); and `profiles.py` prefer_verified switch wrote a DEAD key (`claims.prefer_verified`, read by nothing) -> repointed to the live `controls.prefer_verified` with an explicit off_value so install-time consent reaches the gate and Minimal/Quality can opt out against the now-default-on gate. Commits on the branch: 0a86b1e (API), cfc98c4 (dashboard), 2f5b0fa (reconcile+wire), 48b57e9 (README), e74787a (@taOS handoff). OPEN: push branch + open PR; the @taOS BUS RELAY is still owed (no one-shot bus post from this Mac without the remote host; left for the hourly bus cron / Jay to relay the handoff line on the integration channel). PRIOR LATEST (Jun 21 AM): the ENTIRE E-015 to E-018 pipeline is COMPLETE; pipeline idle, GPU released, everything remaining is Jay's decision. E-018 TRI-JUDGE COMPLETE: prefer_verified drives served-hallucination to 0.000 under ALL 3 judges at n=250 (off baselines llama 0.040 / qwen-instruct 0.056 / gemma 0.036), at no accuracy cost (judge -0.016/+0.020/+0.024) and R@K within 0.02; strict over-trades all three, so the kill criterion's tri-judge clause is MET and prefer_verified fully qualifies for the default flip on the evidence (Jay's final call per never-silently-enabled). Recorded research-report rev 1.37 (master 17162c0); provenance bench/e009-claims-gate-probe 00bca65; [GPU RELEASE] bus #872. The 2 greenlit experiments are both DONE (E-016 resolved valid, E-018 tri-judge complete); the ship batch (BEAM published, 74.6% surfaced, PR #168 merged, @taOS handoff) was already done. Jay's open decisions: flip prefer_verified default (now fully evidenced), E-012 opt-in verified-answer ship, lightning.ai frontier tier.** **PRIOR (Jun 20 eve): GPU driver FIXED, E-016 RESOLVED valid, E-018 tri-judge IN PROGRESS.** GPU: the dnf-update NVML mismatch was fixed by rebooting the bench host (pinned to Fedora first); RTX 3060 back at driver 595.71.05. E-016: the earlier INVALID run was a prompt-truncation bug (ollama default num_ctx 4096 too small for the ~4500-token EventQA prompts -> 1-2 token stubs); root-caused by instrumenting the generation call, FIXED with num_ctx=8192 (feat/eventqa-runner a4135ce; slice 35%->75%, degenerate 12/20->0/20). Valid re-run (100 Q/arm): self-verify off 75.0% / on 66.0% -> self-verify does NOT transfer, regresses 9pp on EventQA (reinforces N-013); 75% is a solid local-tier number on a hard ICLR-2026 benchmark. Recorded research-report rev 1.36 (master 335fb46); provenance feat/eventqa-runner d25e7eb. E-018 tri-judge completion RUNNING (run #2 qwen3-4b-instruct judge PID 1350591 watcher bssp502d0; run #3 gemma4:e2b next). LESSON: a self-inflicted pkill bug (my SSH command contained the literal "benchmarks/eventqa_runner.py", so pkill -f killed the SSH session -> exit 255, looked like an sshd ban for ~1h); fixed by killing by PID + self-safe bracketed detection. **PRIOR (Jun 20 PM): Jay greenlit a ship batch + 2 experiments. SHIP BATCH ALL DONE: (1) BEAM local-tier numbers PUBLISHED (47%/40%@1M) in README + benchmarks.md (master 95a5b71); (2) the 74.6% end-to-end Judge SURFACED on the README front page (was stale "in progress"); (3) PR #168 smart-installer REVIEWED + MERGED (squash, master 7a502de: taosmd/profiles.py + setup_prompt.py + setup-prompt CLI + docs/INSTALL-AGENT-PROMPT.md; merged master in cleanly, full suite 917 pass, CLI verified probing tier->profile->consent, 0 em dashes); (4) @taOS tier/config/toggle HANDOFF shipped: docs/INTEGRATION-memory-config.md (master 074ff7a) + bus pointer #820 on the integration channel. The 2 greenlit EXPERIMENTS (E-016 generation-robustness fix + re-run, E-018 tri-judge completion) are BLOCKED: the Fedora bench-host GPU is DOWN ("Failed to initialize NVML: Driver/library version mismatch, NVML 595.71") after a driver update; needs a reboot or nvidia module reload by Jay before either can run. Working tree clean, all pushed. PRIOR (Jun 20 AM): the E-015 to E-018 experiment pipeline is COMPLETE; E-015/E-017/E-018 resolved + recorded; E-016 RAN but the result is INVALID (harness-blocked, see below). Session ACTIVE.** Usage reset clean (7d 4%, 5h 34%), so the Jun-18/19 wind-down is over and normal work resumed. **E-017 RESOLVED -> F-014 reaffirmed at 1M:** the hardened BEAM-1M re-run (n=200, N-014 empty-answer-FAIL guard + raised timeout, feat/beam-runner f7dc94e; shipped 1M config: arctic + tight retrieval + rerank + self-verify off + qwen3.5:9b gen + Qwen3-4B-Instruct judge) = 40.0% (80/200), avg 0.373, errors=0, abstention 100%, ZERO empty fires; +2.5pp from 37.5% is inside the 3.5pp SE, so the 1M number stands as measured. Recorded research-report rev 1.33 (master f4db2db); log committed feat/beam-runner ca95fa3. **E-018 RESOLVED -> reinforces F-011:** claims_gate_probe E-009 larger-n DONE (250 QAs, 1853 claims, strict llama3.1:8b judge, readtimeouts=0): off/prefer_verified/strict served-hall = 0.040/0.000/0.000; prefer_verified judge -0.016, R@K -0.008 (both within 0.02) so it meets the necessary conditions; strict over-trades (judge -0.024, R@K -0.060). Reproduces F-011 at n=250; only the llama judge ran so the tri-judge clause stays open and the prefer_verified default flip stays Jay's call. Recorded research-report rev 1.34 (master 3755c22); provenance committed bench/e009-claims-gate-probe 23dc643. **E-016 RAN but INVALID (harness-blocked):** the MemoryAgentBench EventQA two-arm run (feat/eventqa-runner, qwen3.5:9b + arctic + rerank, self-verify off vs on, 100 Q/arm) completed (off 19.0% / on 16.0%) but the numbers are not published: about half the generations (51/100 off) degenerated to 1-2 token stubs on the long contexts, so both arms sit at/below the 16.7% six-way-MC chance floor and measure a generation artifact, not the stack or the self-verify transfer. Controlled probes ruled out num_ctx, the option format, and the think:false param, so the trigger is in the real retrieved-context interaction and needs the runner instrumented to replay a degenerate prompt. Recorded research-report rev 1.35 (master 17e03d4); provenance feat/eventqa-runner 9809430. The self-verify transfer question stays open (N-013 remains the evidence it is task-specific); a generation-robustness fix + re-run is pre-registered. GPU RELEASED (qwen3.5:9b unloaded, 3060 free, bus #775). No benches queued; pipeline idle. **E-015 RESOLVED -> N-015** (tau-sweep, research-report rev 1.31; quantified the inert gate, FTS-source half deferred). **E-016 feasibility gate** (rev 1.32): BiTempQA phantom, TEMPO not memory-shaped, retargeted to MemoryAgentBench EventQA. Everything from here (publish BEAM, merge PR #168, E-012 opt-in ship, E-009 prefer_verified flip) stays Jay's decision. **PRIOR (Jun 19 ~19:15Z): E-014 (MemX R1 reject-gate) RESOLVED -> N-015.** Judge-free R@K ablation on LoCoMo (200 QAs, gate off vs on, tau 0.50, arctic): R@K identical 0.73/0.73 (F1/BLEU byte-identical too) - the gate NEVER FIRES because arctic cosines clear 0.50, so it is inert at the default tau on this embedder. Formally clears the 0.02 kill-criterion floor (0.00 drop) but the pass is hollow: safe, zero recall cost, but no measurable benefit on answerable LoCoMo; tau needs per-embedder calibration and the gate value needs an unanswerable-query benchmark (cf. E-005). Ships default-off opt-in. Recorded research-report rev 1.29 (master 573182d), N-015; logs committed feat/memx-levers c7c1a43; GPU freed + models unloaded. This closes the MemX experiments Jay asked about (BEAM already done; SuperLocalMemory was recon; BiTempQA/BEC/MemoryArena remain surveyed-not-run external benchmarks). **PRIOR: E-013 RESOLVED, F-014 CONFIRMED.** Jay rebooted the bench host back into Fedora (GPU online). Built the N-014 fix on feat/beam-runner (f7dc94e: empty-answer=FAIL guard in judge_prediction + configurable TAOSMD_BEAM_LLM_TIMEOUT default 120; 23 beam tests pass), ran the hardened BEAM-100K re-confirm = 49.0% / avg 0.455 / errors=0, abstention 100%, empty-answer guard fired ZERO times. Per the pre-registered kill criterion F-014 STANDS as measured and N-014 is confined to slow-CPU generation (no empty inflation on GPU; the 100% abstention is genuine). Full 1M re-run judged redundant (zero empties on GPU is tier-independent), not run. Recorded research-report rev 1.28 (master 7511753); [GPU RELEASE] posted + models unloaded, 3060 free. **Company-name / branding scrub (convention relayed by @taOS-dev, Jun 18): COMPLETE.** The attribution change (pyproject author + README footer to 'the taOS project', convention 2) is landed (ac832ab). The loud first scrub commit, whose subject named the former company, was reworded to 'chore: update license and packaging metadata' via filter-branch (message-only, content byte-identical) and force-pushed after Jay greenlit briefly lifting master branch protection (allow_force_pushes flipped true, pushed, restored to false, verified identical to the captured backup). master is now 02d916e; the former company name appears NOWHERE in the tracked tree or in any recent commit message. The 3 deep historical commits (about 180 to 429 back) were intentionally left untouched, since rewriting them would be a huge, conspicuous history rewrite counter to keeping this quiet. Parked reword branch + filter-branch backup ref cleaned up. **Earlier (Jun 18), now resolved:** (1) **E-009 tri-judge confirm DONE + recorded (research-report rev 1.27, master 2820165).** The lenient gemma4:e2b arm is NO-HARM (prefer_verified identical to off: judge 0.490, R@K 0.615, served-hall 0.000) but did NOT reproduce the elimination benefit, because its off baseline already had 0.000 served-hallucination (vs 0.040 in F-011), so the gate had nothing to remove; the difference tracks a config difference (vector-only, no reranker) and is undiagnosed, so the benefit is config-dependent. strict over-trades (R@K 0.615 to 0.455). The prefer_verified default flip still rests on the F-011 strict-llama run + Jay sign-off; the lenient arm only adds no-cost corroboration. Numbers captured in-session (result JSON e009_gemma_20260618_102802.json is on the Fedora disk, now OFFLINE because Jay rebooted the bench host into WINDOWS). (2) **VPS 30B-CPU BEAM run DONE and it surfaced a real harness confound -> N-014:** the runner's hard 60s LLM timeout stores EMPTY answers on slow generation, and the ported mem0 nugget judge PASSES an empty answer on abstention/negative rubrics, so blanks inflate the score. qwen3:30b-a3b on the CPU-only VPS read 55% with 10/20 empties, then 20% honest at a 240s timeout (0 empties, n=20); code-confirmed in beam_runner.py. So a frontier model on CPU did NOT beat the tuned local 9B. F-014's GPU 9B numbers (47% / 37.5%@1M, errors=0) probably stand but cannot be cleared from existing logs (no per-answer lengths recorded; abstention sits at 100%, the most exposed category), so **E-013 is pre-registered: a hardened BEAM re-confirm** (empty-answer=FAIL guard + raised timeout) with a kill criterion written before the run, QUEUED for the next GPU window after E-009. Recorded research-report rev 1.26 (N-014 + E-013 + F-014 caveat + abstract clause), pushed to master **7562327**. (3) **MemX R1 reject-gate BUILT** on feat/memx-levers (96b7725): apply_reject_gate(tau=0.50), env-gated TAOSMD_REJECT_GATE/_TAU default-off, 11+8 tests; the LoCoMo --strategy full ablation + a BEAM wire-in are PENDING the GPU (gate fires via retrieve() only, BEAM's retrieve_memories() needs an explicit call). MemX Lever 2 (freq/importance rerank) STUBBED honestly: no per-memory access-count tracking exists yet (upgrade-path TODO). **USAGE 7d 93% (SOFT WIND-DOWN + WEEKLY GATE, Jun 18 ~15:00Z): monitor-only, no new heavy work until the 7d window resets Jun 21 02:00Z; HARD STOP at 98%. 5h reset to 0% at 14:40Z (next 5h resets 19:40Z). GPU: the Fedora 3060 is OFFLINE (Jay took the box and had me reboot it into Windows via rebootwin); E-009 was collected first and released cleanly. No GPU work (E-013 hardened BEAM re-confirm + anything else needing the card) until the host is back in Fedora. Weekly job-pack refresh (docs/agent-jobs/: job-001 em-dash sweep, job-002 cross-encoder path fix, job-003 http-server dead import + README) is DUE before the Jun 21 dark period.** 5h 57% (resets 14:40Z). Resume pair armed (primary f7ac3a47 14:42Z, retry c720509d 15:02Z); audit a0855fff + daily sweep 74cba0e7 stay monitor-only. **feat/memx-levers (96b7725) and feat/beam-runner (a6006e5) both PUSHED** (owed PRs to master, deferred past wind-down). **Smart-installer PR #168 awaits Jay merge.** The publish-BEAM decision stays Jay's and now waits on the E-013 hardened number (publishing 47-49% before the empty-answer audit would be premature). (SUPERSEDED Jun 16 pause: Jay is rebooting the Mac mini, so this session + its session-only crons [audit de5c15ff, resume pair 30223ec8/30224b4d, daily sweep 4ef41aff] DIE; disk/git/memory persist; on the new session re-arm them per "On arrival" below, and the Fedora 3060 is JAY'S until he frees it [check bus + nvidia-smi before any re-claim]. BEAM is the active thrust, config + retrieval-recall sweeps DONE (47-49% plateau confirmed by BOTH; no config or retrieval lever beats it; wider/lexical retrieval REGRESS; self-verify is BEAM-neutral); 3060 RELEASED + unloaded. Runner BUILT on branch feat/beam-runner [benchmarks/beam_runner.py, ports mem0 own nugget-judge methodology + 10 categories, 13 tests]. FIRST RESULT: BEAM-100K validation [5 convs, 100 Qs, full stack arctic+rerank+self-verify+qwen3.5:9b+Qwen3-4B-Instruct judge] = **31% pass / avg_score 0.278** [results benchmarks/results/beam_100K_*.json scp'd to /tmp/beam_100k_result.json]. DIAGNOSIS [corrected, NOT over-abstention]: the bottleneck is RETRIEVAL PRECISION on long-context needles - retrieval pulls ~19 chunks/Q [zero zero-retrieval] but the exact-fact chunk is often missing; PASS vs FAIL retrieved_count ~equal [18 vs 20] so MORE depth alone will not fix it; only 6-8 abstentions, most failures are genuinely-wrong answers. IMPLICATION: the judge panel [Jay's idea] is a final-number firm-up, NOT the lever [wrong answers fail under any judge] - so score-up retrieval/gen FIRST, judge-panel on the best config. STRATEGY: BEAM rewards FRONTIER generation [mem0 64-70% @1M with GPT-4-class]; our local 9B lands honestly behind, so the play is a TRANSPARENCY one [early, fully-provenanced, methodology-pinned local-tier number] not a score-win. CONTEXT-AMOUNT SWEEP DONE (BEAM-100K, 5 convs, same stack, only the retrieval-depth env changed): rich [40 chunks / rerank15 / 24K ctx] = 9%, baseline [12 / rerank8 / 16K] = 31%, **TIGHT [6 / rerank4 / 6K] = 47%** [best]. CLEAR LEVER: intelligent context release - LESS, cleaner context wins big on the 9B [+16pp baseline->tight; more context BURIES the small model]. This is on-thesis (same as E-012). xtight [4 / rerank3 / 4K] = 47% (no gain over tight, so 47% is the BEAM-100K plateau under the 9B). BEAM-1M n=200 head-to-head MEASURED = **37.5% (75/200, avg_score 0.357)**; mem0 frontier 64-70% @1M, so local-9B lands honestly behind as predicted (per-category: abstention 100% / preference 80% strong; event_ordering 0% / knowledge_update 10% weak). qwen3.6:35b-a3b LOCAL generator arm TRIED + ABANDONED: it CPU-offloads on the 12GB 3060 [0/100 in ~40min, GPU starved at 35% util], so **9B is our practical local ceiling, not 35b** [[feedback_gpu_contention]]. CONFIG SWEEP DONE Jun 17 ~00:50Z (Jay freed the 3060 by waking it from LAN + rebooting Windows->Fedora; ran the 5 remaining arms, [GPU RELEASE]+unload posted). RESULT: NO config lever beats the 47% tight-retrieval plateau beyond n=100 noise. All 7 arms (pass% / avg_score): base_chunk100 47.0/0.436, chunk50 47.0/0.417, chunk200 49.0/0.425, chunk300 44.0/0.394, sentences 47.0/0.425, chunk100_nosv 49.0/0.455, chunk100_sv_decomp 47.0/0.436. Best nominal chunk200 + chunk100_nosv at 49% (+2pp, within noise of base). KEY FINDING: self-verify gives NO BEAM benefit (chunk100_nosv ties the top pass-rate AND leads avg_score 0.455), unlike LongMemEval F-013 - BEAM failures are wrong answers, not abstentions, so the CoVe verify/abstain pass cannot help here. Sweep was benchmarks/beam_campaign.sh (chunk size 50/100/200/300, sentence-mode, self-verify off, decompose on; branch feat/beam-runner, 9B gen + Qwen3-4B-Instruct judge). ARMS DONE: base_chunk100_sv **47.0%** (avg 0.436), chunk50_sv **47.0%** (avg 0.417, smaller chunks no gain); arm3 chunk200_sv interrupted. ARMS PENDING on resume: chunk200, chunk300, sentences, chunk100_nosv, chunk100_sv_decomp. Methodology + provenance: benchmarks/beam_campaign_resume.sh + log benchmarks/results/beam_campaign_resume_20260616_221030.log (branch feat/beam-runner). NO clear winner to carry to 1M (all within noise), so BEAM-1M stays 37.5% (n=200); OPTIONAL follow-up = a larger-n (n>=500) confirm of chunk100_nosv vs base, since self-verify-off is faster AND nominally >= on BEAM, if Jay wants a firmer read. NET: the score-up thesis is confirmed - local-9B plateaus at 47% on BEAM-100K (mem0 frontier 64-70% @1M), so the honest play is the transparent, fully-provenanced local-tier number, NOT a config-tuned score win. RECALL SWEEP DONE (Jun 17, Jay's "push for more"): tight retrieval 49% beats wider (recall_wide 43, recall_wider 46) and lexical-heavy (fts_heavy 41), so the plateau is NOT a retrieval-recall gap either - confirmed across TWO independent sweeps (config + recall), recorded research-report rev 1.24 + benchmarks/results/beam_recall_20260617_130445.log. The PUBLISH DECISION (publish the honest 47-49% / 37.5%@1M now vs keep pushing) is now ripe for Jay. ALSO DONE THIS SESSION: smart-installer BUILT -> PR #168 (profiles registry + setup-prompt CLI + static prompt, 17 tests, awaits Jay merge); SuperLocalMemory competitor dig DONE (first local-first competitor; their numbers are vendor-graded best-of-5 single-lenient-judge, zero-loss is the clean fault line; recs in reference_memory_landscape_jun2026). QUEUED: E-009 tri-judge confirm (next GPU window, mind the qwen3:14b co-resident deadlock - use the small judges) before the prefer_verified flip; E-012 opt-in/config ship. NEXT after that: record a BEAM finding in the research report once the series settles. RECORD a BEAM finding in the research report once the series settles. Jay's call publish-baseline-now vs score-up-first [my lean: score-up first; 47%/37.5% is already a far better, fully-provenanced story than 31%]. E-012 DONE [F-013 74.6%]; ship=opt-in verified+rerank QUEUED; PRs merged; smart-installer SPEC written+committed [docs/superpowers/specs/2026-06-16-smart-installer-design.md] AT THE REVIEW GATE awaiting Jay's OK before writing-plans.)

## Current state

- **master** is healthy: about 886 tests passing. Shipped today (Jun 13): **PR #162 arctic-embed-s as the low-tier dense default** (ships F-010 -- R@K +0.157, judged +0.0565 full-1540, same 384-dim and latency; existing installs keep MiniLM via the config default, only fresh setups fetch arctic) and **PR #163 the v2 claims layer "Provable Memory"** (default-off: `taosmd/claims/` -- zero-loss ClaimStore, cross-family fail-closed entailment verifier, demote-not-delete recall gate, archive-span provenance, async verify-pass, `taosmd verify`/`claims status` CLI). Earlier this cycle (Jun 12): temporal date-range lever (PR #157), pause/resume checkpointing for the LoCoMo runner (PR #158), compact mode on `GET /a2a/messages`, the grants-gate auth fix (`faaaf3c`), project-scoped grants (#151), task-graph (#150), batch ingest + bm25 mode (#149), TTL filter + three-number summaries (#155), admin surface (#153), research report first edition (#156).
- **taOS #744 is CLOSED, e2e-verified 3/3** against the real registry feeds (live grant pass with claim-binding, expired-grant 403, revoked 403). The exercise found and fixed the gate-bypass bug above, the argument for e2e over unit coverage.
- **INTEGRITY CORRECTION (Jun 14, research report rev 1.19): the LongMemEval-S "97.0%" headline was Recall@5, mislabelled "end-to-end Judge accuracy" since an April docs reframe.** Proof: benchmarks/results/enhanced_20260413_133215.json (query_expand, top_k=5) is the source of the published per-category table and is a Recall@5 hits metric; no Judge run ever produced 97.0% (the Judge harness reproduces ~22.6%). Corrected on every public surface (README, benchmarks.md, research-report F-001, AGENTS.md, CHANGELOG) to **"97.0% Recall@5"** with provenance; Jay approved. Recall@5 still beats MemPalace 96.6% head-to-head, so the corrected claim is strong. **E-012 pre-registered** to measure a genuine end-to-end Judge number. The data was never lost (public benchmark + valid local copies).
- **Positioning (Jun 14): README now CO-HEADLINES "Provable, auditable memory" alongside the Recall@5 number.** An independent Letta/Mem0/Graphiti/Cognee comparison found provenance + zero-loss + audit trails unaddressed across the field, so that gap is our lead (the zero-loss archive + F-009/F-011 claims layer answer it). Copy propagated to @taOS for their README section + the website. Landscape intel + the open-ground confirmation are in memory [[reference_memory_landscape_jun2026]]. Research tooling set up: agent-reach (pipx) + no-auth methods (Reddit RSS, HN Algolia, GitHub, Jina) [[reference_research_methods_noauth]]; daily landscape-monitor cron armed. mem0's Apr-2026 numbers (94.4 LongMemEval / 92.5 LoCoMo) have ZERO disclosed judge methodology, a direct contrast to our provenance.
- **Other headline: LoCoMo leader 0.748/0.394/0.659 (tri-judge), unchanged and correctly labelled.** Earlier pre-registered negatives stand: N-008 (projected ColBERT no upgrade), N-009 (surprise-boundary chunking a coverage artifact), N-010 (write-skip under the shipping floor), N-011 (surprisal worst retention signal).

## In flight

- **E-012 SCORE-UP -- CONFIRMED + judge-robust (F-013), GPU released.** Full-500: tuned-depth baseline 56.8% (284/500), winner rerank+self-verify **74.6% (373/500)**. The cross-family llama3.1:8b firm-up reproduced BOTH numbers exactly (identical per-category counts), so the +17.8pp gain is not judge-specific. Recorded F-013 (research-report rev 1.22, firm-up appended) + provenance benchmarks/results/e012_ablation_results.md, all pushed to master. [GPU RELEASE] posted (bus #474). Levers on PR #165 (bench/e012-judge-harness): rerank (TAOSMD_RERANK), self-verify (TAOSMD_SELF_VERIFY), decompose (TAOSMD_DECOMPOSE, a DUD - drop), sampling (TAOSMD_SAMPLE_SEED). OPTIONAL/LATER: lenient gemma4:e2b re-judge (needs a prediction-cache to dodge the VRAM deadlock); qwen3.6:35b-a3b bigger-gen arm (23GB, CPU offload); full-distractor-set run (pre-registered). **SHIP DECISION = JAY's** (never-silently-enabled): self-verify is general + model-agnostic; proposal is an opt-in verified-answer mode + rerank in the recommended retrieve config, NOT a silent core default (taOSmd serves memory; answer-gen is the consumer path). Also pending: PR #165 review/merge (+ #164/#166/#167). Harness env: TAOSMD_OLLAMA_MODEL=qwen3.5:9b, TAOSMD_JUDGE_MODEL=hf.co/unsloth/Qwen3-4B-Instruct-2507-GGUF:Q4_K_M, TAOSMD_DECOMPOSE_MODEL=qwen3.5:9b.
- **E-009 RESOLVED to F-011: the prefer_verified claims gate PASSES its kill criterion.** Full 10-conv LoCoMo sweep (200 QAs, 1853 claims, llama3.1:8b judge, `benchmarks/results/e009.json`): served-hallucination 0.040 -> 0.000 (eliminated), judged accuracy 0.425 -> 0.445 (no drop, +0.020 within noise at n=200), R@K 0.615 -> 0.610 (-0.005, inside the 0.02 bound). strict FAILS (judge -0.065, R@K -0.065, trades accuracy for purity) and stays opt-in. Robust claim: **prefer_verified eliminates served-hallucination at NO measured accuracy cost** (the +0.02 is noise; do not claim a gain). Recorded report rev 1.18 + F-011. **REMAINING before the default flip: Jay sign-off + a tri-judge larger-n confirm** (per "never silently enabled" + ship-the-win); until then the gate stays default-off with the validated trade documented. Harness PR'd: **PR #164** (bench/e009-claims-gate-probe). NOTE: first launch with qwen3:14b judge hung on a 12GB VRAM model-swap deadlock; relaunched with llama3.1:8b co-resident (see [[feedback_gpu_contention]]).
- **E-007 arctic-embed SHIPPED (F-010), PR #162 MERGED.** R@K +0.157, judged +0.0565 full-1540 (arctic-s 0.7305 vs MiniLM 0.6740), same 384 dim and latency (report rev 1.15). Enablers #160/#161 also merged. Existing installs keep MiniLM (config default); fresh setups fetch arctic via the tested HF path (Pi 13ms/embed). **OPEN FOLLOW-UP (not a blocker): @taOS owes arctic store-registration + verified OS-store download path** (integration msg 417; bus 422 reframed it as a follow-up). A gentle Pi-CPU speed sample is the remaining nice-to-have.
- **E-008 RESOLVED to N-011: surprisal is the WORST retention signal; length wins.** The v2 surprisal pillar is dead across retrieval (N-009/N-010) and consolidation (N-011); the v2 spine is the provenance/claims layer (anchored by F-009, 18.8 percent extraction-hallucination, unmatched). **Claims layer (Provable Memory) MERGED, PR #163** (default-off). E-009 (above) is the validation that decides whether `prefer_verified` ever flips default-on. Spec/plan private at ~/tinyagentos-private/{specs,plans}/2026-06-13-v2-claims-layer-*. Side-finding: length is a strong free retention heuristic (own pre-registration deferred).
- **qmd fork exit path:** upstream PR tobi/qmd#728 open (tsc cleanup 51 to 3, zero behavior change); #663 (brettdavies, continues our #511 with credit, includes /search + /vsearch) has two ready branches linked from us (`dim-guard-on-663`, `fix-663-test-types`). When #663 merges and a release cuts, our fork can be dropped. Watch both for maintainer replies.

## Queued next

1. **E-012 BASELINE DONE (F-012: 47.2% end-to-end Judge), score-up phase ACTIVE (Jay: "use all our knowledge to get scores up").** Honest LongMemEval-S oracle Judge number: 236/500 = 47.2% under a strict local judge (qwen3.5:9b gen + Qwen3-4B-Instruct judge + MiniLM), branch bench/e012-judge-harness, log e012_full500.log. DIAGNOSIS: retrieval is solved (97% Recall@5), REASONING is the bottleneck (temporal 17.3%, multi-session 28.6%; single-session/knowledge 71-89%); the generator was evidence-STARVED (assembler 2000 tok / 5 chunks / 3 FTS). Recorded report rev 1.20 / F-012. SCORE-UP LEVERS (each measured vs 47.2%): (a) DONE - depth knobs tunable (TAOSMD_ASSEMBLE_TOKENS=4000/RETRIEVE_LIMIT=12/FTS_LIMIT=5/CONTEXT_CHARS=16000, commit a23ea26); (b) NEXT + highest-leverage - "intelligent context release" (Jay's idea, validated): retrieve MORE then RERANK/PRUNE to a clean subset before generation (bge-v2-m3 already in stack; evaluate Provence pruner [[reference_context_pruning_research]]); the claims layer already drops INCORRECT, pruning adds the REDUNDANT half; (c) arctic embedder + query-expansion; (d) lenient/tri-judge pass for a comparable number + the full-distractor set. GPU runs COORDINATED with @taOS via the lease protocol [[feedback_gpu_lease_protocol]] (their demo priority; CLAIM/RELEASE on the integration channel). **APPROVED + BUILT (Jay "let's go", Jun 14):** spec docs/superpowers/specs/2026-06-14-e012-score-up-ablation-design.md + plan docs/superpowers/plans/2026-06-14-e012-score-up-ablation.md. All four levers now exist on bench/e012-judge-harness (PR #165): (a) depth knobs + (b) rerank context-release (TAOSMD_RERANK) were already built; decomposition (TAOSMD_DECOMPOSE, ported from locomo) and self-verification (TAOSMD_SELF_VERIFY, fresh CoVe-style) added TDD with 7 green tests; default-off so the baseline arm is unchanged. Ablation runner benchmarks/e012_ablation.sh. RUN IN PROGRESS on the leased 3060 (lease msg #465; gen qwen3.5:9b, judge Qwen3-4B-Instruct-2507, decompose reuses the 9B so co-resident ~9GB, no deadlock). n=20 wiring validation PASSED (55% temporal, judge sane). METHODOLOGY NOTE the validation surfaced: the tuned-depth defaults already lift temporal well above F-012's 17.3%, so the screen adds an explicit STARVED anchor arm (2000/5/3) to re-tie to 47.2% and isolate the depth-tuning gain. NEXT: 9-arm screen at n=100, then full-500 + tri-judge firm-up on the winner; record F/N either way.
2. **E-009 default-flip decision (Jay).** Claims gate resolved to F-011: prefer_verified PASSES (zero served-hallucination at no accuracy cost), strict opt-in. Flip the `prefer_verified` default to on only after Jay sign-off + a tri-judge larger-n confirm (n=200 single-judge validates "no cost", not enough to flip a shipped default). Interim option: document prefer_verified as a recommended opt-in integrity mode now. Also: PR the harness branch `bench/e009-claims-gate-probe`.
3. **E-010 DONE -> N-012: no embedder beats arctic-embed-s; arctic retained, F-010 reaffirmed.** Judge-free LoCoMo R@K on the VPS: arctic-s 0.823, e5-small 0.808, bge-small 0.773, granite-r2 0.742, gte-small 0.727; none clears the +0.02 margin (granite, the top MTEB candidate, did not transfer). Recorded report rev 1.21 / N-012. Shipped reusable: env-configurable onnx loader (TAOSMD_ONNX_POOLING/_QUERY_PREFIX/_DOC_PREFIX) + benchmarks/e010_bakeoff.sh. Branch bench/e010-embedding-bakeoff (the loader change is worth a PR to master).
4. **@taOS coordination (their side, no taOSmd work owed):** (a) @taOS is installing an SD image backend on the Fedora 3060 themselves (Jay granted the window; I touch nothing outside taOSmd); (b) they owe arctic store-registration (Snowflake/snowflake-arctic-embed-s with a verified OS-store path); (c) they owe a README correction on their side (taOSmd section should say "97.0% Recall@5", not Judge). Their tool-call leaderboard is DONE (zero flow failures down to 4B).
5. **Interim job pack** at `docs/agent-jobs/` (three low-risk jobs) for weaker agents; review any PRs against each job's verification section, never auto-merge.
6. **Reproducibility hardening (F-001 follow-up): ADDRESSED, PR #167 (chore/reproduce-longmemeval).** Root cause was that the repo could not reproduce its own headline: the dataset was untracked and the recipe undocumented. Fix: committed the previously-untracked proof file (enhanced_20260413_133215.json, query_expand 485/500 = 97.0% Recall@5), pinned the exact recipe in benchmarks/REPRODUCE-longmemeval.md (runner, command, config, embedder, metric, expected per-category table), sha256-pinned the dataset via a tracked benchmarks/data/README.md (gitignore negation; the 265 MiB file stays ignored), and deprecated the stale longmemeval_recall.py. An independent CPU re-run from the pinned recipe is in flight on the bench host; its confirmation line gets appended to the doc's reproduction log before #167 merges. Recall@5 is deterministic so it reproduces host-independently.

## Working agreements

- Durable state lives in three places: GitHub issues (tasks), this file (snapshot), and the A2A bus (the taOSmd-progress channel is the running log).
- Small doc fixes go straight to master. Features, refactors, and redesigns go through a branch and PR.
- Benchmark policy: real datasets, external judges, publish methodology, record negative results too.

## On arrival (incoming agent or contributor)

1. Read this file top to bottom.
2. ARM THE SESSION INFRASTRUCTURE (project rule, every session, no exceptions): (a) the 30-minute audit cron using the canonical prompt in `.claude/audit-cron-prompt.md` (schedule 7,37 * * * *), covering rate-limit recovery, docs freshness, the research report, memory, and the bus; (b) the A2A connection per `.claude/a2a-session-setup.md`: live watcher, durable poll floor verified, history caught up, identity rules observed.
2. `git fetch origin` and review recent commits on master plus any open branches.
3. Check open GitHub issues and PRs.
4. Tail the A2A bus: taOSmd-progress, general, integration channels.
5. Read the project memory index if you have one.
Then take the top unblocked item, or continue whatever "In flight" points at.

## On rate limit or handoff (the moment you see the warning)

1. Commit and push WIP on a branch. Never leave uncommitted work.
2. Update this file: move your task to "In flight" with the branch name, exactly where you stopped, and the next concrete step.
3. Post one line to taOSmd-progress: finished X, mid-flight Y on branch Z, next step W.
The next agent recovers from last push + open PRs + this file + issues.
