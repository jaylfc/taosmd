# STATUS

Working snapshot for whoever picks this repo up next. Updated whenever meaningful state changes. Shipped changes live in [CHANGELOG.md](CHANGELOG.md); measured results live in [docs/benchmarks.md](docs/benchmarks.md); the task list of record is GitHub issues.

Last updated: 2026-06-19 ~12:15Z. **LATEST: E-013 RESOLVED, F-014 CONFIRMED, GPU RELEASED.** Jay rebooted the bench host back into Fedora (GPU online). Built the N-014 fix on feat/beam-runner (f7dc94e: empty-answer=FAIL guard in judge_prediction + configurable TAOSMD_BEAM_LLM_TIMEOUT default 120; 23 beam tests pass), ran the hardened BEAM-100K re-confirm = 49.0% / avg 0.455 / errors=0, abstention 100%, empty-answer guard fired ZERO times. Per the pre-registered kill criterion F-014 STANDS as measured and N-014 is confined to slow-CPU generation (no empty inflation on GPU; the 100% abstention is genuine). Full 1M re-run judged redundant (zero empties on GPU is tier-independent), not run. Recorded research-report rev 1.28 (master 7511753); [GPU RELEASE] posted + models unloaded, 3060 free. **Company-name / branding scrub (convention relayed by @taOS-dev, Jun 18): COMPLETE.** The attribution change (pyproject author + README footer to 'the taOS project', convention 2) is landed (ac832ab). The loud first scrub commit, whose subject named the former company, was reworded to 'chore: update license and packaging metadata' via filter-branch (message-only, content byte-identical) and force-pushed after Jay greenlit briefly lifting master branch protection (allow_force_pushes flipped true, pushed, restored to false, verified identical to the captured backup). master is now 02d916e; the former company name appears NOWHERE in the tracked tree or in any recent commit message. The 3 deep historical commits (about 180 to 429 back) were intentionally left untouched, since rewriting them would be a huge, conspicuous history rewrite counter to keeping this quiet. Parked reword branch + filter-branch backup ref cleaned up. **Earlier (Jun 18), now resolved:** (1) **E-009 tri-judge confirm DONE + recorded (research-report rev 1.27, master 2820165).** The lenient gemma4:e2b arm is NO-HARM (prefer_verified identical to off: judge 0.490, R@K 0.615, served-hall 0.000) but did NOT reproduce the elimination benefit, because its off baseline already had 0.000 served-hallucination (vs 0.040 in F-011), so the gate had nothing to remove; the difference tracks a config difference (vector-only, no reranker) and is undiagnosed, so the benefit is config-dependent. strict over-trades (R@K 0.615 to 0.455). The prefer_verified default flip still rests on the F-011 strict-llama run + Jay sign-off; the lenient arm only adds no-cost corroboration. Numbers captured in-session (result JSON e009_gemma_20260618_102802.json is on the Fedora disk, now OFFLINE because Jay rebooted the bench host into WINDOWS). (2) **VPS 30B-CPU BEAM run DONE and it surfaced a real harness confound -> N-014:** the runner's hard 60s LLM timeout stores EMPTY answers on slow generation, and the ported mem0 nugget judge PASSES an empty answer on abstention/negative rubrics, so blanks inflate the score. qwen3:30b-a3b on the CPU-only VPS read 55% with 10/20 empties, then 20% honest at a 240s timeout (0 empties, n=20); code-confirmed in beam_runner.py. So a frontier model on CPU did NOT beat the tuned local 9B. F-014's GPU 9B numbers (47% / 37.5%@1M, errors=0) probably stand but cannot be cleared from existing logs (no per-answer lengths recorded; abstention sits at 100%, the most exposed category), so **E-013 is pre-registered: a hardened BEAM re-confirm** (empty-answer=FAIL guard + raised timeout) with a kill criterion written before the run, QUEUED for the next GPU window after E-009. Recorded research-report rev 1.26 (N-014 + E-013 + F-014 caveat + abstract clause), pushed to master **7562327**. (3) **MemX R1 reject-gate BUILT** on feat/memx-levers (96b7725): apply_reject_gate(tau=0.50), env-gated TAOSMD_REJECT_GATE/_TAU default-off, 11+8 tests; the LoCoMo --strategy full ablation + a BEAM wire-in are PENDING the GPU (gate fires via retrieve() only, BEAM's retrieve_memories() needs an explicit call). MemX Lever 2 (freq/importance rerank) STUBBED honestly: no per-memory access-count tracking exists yet (upgrade-path TODO). **USAGE 7d 93% (SOFT WIND-DOWN + WEEKLY GATE, Jun 18 ~15:00Z): monitor-only, no new heavy work until the 7d window resets Jun 21 02:00Z; HARD STOP at 98%. 5h reset to 0% at 14:40Z (next 5h resets 19:40Z). GPU: the Fedora 3060 is OFFLINE (Jay took the box and had me reboot it into Windows via rebootwin); E-009 was collected first and released cleanly. No GPU work (E-013 hardened BEAM re-confirm + anything else needing the card) until the host is back in Fedora. Weekly job-pack refresh (docs/agent-jobs/: job-001 em-dash sweep, job-002 cross-encoder path fix, job-003 http-server dead import + README) is DUE before the Jun 21 dark period.** 5h 57% (resets 14:40Z). Resume pair armed (primary f7ac3a47 14:42Z, retry c720509d 15:02Z); audit a0855fff + daily sweep 74cba0e7 stay monitor-only. **feat/memx-levers (96b7725) and feat/beam-runner (a6006e5) both PUSHED** (owed PRs to master, deferred past wind-down). **Smart-installer PR #168 awaits Jay merge.** The publish-BEAM decision stays Jay's and now waits on the E-013 hardened number (publishing 47-49% before the empty-answer audit would be premature). (SUPERSEDED Jun 16 pause: Jay is rebooting the Mac mini, so this session + its session-only crons [audit de5c15ff, resume pair 30223ec8/30224b4d, daily sweep 4ef41aff] DIE; disk/git/memory persist; on the new session re-arm them per "On arrival" below, and the Fedora 3060 is JAY'S until he frees it [check bus + nvidia-smi before any re-claim]. BEAM is the active thrust, config + retrieval-recall sweeps DONE (47-49% plateau confirmed by BOTH; no config or retrieval lever beats it; wider/lexical retrieval REGRESS; self-verify is BEAM-neutral); 3060 RELEASED + unloaded. Runner BUILT on branch feat/beam-runner [benchmarks/beam_runner.py, ports mem0 own nugget-judge methodology + 10 categories, 13 tests]. FIRST RESULT: BEAM-100K validation [5 convs, 100 Qs, full stack arctic+rerank+self-verify+qwen3.5:9b+Qwen3-4B-Instruct judge] = **31% pass / avg_score 0.278** [results benchmarks/results/beam_100K_*.json scp'd to /tmp/beam_100k_result.json]. DIAGNOSIS [corrected, NOT over-abstention]: the bottleneck is RETRIEVAL PRECISION on long-context needles - retrieval pulls ~19 chunks/Q [zero zero-retrieval] but the exact-fact chunk is often missing; PASS vs FAIL retrieved_count ~equal [18 vs 20] so MORE depth alone will not fix it; only 6-8 abstentions, most failures are genuinely-wrong answers. IMPLICATION: the judge panel [Jay's idea] is a final-number firm-up, NOT the lever [wrong answers fail under any judge] - so score-up retrieval/gen FIRST, judge-panel on the best config. STRATEGY: BEAM rewards FRONTIER generation [mem0 64-70% @1M with GPT-4-class]; our local 9B lands honestly behind, so the play is a TRANSPARENCY one [early, fully-provenanced, methodology-pinned local-tier number] not a score-win. CONTEXT-AMOUNT SWEEP DONE (BEAM-100K, 5 convs, same stack, only the retrieval-depth env changed): rich [40 chunks / rerank15 / 24K ctx] = 9%, baseline [12 / rerank8 / 16K] = 31%, **TIGHT [6 / rerank4 / 6K] = 47%** [best]. CLEAR LEVER: intelligent context release - LESS, cleaner context wins big on the 9B [+16pp baseline->tight; more context BURIES the small model]. This is on-thesis (same as E-012). xtight [4 / rerank3 / 4K] = 47% (no gain over tight, so 47% is the BEAM-100K plateau under the 9B). BEAM-1M n=200 head-to-head MEASURED = **37.5% (75/200, avg_score 0.357)**; mem0 frontier 64-70% @1M, so local-9B lands honestly behind as predicted (per-category: abstention 100% / preference 80% strong; event_ordering 0% / knowledge_update 10% weak). qwen3.6:35b-a3b LOCAL generator arm TRIED + ABANDONED: it CPU-offloads on the 12GB 3060 [0/100 in ~40min, GPU starved at 35% util], so **9B is our practical local ceiling, not 35b** [[feedback_gpu_contention]]. CONFIG SWEEP DONE Jun 17 ~00:50Z (Jay freed the 3060 by waking it from LAN + rebooting Windows->Fedora; ran the 5 remaining arms, [GPU RELEASE]+unload posted). RESULT: NO config lever beats the 47% tight-retrieval plateau beyond n=100 noise. All 7 arms (pass% / avg_score): base_chunk100 47.0/0.436, chunk50 47.0/0.417, chunk200 49.0/0.425, chunk300 44.0/0.394, sentences 47.0/0.425, chunk100_nosv 49.0/0.455, chunk100_sv_decomp 47.0/0.436. Best nominal chunk200 + chunk100_nosv at 49% (+2pp, within noise of base). KEY FINDING: self-verify gives NO BEAM benefit (chunk100_nosv ties the top pass-rate AND leads avg_score 0.455), unlike LongMemEval F-013 - BEAM failures are wrong answers, not abstentions, so the CoVe verify/abstain pass cannot help here. Sweep was benchmarks/beam_campaign.sh (chunk size 50/100/200/300, sentence-mode, self-verify off, decompose on; branch feat/beam-runner, 9B gen + Qwen3-4B-Instruct judge). ARMS DONE: base_chunk100_sv **47.0%** (avg 0.436), chunk50_sv **47.0%** (avg 0.417, smaller chunks no gain); arm3 chunk200_sv interrupted. ARMS PENDING on resume: chunk200, chunk300, sentences, chunk100_nosv, chunk100_sv_decomp. Methodology + provenance: benchmarks/beam_campaign_resume.sh + log benchmarks/results/beam_campaign_resume_20260616_221030.log (branch feat/beam-runner). NO clear winner to carry to 1M (all within noise), so BEAM-1M stays 37.5% (n=200); OPTIONAL follow-up = a larger-n (n>=500) confirm of chunk100_nosv vs base, since self-verify-off is faster AND nominally >= on BEAM, if Jay wants a firmer read. NET: the score-up thesis is confirmed - local-9B plateaus at 47% on BEAM-100K (mem0 frontier 64-70% @1M), so the honest play is the transparent, fully-provenanced local-tier number, NOT a config-tuned score win. RECALL SWEEP DONE (Jun 17, Jay's "push for more"): tight retrieval 49% beats wider (recall_wide 43, recall_wider 46) and lexical-heavy (fts_heavy 41), so the plateau is NOT a retrieval-recall gap either - confirmed across TWO independent sweeps (config + recall), recorded research-report rev 1.24 + benchmarks/results/beam_recall_20260617_130445.log. The PUBLISH DECISION (publish the honest 47-49% / 37.5%@1M now vs keep pushing) is now ripe for Jay. ALSO DONE THIS SESSION: smart-installer BUILT -> PR #168 (profiles registry + setup-prompt CLI + static prompt, 17 tests, awaits Jay merge); SuperLocalMemory competitor dig DONE (first local-first competitor; their numbers are vendor-graded best-of-5 single-lenient-judge, zero-loss is the clean fault line; recs in reference_memory_landscape_jun2026). QUEUED: E-009 tri-judge confirm (next GPU window, mind the qwen3:14b co-resident deadlock - use the small judges) before the prefer_verified flip; E-012 opt-in/config ship. NEXT after that: record a BEAM finding in the research report once the series settles. RECORD a BEAM finding in the research report once the series settles. Jay's call publish-baseline-now vs score-up-first [my lean: score-up first; 47%/37.5% is already a far better, fully-provenanced story than 31%]. E-012 DONE [F-013 74.6%]; ship=opt-in verified+rerank QUEUED; PRs merged; smart-installer SPEC written+committed [docs/superpowers/specs/2026-06-16-smart-installer-design.md] AT THE REVIEW GATE awaiting Jay's OK before writing-plans.)

## Current state

- **master** is healthy: about 886 tests passing. Shipped today (Jun 13): **PR #162 arctic-embed-s as the low-tier dense default** (ships F-010 -- R@K +0.157, judged +0.0565 full-1540, same 384-dim and latency; existing installs keep MiniLM via the config default, only fresh setups fetch arctic) and **PR #163 the v2 claims layer "Provable Memory"** (default-off: `taosmd/claims/` -- zero-loss ClaimStore, cross-family fail-closed entailment verifier, demote-not-delete recall gate, archive-span provenance, async verify-pass, `taosmd verify`/`claims status` CLI). Earlier this cycle (Jun 12): temporal date-range lever (PR #157), pause/resume checkpointing for the LoCoMo runner (PR #158), compact mode on `GET /a2a/messages`, the grants-gate auth fix (`faaaf3c`), project-scoped grants (#151), task-graph (#150), batch ingest + bm25 mode (#149), TTL filter + three-number summaries (#155), admin surface (#153), research report first edition (#156).
- **taOS #744 is CLOSED, e2e-verified 3/3** against the real registry feeds (live grant pass with claim-binding, expired-grant 403, revoked 403). The exercise found and fixed the gate-bypass bug above, the argument for e2e over unit coverage.
- **INTEGRITY CORRECTION (Jun 14, research report rev 1.19): the LongMemEval-S "97.0%" headline was Recall@5, mislabelled "end-to-end Judge accuracy" since an April docs reframe.** Proof: benchmarks/results/enhanced_20260413_133215.json (query_expand, top_k=5) is the source of the published per-category table and is a Recall@5 hits metric; no Judge run ever produced 97.0% (the Judge harness reproduces ~22.6%). Corrected on every public surface (README, benchmarks.md, research-report F-001, AGENTS.md, CHANGELOG) to **"97.0% Recall@5"** with provenance; Jay approved. Recall@5 still beats MemPalace 96.6% head-to-head, so the corrected claim is strong. **E-012 pre-registered** to measure a genuine end-to-end Judge number. The data was never lost (public benchmark + valid local copies).
- **Positioning (Jun 14): README now CO-HEADLINES "Provable, auditable memory" alongside the Recall@5 number.** An independent Letta/Mem0/Graphiti/Cognee comparison found provenance + zero-loss + audit trails unaddressed across the field, so that gap is our lead (the zero-loss archive + F-009/F-011 claims layer answer it). Copy propagated to @taOS for their README section + the website. Landscape intel + the open-ground confirmation are in memory [[reference_memory_landscape_jun2026]]. Research tooling set up: agent-reach (pipx) + no-auth methods (Reddit RSS, HN Algolia, GitHub, Jina) [[reference_research_methods_noauth]]; daily landscape-monitor cron armed. mem0's Apr-2026 numbers (94.4 LongMemEval / 92.5 LoCoMo) have ZERO disclosed judge methodology, a direct contrast to our provenance.
- **Other headline: LoCoMo leader 0.748/0.394/0.659 (tri-judge), unchanged and correctly labelled.** Earlier pre-registered negatives stand: N-008 (projected ColBERT no upgrade), N-009 (surprise-boundary chunking a coverage artifact), N-010 (write-skip under the shipping floor), N-011 (surprisal worst retention signal).

## In flight

- **E-012 SCORE-UP -- CONFIRMED + judge-robust (F-013), GPU released.** Full-500: tuned-depth baseline 56.8% (284/500), winner rerank+self-verify **74.6% (373/500)**. The cross-family llama3.1:8b firm-up reproduced BOTH numbers exactly (identical per-category counts), so the +17.8pp gain is not judge-specific. Recorded F-013 (research-report rev 1.22, firm-up appended) + provenance benchmarks/results/e012_ablation_results.md, all pushed to master. [GPU RELEASE] posted (bus #474). Levers on PR #165 (bench/e012-judge-harness): rerank (TAOSMD_RERANK), self-verify (TAOSMD_SELF_VERIFY), decompose (TAOSMD_DECOMPOSE, a DUD - drop), sampling (TAOSMD_SAMPLE_SEED). OPTIONAL/LATER: lenient gemma4:e2b re-judge (needs a prediction-cache to dodge the VRAM deadlock); qwen3.6:35b-a3b bigger-gen arm (23GB, CPU offload); full-distractor-set run (pre-registered). **SHIP DECISION = JAY's** (never-silently-enabled): self-verify is general + model-agnostic; proposal is an opt-in verified-answer mode + rerank in the recommended retrieve config, NOT a silent core default (taOSmd serves memory; answer-gen is the consumer path). Also pending: PR #165 review/merge (+ #164/#166/#167). Harness env: TAOSMD_OLLAMA_MODEL=qwen3.5:9b, TAOSMD_JUDGE_MODEL=hf.co/unsloth/Qwen3-4B-Instruct-2507-GGUF:Q4_K_M, TAOSMD_DECOMPOSE_MODEL=qwen3.5:9b.
- **E-009 RESOLVED to F-011: the prefer_verified claims gate PASSES its kill criterion.** Full 10-conv LoCoMo sweep (200 QAs, 1853 claims, llama3.1:8b judge, `benchmarks/results/e009.json`): served-hallucination 0.040 -> 0.000 (eliminated), judged accuracy 0.425 -> 0.445 (no drop, +0.020 within noise at n=200), R@K 0.615 -> 0.610 (-0.005, inside the 0.02 bound). strict FAILS (judge -0.065, R@K -0.065, trades accuracy for purity) and stays opt-in. Robust claim: **prefer_verified eliminates served-hallucination at NO measured accuracy cost** (the +0.02 is noise; do not claim a gain). Recorded report rev 1.18 + F-011. **REMAINING before the default flip: Jay sign-off + a tri-judge larger-n confirm** (per "never silently enabled" + ship-the-win); until then the gate stays default-off with the validated trade documented. Harness PR'd: **PR #164** (bench/e009-claims-gate-probe). NOTE: first launch with qwen3:14b judge hung on a 12GB VRAM model-swap deadlock; relaunched with llama3.1:8b co-resident (see [[feedback_gpu_contention]]).
- **E-007 arctic-embed SHIPPED (F-010), PR #162 MERGED.** R@K +0.157, judged +0.0565 full-1540 (arctic-s 0.7305 vs MiniLM 0.6740), same 384 dim and latency (report rev 1.15). Enablers #160/#161 also merged. Existing installs keep MiniLM (config default); fresh setups fetch arctic via the tested HF path (Pi 13ms/embed). **OPEN FOLLOW-UP (not a blocker): @taOS owes arctic store-registration + verified OS-store download path** (integration msg 417; bus 422 reframed it as a follow-up). A gentle Pi-CPU speed sample is the remaining nice-to-have.
- **E-008 RESOLVED to N-011: surprisal is the WORST retention signal; length wins.** The v2 surprisal pillar is dead across retrieval (N-009/N-010) and consolidation (N-011); the v2 spine is the provenance/claims layer (anchored by F-009, 18.8 percent extraction-hallucination, unmatched). **Claims layer (Provable Memory) MERGED, PR #163** (default-off). E-009 (above) is the validation that decides whether `prefer_verified` ever flips default-on. Spec/plan private at ~/tinyagentos-private/{specs,plans}/2026-06-13-v2-claims-layer-*. Side-finding: length is a strong free retention heuristic (own pre-registration deferred).
- **qmd fork exit path:** upstream PR tobi/qmd#728 open (tsc cleanup 51 to 3, zero behavior change); #663 (brettdavies, continues our #511 with credit, includes /search + /vsearch) has two ready branches linked from us (`dim-guard-on-663`, `fix-663-test-types`). When #663 merges and a release cuts, our fork can be dropped. Watch both for maintainer replies.

## Queued next

1. **E-012 BASELINE DONE (F-012: 47.2% end-to-end Judge), score-up phase ACTIVE (Jay: "use all our knowledge to get scores up").** Honest LongMemEval-S oracle Judge number: 236/500 = 47.2% under a strict local judge (qwen3.5:9b gen + Qwen3-4B-Instruct judge + MiniLM), branch bench/e012-judge-harness, log e012_full500.log. DIAGNOSIS: retrieval is solved (97% Recall@5), REASONING is the bottleneck (temporal 17.3%, multi-session 28.6%; single-session/knowledge 71-89%); the generator was evidence-STARVED (assembler 2000 tok / 5 chunks / 3 FTS). Recorded report rev 1.20 / F-012. SCORE-UP LEVERS (each measured vs 47.2%): (a) DONE - depth knobs tunable (TAOSMD_ASSEMBLE_TOKENS=4000/RETRIEVE_LIMIT=12/FTS_LIMIT=5/CONTEXT_CHARS=16000, commit a23ea26); (b) NEXT + highest-leverage - "intelligent context release" (Jay's idea, validated): retrieve MORE then RERANK/PRUNE to a clean subset before generation (bge-v2-m3 already in stack; evaluate Provence pruner [[reference_context_pruning_research]]); the claims layer already drops INCORRECT, pruning adds the REDUNDANT half; (c) arctic embedder + query-expansion; (d) lenient/tri-judge pass for a comparable number + the full-distractor set. GPU runs COORDINATED with @taOS via the lease protocol [[feedback_gpu_lease_protocol]] (their demo priority; CLAIM/RELEASE on the integration channel). **APPROVED + BUILT (Jay "let's go", Jun 14):** spec docs/superpowers/specs/2026-06-14-e012-score-up-ablation-design.md + plan docs/superpowers/plans/2026-06-14-e012-score-up-ablation.md. All four levers now exist on bench/e012-judge-harness (PR #165): (a) depth knobs + (b) rerank context-release (TAOSMD_RERANK) were already built; decomposition (TAOSMD_DECOMPOSE, ported from locomo) and self-verification (TAOSMD_SELF_VERIFY, fresh CoVe-style) added TDD with 7 green tests; default-off so the baseline arm is unchanged. Ablation runner benchmarks/e012_ablation.sh. RUN IN PROGRESS on the leased 3060 (lease msg #465; gen qwen3.5:9b, judge Qwen3-4B-Instruct-2507, decompose reuses the 9B so co-resident ~9GB, no deadlock). n=20 wiring validation PASSED (55% temporal, judge sane). METHODOLOGY NOTE the validation surfaced: the tuned-depth defaults already lift temporal well above F-012's 17.3%, so the screen adds an explicit STARVED anchor arm (2000/5/3) to re-tie to 47.2% and isolate the depth-tuning gain. NEXT: 9-arm screen at n=100, then full-500 + tri-judge firm-up on the winner; record F/N either way.
2. **E-009 default-flip decision (Jay).** Claims gate resolved to F-011: prefer_verified PASSES (zero served-hallucination at no accuracy cost), strict opt-in. Flip the `prefer_verified` default to on only after Jay sign-off + a tri-judge larger-n confirm (n=200 single-judge validates "no cost", not enough to flip a shipped default). Interim option: document prefer_verified as a recommended opt-in integrity mode now. Also: PR the harness branch `bench/e009-claims-gate-probe`.
3. **E-010 DONE -> N-012: no embedder beats arctic-embed-s; arctic retained, F-010 reaffirmed.** Judge-free LoCoMo R@K on the VPS: arctic-s 0.823, e5-small 0.808, bge-small 0.773, granite-r2 0.742, gte-small 0.727; none clears the +0.02 margin (granite, the top MTEB candidate, did not transfer). Recorded report rev 1.21 / N-012. Shipped reusable: env-configurable onnx loader (TAOSMD_ONNX_POOLING/_QUERY_PREFIX/_DOC_PREFIX) + benchmarks/e010_bakeoff.sh. Branch bench/e010-embedding-bakeoff (the loader change is worth a PR to master).
4. **@taOS coordination (their side, no taOSmd work owed):** (a) @taOS is installing an SD image backend on the Fedora 3060 themselves (Jay granted the window; I touch nothing outside taOSmd); (b) they owe arctic store-registration (Snowflake/snowflake-arctic-embed-s with a verified OS-store path); (c) they owe a README correction on their side (taOSmd section should say "97.0% Recall@5", not Judge). Their tool-call leaderboard is DONE (zero flow failures down to 4B).
5. **Interim job pack** at `docs/agent-jobs/` (three low-risk jobs) for weaker agents; review any PRs against each job's verification section, never auto-merge.
6. **Reproducibility hardening (F-001 follow-up): ADDRESSED, PR #167 (chore/reproduce-longmemeval).** Root cause was that the repo could not reproduce its own headline: the dataset was untracked and the recipe undocumented. Fix: committed the previously-untracked proof file (enhanced_20260413_133215.json, query_expand 485/500 = 97.0% Recall@5), pinned the exact recipe in benchmarks/REPRODUCE-longmemeval.md (runner, command, config, embedder, metric, expected per-category table), sha256-pinned the dataset via a tracked benchmarks/data/README.md (gitignore negation; the 265 MiB file stays ignored), and deprecated the stale longmemeval_recall.py. An independent CPU re-run from the pinned recipe is in flight on the bench host; its confirmation line gets appended to the doc's reproduction log before #167 merges. Recall@5 is deterministic so it reproduces host-independently.

## Working agreements

- Durable state lives in three places: GitHub issues (tasks), this file (snapshot), and the A2A bus (the taOSmd-progress channel is the running log).
- Small doc fixes go straight to master. Features, refactors, and redesigns go through a branch and PR.
- Benchmark policy: real datasets, external judges, publish methodology, record negative results too.

## On arrival (incoming agent or contributor)

1. Read this file top to bottom.
2. ARM THE SESSION INFRASTRUCTURE (project rule, every session, no exceptions): (a) the 30-minute audit cron using the canonical prompt in `.claude/audit-cron-prompt.md` (schedule 7,37 * * * *), covering rate-limit recovery, docs freshness, the research report, memory, and the bus; (b) the A2A connection per `.claude/a2a-session-setup.md`: live watcher, durable poll floor verified, history caught up, identity rules observed.
2. `git fetch origin` and review recent commits on master plus any open branches.
3. Check open GitHub issues and PRs.
4. Tail the A2A bus: taOSmd-progress, general, integration channels.
5. Read the project memory index if you have one.
Then take the top unblocked item, or continue whatever "In flight" points at.

## On rate limit or handoff (the moment you see the warning)

1. Commit and push WIP on a branch. Never leave uncommitted work.
2. Update this file: move your task to "In flight" with the branch name, exactly where you stopped, and the next concrete step.
3. Post one line to taOSmd-progress: finished X, mid-flight Y on branch Z, next step W.
The next agent recovers from last push + open PRs + this file + issues.
