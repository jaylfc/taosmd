Last updated: 2026-08-18 07:3x UTC (@taOSmd-dev, post-clear #40; #374 merged, master a75496a1, baseline 1661 passed / 12 skipped; a gate that was correct and unreachable read as coverage for six shipped defects, and a stale worktree nearly reverted this very file).

A GATE HAS TWO PROPERTIES, WHETHER IT CAN DETECT THE DEFECT AND WHETHER ANYTHING EVER ASKS IT TO RUN, AND A PASSING SUITE ONLY EVER DEMONSTRATES THE FIRST. #374 closes tsk-n3eubn and supersedes the blocked #368. The trailing-newline gate carded after three shipped offenders was written correctly, went red on a real fragment, and passed its ten tests, and it was blocked for the one thing its card never mentioned, which is that nothing invoked it. No workflow, makefile, shell script or config file referenced the script anywhere in the repository, while each of the three sibling gates ships its own workflow, so the pull request added a fourth gate and no fourth trigger. What makes this worth recording rather than filing as an oversight is the measurement: with a real fragment whose last byte was not a newline sitting in changelog.d, the suite returned 1627 passed, and all ten of the new gate's tests were among them. Every one of those tests built its own fixture under tmp_path, so not one of them ever read the repository the gate was written to protect, and the defect sat unreported inside a green run. The fix is the workflow, which is the only mandatory part, plus a single test that runs the gate against the repository's own root so a plain pytest run goes red as well as CI. That test is the one thing the original card forbade as vacuous on the grounds that it breaks whenever a fragment is added, and the objection is half right in a way that inverts its conclusion, because it breaks only when a fragment is added BADLY, which is the entire purpose. Measured on the merge: 1661 passed clean, and 1 failed against 1660 passed with an offending fragment planted, the single failure being that test. The contrast with the all-green 1627 is the whole finding.

A DOCUMENTED MECHANISM HAD ZERO COVERAGE BECAUSE OF WHEN IT IS READ RATHER THAN WHETHER IT WAS TESTED, AND THE TESTS THAT LOOKED LIKE THEY EXERCISED IT WERE BUILDING THE EVIDENCE AND THROWING IT AWAY. The gate documents TRAILING_NEWLINE_ROOT, an environment variable overriding the root it scans, and the test helper duly copied os.environ and set that variable on every call, which reads as coverage on inspection. The variable is read once at module import, and the helper then called main() in process and rebound tng.REPO_ROOT directly, so the dict it had just built was never passed to anything and the override was never once exercised. The proof is an interface-preserving mutant that deletes the override and keeps the default, REPO_ROOT = _REPO_ROOT, under which all ten tests still passed. The general shape is that a mock or an environment mutation only reaches code that runs after it and inside the same interpreter, so an import-time read needs a fresh process to test at all. That is the same boundary lesson that killed a monkeypatched a2a_send aimed across a subprocess in #371 last session, arriving from the other direction: there the mock was in the parent and the code in the child, here the read had already happened before the mock existed. Two subprocess tests now cover it and both fail under that mutant, and the kill is asserted on the REPORTED PATH rather than the exit status, since only the path distinguishes honouring the variable from coincidentally agreeing with it.

THE PULL REQUEST THAT EXISTS TO REQUIRE A TERMINATING BYTE SHIPPED TWO FILES WITHOUT ONE. The gate script ended in 0x29 and its test module in 0x74, while every sibling script already on master ends in 0x0a. Neither file is inside the gate's own scope, which is deliberately narrow and correctly so, so the gate could not have caught it and no check in the repository would have. It is worth stating plainly because the defect class has now shipped in six pull requests and the reflex has become to look for it in changelog.d, which is exactly where this instance was not. A gate defines where it looks, and the place it does not look is where the next instance goes.

A STALE WORKTREE ALMOST REVERTED THIS FILE, AND THE ONLY THING THAT REVEALED IT WAS COUNTING COMMITS RATHER THAN READING CONTENT. The main checkout is 12 commits behind master and holds another agent's uncommitted work, so it was deliberately left alone, and this update was drafted into it anyway out of habit. The edit applied cleanly, the paragraph it replaced was present and matched exactly, and both seams read correctly afterwards, so every check that operates on the text itself passed. The base was 15833 bytes short of master and predated two STATUS.md updates from the previous session, which the commit would have silently deleted while its own diff looked like a pure addition. This is the same failure the previous session recorded from the other end, that a diff is computed against its base and therefore cannot show a revert, and the cheap detector is the same one: git rev-list --count HEAD..origin/master before editing anything, not after. An edit succeeding is evidence about the file it was applied to, never about whether that file was current.

ONE HONEST TEST CAN ABSORB A WHOLE-FEATURE MUTANT AND HIDE A VACUOUS SIBLING BEHIND IT. #372 closes tsk-3m3irr. _validate_helper_path refuses two things and only one was covered: test_guard_refuses_linked_worktree never called main() and never asserted a refusal, it checked that a detector returned a string. Killing the ENTIRE guard failed exactly one test, and one failure reads as coverage, which is why no suite count could ever have found this. The mutant has to be aimed at the individual branch, leaving the detector intact so the existing test still has its function to call: 9 passed before, 1 failed and 10 passed after. THE FIX HAS A TRAP WORTH KNOWING, because the obvious version fails in a way that looks like success. The two refusals are checked IN ORDER, temp first, and tmp_path is itself under a temp root, so a fixture built there trips the TEMP refusal and never reaches the branch the test is named for. A test asserting only SystemExit would pass, would survive the whole-guard mutant, and would still not notice the worktree refusal being deleted. The test therefore neutralises _is_under_temp deliberately and asserts that "temp directory" is NOT in the message.

A STABLE SUITE COUNT IS NOT EVIDENCE WHEN A PR ADDS NO NEW TEST FUNCTIONS. #371 (exec/tsk-u22koy) BLOCKED, branch kept, revision card tsk-tsoylj. It added 21 lines and moved the count by zero, by construction, so both findings came from probes. Its two assertions on test_system_crontab_block_names_usr_bin_python3 are CORRECT and must be kept verbatim: with _HELPER_PATH hardcoded again they give 1 failed where master gives 1 passed. The other 17 lines do not survive. A monkeypatch.setattr of a2a_send was placed in a test that runs the script through subprocess.run, and setattr rebinds an attribute in the PARENT interpreter, so it cannot reach a separate process: deleting the whole block leaves the test at 1 passed, identical to the control, while its comment claims an effect it does not have. And its new assertion reads a test-built f-string against a test-built substring, consulting neither proc nor record; hoisted above subprocess.run and followed by a hard stop, it still passes. That is the SAME SHAPE as the defect the card existed to fix, reintroduced one test lower down.

THE SIXTH FRAGMENT IN A ROW SHIPPED WITHOUT A TERMINATING NEWLINE, AND NOW THE REASON IS NAMED. #371's fragment ends 0x2e. The gate that catches exactly this was blocked along with #368 and is NOT on master: ls scripts/ has no such script and no workflow references one. So this is not recurring carelessness, it is an unmerged gate, which is tsk-n3eubn. The same fragment also names the CLOSED predecessor card tsk-vqfjsm rather than the card it executes, and stamps year 2025 on 2026 work.

RESOLVING A PATH PROVES A FILE IS REACHABLE, NEVER THAT ITS CONTENTS ARE CURRENT, AND THAT IS WHY THIS ONE SURVIVED WEEKS. The audit cron prompt spelled out its resume-arming command twice, both times naming an out-of-repo copy at ~/.taos-team/resume_arm_time.py. Since #354 the script derives its own location from __file__, so the in-repo file is the only copy that can be correct and any other is a second document that drifts. One did, resolving to a checkout fifteen commits behind master. Every path involved resolved successfully the entire time, which is exactly why nothing caught it: a symlink check answers reachability, and the question was identity. Hash the target against origin/master instead. #370 points both invocations at scripts/resume_arm_time.py and adds tests/test_docs_name_in_repo_helper_path.py, which fails if any file under .claude/ or docs/ names the script at any other path, reading files out of the repository rather than inspecting the filesystem it describes so it holds in CI on a machine with no /home/jay at all.

THE COUNT RECONCILED IN BOTH DIRECTIONS, BECAUSE THE OBVIOUS COMMAND DISAGREED WITH THE TRUTH. git grep -c reported 1 while the real occurrence count was 2, because it counts matching LINES and that document is a single 15KB line. Found-not-claimed was 3 further bare mentions carrying no path, which the gate deliberately permits since a bare mention names the script without claiming a location. A count is only evidence once you know what the tool counts.

A CARD'S OWN MEASUREMENT GOES STALE LIKE ANY OTHER DOCUMENT. tsk-cdqsgy recorded the live helper as fifteen commits behind and running a pre-#354 script. Re-measured at checkout that was no longer true: the live copy is byte-identical to origin/master and carries both the __file__ derivation and #369's guard. But it is current only via UNCOMMITTED modifications sitting over a HEAD seven commits behind, in a worktree that also holds staged #315 work, so any git checkout or git stash there silently re-arms a pre-#354 script. It is correct by accident. Left untouched deliberately and raised on the bus at 3194; it is not this session's checkout to reset.

THE STALE-WORKTREE CLEANUP WAS DESCRIBED AS SAFE AND WAS NOT. The previous checkpoint carried ~77 registered worktrees as prunable idle-fire work. Measured, ZERO pointed at a missing directory, so git worktree prune was a no-op and a blind sweep would have deleted live directories: 21 held uncommitted changes. Only 11 were removed, the ones provably holding nothing unique (clean AND zero commits beyond origin/master); 67 remain by design. The audit also surfaced /tmp/exec-tsk-u22koy, a LOCAL-ONLY branch with 18 uncommitted test lines and no PR, whose card tsk-u22koy is still CLAIMED by stepflash-taos-20260713-103907. That is a stuck lane, not junk. Its diff is preserved at ~/.taos-team/salvage/ and the worktree was left in place. The abandoned work carries three defects worth not repeating: trailing-whitespace lines, a fragment ending 0x2e, and a monkeypatch.setattr against a test that runs the script as a SUBPROCESS, which cannot take effect and is therefore vacuous.

A DIFF IS COMPUTED AGAINST ITS BASE, SO A STALE BASE MAKES REDUNDANT WORK LOOK LIKE A FIX AND MAKES A REVERT INVISIBLE. Three of the six open exec PRs this session were the same artefact wearing different clothes. #361 and #364 each opened by re-adding the module constant that derives the helper path from __file__, and both diffs read as clean fixes, because both were computed against a base predating #354, which had already landed exactly that constant and all three of its call sites. Master carried it at lines 57, 508, 509 and 544 the whole time. Neither PR could merge, and the conflict was the only signal that anything was wrong: read on its own each diff was a correct-looking change to a file it did not need to touch. Worse than the redundancy, the test halves would have given back work master already held. Master derives the expected marker path from a constant the test file computes for itself, and both PRs rewrote it to read the value back out of the module under test, which makes the assertion incapable of failing. The lesson that generalises is that a PR's own diff cannot show you a revert, because against its base the revert looks like an addition. Only a trial merge against current master shows it, and this is now the third occurrence in this repo after #358 into #360.

A TEST CAN BE VACUOUS ON MASTER FOR MONTHS AND EVERY RUN OF IT LOOKS LIKE COVERAGE. The card behind #364 claimed two of the resume_arm_time tests measured nothing, and rather than take that on trust it was settled with an API-preserving mutant: set the helper path constant to a directory that does not exist, change nothing else, assert the replacement target was present before writing, and run the whole file. Two of the seven tests fail and five pass. Both of the tests that are specifically about the emitted path are in the passing group, because the assertion compares a string built from the constant against a string built from the same constant, so the two sides move together and no value can ever break it. Deriving the expected value independently in the test file closes it, and the same mutant then produces one failure where it previously produced none. The card was right and its fix was right; what blocked it was that it arrived as a rename of the production constant, against a stale base, when the whole correction is two lines in a test file and no production change at all.

A REPAIR THAT MAKES EVERY PATH RESOLVE TO ONE FILE HAS NOT ESTABLISHED WHICH FILE. The previous session found the resume helper missing from the shared tools directory and repaired it by pointing all three documented paths at the in-repo copy, then recorded that as restoring the canonical script. The consistency half was real. The canonical half was not: those symlinks resolve to the main worktree's working copy, and that worktree sits fifteen commits behind origin/master, so the file they reach is a pre-#354 script that still hardcodes the very out-of-repo path the fix was meant to retire. Master's copy has md5 b972d21f and the resolved copy has 446505544c. Nothing is broken today, because the hardcoded path exists and the arming and firing markers are self-consistent with each other, which is exactly why it survived a repair, a verification and a checkpoint without being noticed. The failure mode to carry forward is that resolving a symlink tells you the file is reachable and says nothing about whether its contents are current.

A CARD'S OWN WORDING IS A DEFECT SURFACE. #365 landed correctly and closed the trailing-newline chain properly, with all thirty three changelog fragments terminated on the merged tree rather than just the two the PR touched. Its changelog fragment nevertheless asserted that both files had lost their newline in the #360 merge. #360 was blocked and closed with merged=false, so no such merge happened, and the README's terminating byte was actually dropped by #348: the file ends 0a at ea882a29 and 2a at 17ea02a8. The lane did not invent this. The revision card was titled "the merged README", meaning the trial-merged tree, which is what its body measured, and read on its own that sentence says the PR was merged. A card is read by someone who does not have the context that made its shorthand unambiguous, and the changelog is permanent, so the correction went straight to master rather than through another round trip.


A SUITE COUNT THAT DOES NOT MOVE PROVES NOTHING ABOUT WHETHER COVERAGE WAS DROPPED, AND THIS IS THE SHARPEST VERSION OF THAT LESSON SO FAR. A revision carried a witness-gate regex fix forward correctly and touched no test file at all, and the reassuring thing about it was that the suite came in at exactly the baseline, which reads as evidence that nothing was lost. The predecessor branch had not added a test method; it had added one more arm inside a test that already existed, and assertions added inside an existing test do not change the number of tests collected. The count was therefore equally consistent with keeping the arm and with dropping it, and it was about to be recorded as though it discriminated between them. Only the file list answers that question. What settled it was an API-preserving mutant that kept everything the change ships and reverted the regex alone, with the replacement target asserted present before anything was written: the gate tests pass twenty five of twenty five with the arm absent and fail exactly one with it restored, so the behaviour change shipped with no discriminating coverage and could have been reverted later in silence. The rule now has three parts rather than two, since a test that passes on both trees measured nothing, a test that fails on the old tree may still have measured nothing, and a suite count that holds steady says nothing at all about coverage that was removed.

A REVISION FIXED THE ONE THING IT WAS BLOCKED FOR AND DROPPED THE ONE THING ITS CHAIN EXISTED TO WIN, WHICH IS A TRADE RATHER THAN A CLOSURE. The benchmark readme chain has been running for five rounds over a document whose whole stated purpose is that dataset identities are pinned so a missing file never means an ambiguous one, and the round before last finally landed the terminating newline the chain had failed on repeatedly, only to be blocked for a single relabelled unit. The revision got the arithmetic exactly right, to the point where all four figures in the merged file now agree with the byte counts and no mixed unit remains, and it ended the readme with no terminating byte, so the merge regressed the thing the previous round had won. Both halves have to be measured on the trial merge every time, because a revision built from master inherits master's defects silently while its own diff looks immaculate. The wider finding is that this is no longer an incident. Three pull requests have now shipped a file with no terminating newline, one earlier round was caught only because it was closed for unrelated reasons, and a directory-wide hand fix regressed twice inside a single session. Nothing in the repository guards it, which was confirmed with a positive control so the greps were known to engage, and a hand check performed a fourth time is not a durable answer, so a real gate is carded with fixture-based tests and a required mutation check to prove those tests discriminate.

THE AUTOMATED WARNING THAT HAD BEEN WRONG SIX TIMES RUNNING WAS RIGHT, AND IT WAS RIGHT ON THE ONE CARD WHERE PRECEDENT SAID TO IGNORE IT. It fires when a card mentions tests while the diff touches none, and the habit that had formed was to dismiss it on sight, which is what nearly happened again on the very change that had dropped its only discriminating arm. The distinction that makes it usable is the kind of card rather than the tally: on documentation cards it is now seven for seven wrong, because an assertion that a named file contains a phrase passes on any rewording, and on behaviour cards it is one for one right. A tally is not a rule, and dismissing a warning by precedent is how the one true positive in a run of eight gets waved through. The same session produced two instrument failures of the reviewer's own making, both caught by controls rather than by care: a gate's exit status was read from the end of a pipeline and reported the status of the command that formatted the output instead, and a count of test functions came back as zero because the pattern assumed they sat at the left margin when every one of them is a method inside a class. A third party's automated reviewer, meanwhile, returned no issues found and recommended merging the change that carried two byte-level defects, which is the reminder that a bot verdict is a claim to triage and is blind to the class of defect that lives in the last byte of a file.

A HARDCODED PATH POINTED AT A FILE FORTY FIVE LINES STALE, AND NOTHING INSIDE THE REPOSITORY COULD SEE IT, BECAUSE THE DEFECT WAS A STRING. A durable-cron helper emitted crontab lines naming an out-of-repo copy of itself rather than the canonical one it had been shipped as, and the card that reported it said only that the emitted lines invoke a different file. The file was there, so an existence check passes and the finding reads as cosmetic. Diffing the two copies is what turns it from cosmetic into serious: the out-of-repo target is a pre-revision copy whose fire path has no bus posting in it at all, so an armed resume fired, wrote one line to a log, and notified nobody, which is precisely the failure the helper exists to prevent. The rule that generalises is that when code hardcodes a path outside its own tree, existence is not identity, and the only check worth running is a diff of the target against the copy the repository believes is canonical. Fixing the emission does not reconcile the two copies, and the stale one is still on disk with no card tracking it.

A SET OF TESTS CAN GO RED AGAINST THE OLD IMPLEMENTATION AND STILL MEASURE ALMOST NOTHING, WHICH MAKES THE FAVOURITE PROOF IN THIS REPOSITORY WEAKER THAN IT LOOKS. Running a revision's new tests unchanged against the code it replaces has caught real vacuity three sessions running, and here all four touched tests duly failed, which reads as four arms of coverage. Every one of them failed with the same missing-attribute error, which proves only that a constant is new. The sharper instrument is a mutant that keeps the new interface and breaks only the behaviour, in this case defining the constant and then hardcoding it straight back to the path being eliminated: under that, two of the four still fail and the other two pass, because they put the module's own constant on both sides of their assertion and therefore hold for any value it takes. Two arms genuinely discriminate and two were credited by the write-up without discriminating anything, and the difference is invisible unless the API-preserving mutant is built. Worth pairing with the older habit: a test that passes on both trees has measured nothing, and a test that fails on the old tree may still have measured nothing.

THE MOST USEFUL FAILURE THIS SESSION WAS A SUITE RESULT THAT WAS PURELY THE REVIEWER'S OWN CONTAMINATION, AND IT WAS ONE PLAUSIBLE FAILURE RATHER THAN AN OBVIOUS WRECK. A trial merge reported a single test failing, in the very file the change touched, which is exactly what a real regression looks like and is far more convincing than a suite that collapses. The tree had been reverted underneath the running suite by an unrelated checkout issued into the same worktree, so the implementation being exercised at the end of the run was the base version rather than the change. Checksumming the implementation and the tests before starting, and confirming the tree is unmodified after finishing, separates the two cases in seconds and nothing else does, since a contaminated run and a genuine one produce identical-looking output. The companion lesson is smaller and sharper: never issue any command into a worktree that another job is currently reading. A second self-correction landed the same way, when two unit conversions were both about to be reported as wrong and explicit arithmetic reduced them to one, the other being a fair rounding, so the number was computed before the claim was made rather than after.

A PULL REQUEST HEAD CAN REPORT A FIX THAT THE MERGE DOES NOT CONTAIN, AND CHECKING THE HEAD IS THE OBVIOUS THING TO DO. A card asked for a trailing newline to be restored on a benchmark README, and on the branch head that file ended in a newline exactly as required, which reads as done. It was never touched. The branch was cut from a commit that predates the merge which removed the newline in the first place, so it inherited a clean copy and carried it forward untouched, while the changelog fragment it added described the fix as complete. On the trial merge the byte is still wrong, and the file does not appear in the branch's own diff at all. The rule that falls out is narrow and worth keeping: when a branch's base is older than the defect the branch claims to fix, the head cannot answer the question, and only the trial merge can. The same review corrected one of its own earlier claims, which had folded three findings into that pull request on the stated grounds that it already edited the file it does not edit.

TWO PROBES MEASURED NOTHING AND REPORTED IT AS A CLEAN RESULT, WHICH IS NOW THE FIFTH SESSION RUNNING IN WHICH THE INSTRUMENT RATHER THAN THE SUBJECT WAS AT FAULT. One read the wrong file, checking an export list in the module that does not define it, and returned an answer that looked like proof a regression was still present; it was caught only because a control disagreed with a value already known to be true. The other tested a regular expression using example strings that never engage its prefix, so an entire table of negatives was an artefact of the inputs rather than a property of the pattern, and a conclusion was published from it before the strings were re-read. Asserting that each probe input actually engages the thing under test, before any row is recorded, would have caught both in seconds. Pulling the other way, a widening flagged in that same gate looked like a new class of false positive until it was swept across all 339 tracked files, where it produced zero new reports and lost zero coverage, which turned a blocker into a single sentence of documentation; and a duplicate changelog finding died before it was filed once both copies proved byte-identical. Measuring a worry is as often what shrinks it as what confirms it.

A PROBE THAT NEVER RAN REPORTED A PASS THREE TIMES IN A ROW, AND THAT IS THE MOST TRANSFERABLE THING TO COME OUT OF EITHER SESSION. A chunk-size probe patched its target by replacing a whole-file string, the assertion counting the replacements threw before anything was written, and the run still printed five passed on three separate invocations, which is output indistinguishable from a clean result. Patched correctly by line number instead, the main line fails four of five at the larger receive size, so the real answer was the opposite of harmless. The same failure recurred twice more while reviewing the duplicate-definition gate: fixtures written as a bare class with a pass body assert nothing, because that gate tracks function definitions only and a class contributes scope without ever being a definition itself, and the replacement fixtures put method bodies at the same indentation as the class that should contain them, making them indentation errors that the gate skips by design. Both read as silent on every implementation including the one carrying the defect being hunted. The habit that catches all three is to print the patched line or parse the fixture before believing any pass, and to treat a fixture that does not compile as a failure of the probe rather than a result about the subject. The only reason the first of the three was caught is that a control disagreed with a value already known to be true.

FABRICATION MIGRATED FROM NUMBERS TO RELATIONSHIPS, AND THE CHAIN THAT PRODUCED IT IS NOW CLOSED. An earlier change invented two checksums, its revision removed them correctly and invented a byte size in the same edit, and the revision after that got every number right and then asserted a relationship between two dataset files, one of which exists nowhere on this machine. Measurement said the opposite of the claim, and the pattern worth carrying is that when a chain has been blocked twice for invented values the next thing to check is the non-numeric fields, because the invention moves to whichever adjacent field is still unverified. The final revision in that chain is merged. Every number in it was re-measured independently, the file it says is absent really is absent across the whole machine, the three sibling copies it says exist really do and are byte-identical, and the strongest claim it makes, that the oracle haystacks contain evidence sessions only, holds at five hundred of five hundred with a control confirming the file is not degenerate. Its nine-name list of benchmark runners, which replaced a catch-all phrase, reconciles at nine of nine real and none invented; a freshly written enumeration is exactly where the next invention would sit, so it was checked in both directions rather than spot-checked.

A DEFECT SURVIVED TWO REVIEWS BECAUSE THE PROBES COVERED THE SHAPES THE CHANGE NAMED AND NOT THE ADJACENT ONE. The duplicate-definition gate was widened to descend into classes defined inside function bodies, and the widening was correct, but a class defined inside a method collapsed to a bare class scope and collided with a real module-level class of the same name, because the function branch resets the class path while the scope string still begins with the word class. It was missed when the original was reviewed and again when its rebase was reviewed and called correct. The fix is one line. What makes the outcome trustworthy this time is that the scope shapes were enumerated independently rather than taken from the change's own test list: sixteen of them, each measured against the main line, the pre-fix commit and the fix, giving two mismatches, two mismatches and zero. That enumeration also turned up a second false positive nobody had named, where the same class name in two sibling methods of one class collided, and it is fixed by the same line. When a change alters a scoping rule, the reviewer owes the shape list, not the author.

A GATE HARDENING FIXED BOTH WEAKNESSES IT WAS FILED FOR AND QUIETLY NARROWED A THIRD THING NOBODY ASKED IT TO TOUCH. The witness-token gate's near-miss detector was flagging ordinary prose that merely mentions the marker word, and its exemption for the gate's own documentation examples was file-level, so a genuine de-marked marker appended to that file was swallowed whole. Both are fixed and both were measured with controls sitting flat on either side. The lookahead that spares the prose, however, also requires the marker payload to be present, so a de-marked marker with no payload went from being caught to being silent; the same shape with an intact colon is still caught through the other path, so the gap needs two coincident defects and is narrow, but it is in the silent direction, which is the direction the whole chain exists to close, and the changelog shipped a sentence claiming the opposite. It is carded with the correction rather than smoothed over. One thing was checked instead of assumed: the exemption now hardcodes three line numbers, which looks brittle, and inserting a single blank line into the docstring makes the gate fail loudly on itself rather than swallow anything, with the change's own test going red as well. Brittle in the safe direction is not a defect, and saying so in the record stops the next reviewer re-triaging it.

THE SAME AUTOMATED WARNING HAS NOW BEEN WRONG FOUR TIMES OUT OF FOUR, ALWAYS IN THE SAME SHAPE. It fires whenever a card's text contains the word test while the diff touches no test file, and it has fired on four documentation-only cards, two of which forbid a test in as many words because a guard asserting that a docstring contains a phrase passes on any rewording and cannot see the defect it is named after. It is a claim to be checked against the card, not a verdict, and on a documentation card it should not be re-triaged at all. Meanwhile the write-up pattern noted last session is now five pull requests deep: each omitted a suite count or a proof its card requested explicitly. Re-running the harness at review time retires the risk, and correct work is not bounced over paperwork, but five is a pattern in how work is written up rather than five separate lapses, and it has been raised on the bus rather than left in a review nobody re-reads.

TWO CHANGES WERE BOTH CORRECT, BOTH FULLY VERIFIED, AND ONLY ONE COULD LAND, WHICH IS A DIFFERENT KIND OF BLOCK AND HAS TO BE SAID PLAINLY. PR 340 is merged and PR 341 is blocked and closed with a rebase card, and nothing is wrong with PR 341. Both were dispatched from the same base and both rewrite the duplicate-definition gate and its test module, and they conflict in either merge order, which was measured in both directions rather than assumed from one. The one that landed first was chosen on a reason that makes the other's rebase smaller rather than on merit: the merged change deletes a parameter that the blocked change still passes, so the survivor now needs a single argument dropped instead of a re-derivation against moved code. The habit worth keeping is that a block on a conflict must state in its first line that the work is sound, because the closing verb is identical to the one used for fabricated values and a reader who skims the verb will conclude the opposite of the truth. Every measurement taken during the review was copied into the revision card, so a lane picking it up re-runs the probes as acceptance rather than rediscovering the finding.
THREE TESTS NAMED AFTER A BEHAVIOUR WERE SILENT ON EVERY GATE THAT HAS EVER EXISTED, INCLUDING THE ONE THEY WERE WRITTEN TO CATCH. The merged change fixed them, and the confirmation is the part worth carrying: the old fixtures contained no duplicate definition at all, only an import and an unrelated function, so the function under test returned the empty list on the current gate, on its predecessor, and on a version with the feature deleted outright. A test that passes on every implementation cannot fail, and its name is the only thing asserting that it guards anything. The rewritten fixtures were checked against the predecessor gate rather than trusted, and four of the five go red there while the fifth does not, because that predecessor already silenced the exact spelling it uses. That fifth case is a real improvement over testing nothing and it is not the discriminating test its summary line claims it is, and the difference was recorded in the merge rather than smoothed over. Both changes also omitted the suite count their cards required in the body, which is the same omission twice in one queue and is therefore a pattern in how the work is being written up rather than an oversight in one of them.
THE CHECK THAT VERIFIES A POSTED COMMENT SURVIVED INTACT REPORTED A DIFFERENCE THAT DID NOT EXIST, BECAUSE THE COMPARISON STRIPPED ONE SIDE HARDER THAN THE OTHER. The recipe carried forward for several sessions compares the text sent against the text read back with trailing newlines removed from the retrieved copy only, which is right when the sent text has no trailing newline and wrong the moment it does, since stripping then removes a character from one side that was never added to the other. It reported a mismatch on a comment that was in fact byte-identical, and the correct form strips both sides. This is the third session running in which the instrument rather than the subject was the thing at fault, and the pattern across those three is the same: the failure announced itself as a surprising result rather than as an error, and the only reason it was caught is that a surprising result was investigated instead of reported.

A REVISION THAT REMOVED TWO FABRICATED CHECKSUMS INVENTED A BYTE SIZE IN THE SAME EDIT, AND THE ONE NUMBER THAT COULD BE CHECKED WAS WRONG BY A FACTOR OF EIGHTEEN. PR 339 is blocked and closed, revision card open. Its job was to undo the invented identity data that blocked PR 334, and it did that part properly: both invalid checksums are gone, every remaining hash is either the pre-existing sixty-four character pin or an explicit statement that nothing is pinned, and the corrected list of nine benchmark runners is accurate, all nine existing and all nine loading the file they are said to load. What it added instead was a byte size for each of the two unpinned datasets, presented as fact, alongside a claim repeated twice that the files do not exist on this machine. Three byte-identical copies of the oracle file sit in sibling repositories on this machine, measuring fifteen million bytes against the two hundred and eighty five million the readme asserts, and a valid checksum for it could have been produced in a single command. The claim about the machine appears to be a true statement about the repository that was widened without being re-checked, since the data directory in this repository really does hold only a readme and one small file. The lesson worth carrying is narrower than "verify numbers", because the fabrication did not stop when it was caught, it moved: blocked on an invented hash, the work invented the adjacent field that was still unverified. A reviewer who confirms the specific value that was called out and reads no further will pass the same defect wearing different clothes. Both new sizes also carry the shape of generated numbers, one of them being exactly the leading nine digits of two to the forty-first power, but that is a signature and not the proof; the proof is the file on disk.

A SYMBOL VANISHED FROM THE PUBLIC EXPORT LIST WHILE EVERY ONE OF FIFTEEN HUNDRED AND SEVENTY TESTS STAYED GREEN. PR 336 is blocked and closed, revision card open, and this is the finding worth generalising. The change fixed both of the blockers it was filed for, and fixed them properly, with an authentication path that now fails closed where it previously served another agent's private messages to anyone who asked, proven by three controls that behave and one arm that flipped. While rewriting the export list to add its own two entries it dropped an unrelated third, a census function it has nothing to do with. The function, its route, its client method and all four of its tests survive untouched, so attribute access still succeeds and the suite cannot see the change at all; only a star-import notices, and only the deleted-symbols gate reports it. A green suite is therefore not evidence about the export surface, and the gate that does see it is advisory, which means its output has to be read rather than its exit code trusted. The same review found the fail-closed check is written against a normalising comparison that can never run, because a raw string comparison upstream raises first, so an honest client asking for its own messages under the at-prefixed form of its own handle is refused with an impersonation error naming itself. That is dead code that reads as a working safeguard.

TWO REVISIONS LANDED, ONE OF THEM ON THE FIFTH ATTEMPT AND THE FIRST OF THE FIVE TO CHANGE ANYTHING AT ALL. PR 337 is merged. The chain behind it had reached the point where a revision was published at the identical commit it was meant to revise, so the cheapest possible check, whether the branch differs from its predecessor, was written into the card as the first thing to run; this time it returns nearly three hundred kilobytes where the previous attempt returned nothing. All five blockers are fixed and each has a test that goes red against the unfixed code, demonstrated by copying the new test file unchanged onto the blocked tree and watching all seven fail. The durable resume helper now lives in the repository with a real test suite instead of only in an untracked tools directory. It still names an absolute path outside the repository in the crontab lines it emits, so the copy that was reviewed is not the copy that would run, which is carded rather than blocking because the alternative was leaving the helper untracked entirely. PR 338 is merged on the same standard, its gate now failing on the de-marked marker that previously passed silently, with the two weaknesses it introduced carded and, importantly, sequenced: its exemption cannot be narrowed until its pattern is tightened, because the gate's own documentation would otherwise trip it.

A LOAD-BEARING SYMLINK WAS POINTED AT A TEMPORARY WORKTREE OF AN UNMERGED BRANCH, AND WHEN THE WORKTREE WAS CLEANED UP THE FLEET RESUME PATH BROKE. The link that resolves the helper used to derive resume times had been repointed into a temporary directory belonging to the very change under review, and that directory no longer exists, so the path was dangling and silently unusable when this session picked it up. It has been restored to the canonical copy and the derivation re-verified as identical to the times already armed. It is deliberately not being pointed at the newly merged repository copy, because that copy still emits crontab lines naming the old location and the two must not disagree; the cutover waits on the card that fixes the path. The general shape is the one that keeps recurring here, which is that the thing which breaks is rarely the code under review and is usually a path, an export list, or a claim about the world that was true when it was written and silently stopped being true.

A CHANGE WHOSE ENTIRE PURPOSE WAS MAKING DATASETS IDENTIFIABLE INVENTED THE CHECKSUMS, AND ONE OF THEM IS NOT LONG ENOUGH TO BE A CHECKSUM. PR 334 is blocked and closed, revision card open. The card existed because a published number could be produced from a file nobody can identify: three LongMemEval dataset files are loaded by the benchmarks and only one was pinned. The change gave the other two full identity blocks, with a relationship to the pinned file, a question count, a byte size and a sha256 each. The sha256 for the cleaned set is sixty six hexadecimal characters. A sha256 is sixty four, so that string cannot match any file that has ever existed, and the value for the oracle set is a walk through consecutive hexadecimal pairs that continues into the cleaned one. Neither dataset is on this machine, which is the plain reason nothing could have been hashed: the data directory holds a readme and one small collections file and nothing else. The defect is therefore not an inaccuracy but an inversion, because no identity has been replaced with confident false identity, and a reader who follows the readme and runs the shasum line it supplies gets a mismatch that cannot distinguish a wrong file from a wrong readme. Leaving the hole and labelling it unpinned was available and would have closed the real gap honestly. The gate shipped alongside made the invention durable rather than catching it, because it tests for the presence of a heading in the readme and nothing inside it, so a heading with no checksum, no size, no count and no way to obtain the file is accepted as pinned, and a single check that a sha256 is sixty four hexadecimal characters would have caught the whole thing with no human reading it. The same gate cannot see any dataset whose name does not begin with the benchmark's prefix, and a real unpinned dataset that is present in the repo today is loaded through an argument default by the collections evaluation script and reported as fine. The readme's corrected list of which scripts load which file names two scripts that load the other dataset, which the shipped gate prints on its own first lines, so the change ships a tool that contradicts its own documentation. What was genuinely right is kept verbatim in the revision card: the environment overrides landed correctly in all four loaders, the loader census of thirteen matches the card, and the gate's red direction does fire, which was verified here because the body demonstrated neither direction despite the card asking for both in bold.

A REVISION FIXED ALL THREE BLOCKERS AND EACH ONE WAS PROVEN BY A CONTROL THAT DISAGREES. PR 335 is merged, the second revision in a row to be genuinely revised rather than the blocked tree under a new name, and the cheap check that establishes this is worth keeping because four earlier revisions were the unchanged tree. Diffing the two branches against each other suggests eight files and two hundred lines, and diffing each against its own merge base shows the truth: the blocked attempt contributed three lines and this one contributes seventy three, with the remainder being drift from the three merges that landed in between. The scan now covers the scripts directory as well, and an injected marker naming a test file that does not exist fails the new gate and passes the old one on the same tree, which is what shows the widening has power rather than merely being present. The citation was corrected and both of its new claims were checked against the sibling sources rather than taken on trust, confirming also that the sentence removed had been false. The live marker count inside the scanned scope went from two to zero, measured with the gate's own expression rather than by eye, using a zero width space between the word and its colon, a technique the card itself proposed. Its tests have real power: run unchanged against the previous implementation two fail and nineteen stay green, and the positive control passes twenty one. The two green tests for the new scope are vacuous alone, since the old gate never looked at that directory and returns the right answer for the wrong reason, and what gives them meaning is the paired non vacuity test that deletes only the token and demands the verdict flip. Two things were carded rather than blocked. The documented marker example, copied out and edited to real values, is silently ignored, which is the gate's worst failure mode and one it cannot warn about, since a marker it does not match is a marker it cannot see; and the widened scope currently verifies nothing at all, because the only markers that directory ever held were the examples this same change removed, so the coverage is real but wholly prospective and the changelog should not point at an instance that does not exist. One deviation is recorded against the card rather than the work, because the demand that the live marker count be zero in every file was unmeetable for the test module, whose remaining matches are the fixtures that are the test data.


A GATE FIX ARRIVED CORRECT AND THE ONLY THING WRONG WITH IT WAS A FEATURE THAT NO LONGER DOES ANYTHING. PR 333 is merged, the fourth pass over the duplicate definition gate and the first where every blocker moved. All ten cases from the card table give the verdict the card asked for, measured by loading each version of the gate directly and calling its collector on fixture files, and the columns for the main line and for the blocked attempt reproduce the table recorded last night exactly, which is what shows the instrument is sound rather than agreeing with itself. Definitions in mutually exclusive arms of one branch or one try statement no longer collide, while a definition at module level still collides with one inside a branch body, which is the binding truth the rule is meant to encode. Nested classes are scanned again at any depth. The tests have real power this time: run against the blocked attempt six of them fail, and the scope parity corpus is among them, asserting the complete verdict for every case from written constants rather than by recomputing the logic under test, so a scope that quietly stops firing now fails loudly. That corpus is the systemic request that took three rounds to land. Three defects were found and carded rather than blocked. A flag that marks an import fallback is threaded through eleven places and read by nothing, and forcing the function that sets it to answer no changes no verdict on any input that could be constructed; it died honourably, because the general rule about mutually exclusive arms swallowed the special case whole. As a consequence the three tests named after the exception spellings have no power at all, since each of their inputs contains a single definition and therefore no duplicate for any version of the gate ever written to find. And the changelog entry describing the widening was deleted while the widening itself still ships. The rule worth keeping from this is that a test which passes when its feature is deleted is normally enough to send work back, and it was not this time only because the feature it names had already become dead, so the test documents a mechanism instead of guarding one. The suite reads fifteen hundred and thirty nine, which is the baseline plus exactly the seventeen tests the file gained.


AN AUTOMATED WARNING ASKED FOR THE ONE THING THE CARD EXPLICITLY FORBADE, AND READING THE CARD WAS THE WHOLE REVIEW. PR 331 is merged. The pull request carried a generated warning saying the card asked for tests while the change touched no test file, and that the work should not land until the discrepancy was resolved. The card says the opposite in plain words, instructing the builder to write no test at all, and giving the reason: a guard over documentation wording would assert that some phrase is present, which passes on any rewording, fails on a harmless edit, and cannot see the defect it is named after. One of those has been shipped here before and it locked the bug in place. The warning had keyed on the word test occurring in the card at all. Changing no test file was therefore the acceptance criterion rather than a breach of it, and the builder also declined the trap the card had set by leaving every guard alone. This is the opposite outcome to the previous night, when the same class of generated warning was correct and understated, which is the whole argument for treating each one as a claim to be checked against the card rather than a verdict in either direction. The documentation itself describes an identifier list that exempts its members from the revocation check and from the refusal that fires when the revocation feed has never loaded. Every claim was measured rather than read, with controls on both sides: an exempt identifier is accepted both when it sits on the revocation feed and when the feed has never loaded, while an empty list and a list naming somebody else are both refused, and the two situations refuse for different stated reasons, which is what proves the probe separates the two mechanisms instead of producing one generic failure. A mismatch between the token subject and the claimed sender is still refused even for an exempt identifier. The justification the docstring offers, that the feed covers agents only, is not an unbacked assertion about somebody else's system: the transition specification already on the main line records it as a decision, though the same section notes that the boundary is still owed a pinning test on the controller side, and the review says so. The suite came back unchanged at fifteen hundred and one, which for a change of comments and docstrings is the expected answer and the third witness that no behaviour moved.

A THIRD ATTEMPT AT ONE MEASUREMENT FINALLY DISAGREED WITH THE VERSION IT REPLACED, AND THE DISAGREEMENT IS THE EVIDENCE. PR 332 is merged. Two earlier attempts at the same quantity were blocked, the first for simulating the network call and the second for making a real one and still keying on the wrong field. The harness watches for an upstream usage window rolling over. The response carries two numbers: the window identity, which changes exactly once at rollover, and the consumption figure, which moves whenever the account is used. The blocked version keyed on consumption, so on an account three agents share it would report the first ordinary tick as the rollover delay, biasing the answer downward, and downward is the dangerous direction for a lead time. The card demanded the fix be evidenced by a disagreement rather than by a passing test, and named the two inputs. Both versions were loaded side by side and run on identical inputs. Where both signals move together they agree, which proves each can reach the right answer. Where the identity is pinned and consumption drifts they split, the old one calling it a rollover and the new one correctly refusing. Where the identity changes on an idle window they split the other way, the old one seeing nothing and the new one measuring it. The polling budget now starts when the boundary arrives rather than at process start, so the documented default invocation went from never polling once to detecting. The strongest single check was running the new test file unchanged against the old harness, where five tests go red and sixteen stay green, the five being the behavioural ones and the sixteen being pins on artefacts the blocked version had genuinely fixed. What this replaces passed every one of its seventeen tests with the defect present and blessed it. The honest unmeasured label survives, which the card permitted and which a working harness with no live credentials should produce. One thing was noted without blocking: running the tool regenerates its evidence file with different spacing and a shorter reason than the copy committed beside it, so that record is a polished version of the output rather than the output itself, and a later run would overwrite the fuller text. The suite reads fifteen hundred and twenty two, which is the new baseline plus exactly the twenty one tests added.
A REVISION DELIVERED ITS CARRY FORWARD STEP AND NOTHING ELSE, AND THE FILE HOLDING EVERY BLOCKER WAS THE SAME OBJECT ON BOTH BRANCHES. PR 328 is closed under the block policy and its revision is carded. Its card opened with a step about mechanics, requiring the previous branch to be squashed in as the first commit because the executor bounces a card on any merge commit, and that step was performed exactly: the first and only commit is the squashed carry forward, there are no merge commits, and every piece of the previous work survives intact, including the dataclass carrying the import fallback flags, the pairing helper, the scope strings and all thirty three tests. What did not arrive is any of the three fixes the branch is titled after. The gate script is not merely equivalent between this branch and the branch it revises, it is the identical stored object, the same content identifier, and the difference between the two files is zero bytes; the test file is the same way. Against the main line the change reads as four files and two hundred and seventy added lines, which is what the carry forward looks like when the main branch has moved underneath it, and that appearance is the entire reason the absence is invisible from the pull request page. The card had even predicted it, telling the builder to diff against the branch being revised and expect only the new fixes. All three blockers were re-measured on the branch itself rather than inherited, by importing each version of the gate directly and calling its collector on fixture files, which sidesteps the repository root constant that has misled three earlier attempts. The platform split arms, the chained alternative arms and the sibling arms of one exception statement all still report a collision where the main branch is silent and exactly one arm can ever bind; the modern spelling of the import error still fires both bare and inside a tuple; and duplicate methods of a class nested inside a class are still invisible where the main branch catches them. Four controls in the same table hold their expected answers in both directions, including the widening the previous attempt genuinely delivered, so the instrument is shown to discriminate rather than agreeing with itself. The suite is the third witness and the quietest: it rises to fifteen hundred and two, which is the main line plus exactly the six net tests the previous attempt already had, so not one test was written for any blocker. The rule this hardens is that a clean carry forward produces a large diff and a rising count and neither is evidence, and the check that separates them costs two seconds.

A CENSUS THAT ONCE SAW ONE SENDER WHERE THERE WERE ELEVEN NOW SEES EVERY ONE OF THEM, AND THE REPORT IT CLAIMED TO POST EXISTS THIS TIME. PR 329 is merged. It answers an attempt that was blocked for two things, and both are repaired and were verified independently rather than taken from the body. The first was that the one deliverable, an audit report on the message bus, was described as posted and had never been posted; it is posted now, and the claim was checked by reading the channel back rather than by trusting the send response, and the report reconciles internally, with every channel's per sender column summing exactly to that channel's stated total across all eighteen channels, three thousand and fifty one messages and thirty seven distinct senders. The second was that the census saw a single sender where the live bus carried eleven, which is the direction that hides risk because the whole purpose of the step is to size the blast radius of a fleet wide authentication change. Measured on the shipped function rather than on the report, over a store rebuilt to the exact eleven sender distribution the card had recorded, it sees all eleven with every total and every per channel split exact, orders them correctly, agrees set for set with the sibling that lists a channel's members, returns nothing on an empty store as a discriminating control, and counts a hundred and twenty messages from one sender without truncation because the archive read carries an explicit limit, which is precisely the defect an earlier pull request was blocked for dropping. What the census does not answer is whether each sender holds a valid token and whether its subject matches the identity it claims, and the report says so plainly and gives the reason, which is that the registry feeds require a grant the available token does not hold; the card permits stopping short when it is stated plainly, so that is within scope rather than a gap. The census also independently found the two unauthenticated probe senders on the build channel, which corroborates from a second direction the observation that bus authentication is currently not enforced.

A PULL REQUEST DESCRIBED THREE CHANGES, SHIPPED ONE, AND THAT ONE MOVED THE COUNT IT CLAIMED TO FIX IN THE WRONG DIRECTION. PR 330 is closed under the block policy and its revision is carded. It was asked to widen a documentation gate's scan to the scripts directory, where the constant that motivated the gate actually lives, to correct a sentence in that gate misciting which sibling shares its scope, and to neutralise the illustrative marker examples in its own docstring so that widening the scope would not make the gate fail on itself. The scope is untouched: the glob is the same string on both sides and so is the line of the docstring that states it, so the scripts directory is still unscanned. The miscitation is untouched: the sentence is identical on both sides. The one change that landed rewrote the example marker into a quoted form, and the gate's own expression searches anywhere in a line rather than anchoring at the start, so a marker inside quotation marks still matches; counting the lines in that file which parse as live declarations gives two on the main branch and three on the branch, both of the new ones capturing fragments rather than a well formed reference. Controls in the same run behave, with ordinary prose staying silent and the original example matching and capturing a well formed reference, so the instrument discriminates. No test file is touched, so neither the failing control nor the non regression control the card required exists, and the pull request's own automated warning said exactly that and was correct. Live exposure is zero, but only because the scope was never widened, so the two defects mask each other and repairing only the scope would light every one of those lines at once. Every gate in the repository passes on this branch, which is the uncomfortable part and the general lesson: gates check the tree and not the description, and the check that caught this was reading each claim in the body against a line the diff actually changed.

A DIFF AGAINST A MOVED MAIN BRANCH SHOWED A FEATURE BEING DELETED THAT THE BRANCH HAD NEVER TOUCHED. While opening the next review, the comparison against the main line reported the newly merged census being removed across five files, which is the exact shape of a real loss recorded twice this month. It was not one. The branch was cut before that merge landed, so the comparison was against a main branch that had moved underneath it, and the true change measured against the branch's own base is four files and thirty six added lines. Merging it in a scratch tree and counting the census in each of the files it lives in confirms every occurrence survives, along with its tests and its changelog. The rule that separates the two cases is to diff against the merge base rather than against the current main branch before reporting a deletion, because both situations produce a stat block full of removals and only one of them is a defect.

A CARRY FORWARD WAS DONE PERFECTLY AND NEITHER BLOCKER WAS TOUCHED, AND THE HEALTHY LOOKING DIFF IS WHY. PR 327 is closed under the block policy and its revision is carded. The card it answers opens with a step about mechanics, because the executor bounces a card on any merge commit in the range, so the previous work had to be squashed in rather than merged, and that step was followed exactly: there are no merge commits, the whole file set came across, all twenty five inherited tests are present, the suite rises to fifteen hundred and twenty one, and every gate is clean. What did not happen is either fix. The handler that resolves which agent's mention feed to serve is string equal between this branch and the branch it revises, the same length to the character and at the same line numbers, so the query parameter still wins over the verified token subject and there is no comparison anywhere in the function that could fail closed. Measured on the merged tree with the branch's own authenticated fixture, one agent's valid token asking for another agent's mentions returns the other agent's message where the card requires a refusal, with both controls behaving. The second blocker is untouched in the same way: the feed's archive read still carries no explicit row limit while four sibling reads in the same module all carry one, so the visibility cliff is still exactly at fifty messages, present at ten and forty and forty nine and gone at fifty one and above, on a live channel past three thousand one hundred. The suite count is the third witness and the quietest one, because the rise is exactly the number of inherited tests and therefore proves that nothing was added for either blocker, which the card had required by name. The lesson is that a clean carry forward produces a large diff and a rising count, and both read as progress on a revision while being evidence of neither; the check that separates them is to diff the named lines against the branch being revised and re-run the arms the previous review recorded, which took seconds and answered both.

A HARNESS TRADED ITS SIMULATOR FOR A REAL NETWORK CALL AND STILL DID NOT MEASURE THE QUANTITY. PR 325 is closed under the block policy and its revision is carded. The card it answers was itself a revision, of an attempt that simulated an upstream usage window rollover and then labelled the result measured in the source, so this is the third pass at one number. Much of it is genuinely repaired and was verified rather than taken from the body: the mock class is gone and the fetch really does issue a request to the upstream usage endpoint, the evidence is committed inside the repository instead of a temporary directory that gets reaped, its timestamp is in the past, the fabricated identical range is gone, the scratch scripts have left the repository root, and nothing outside the repository is written any more, which was checked by enumerating every write in the file rather than by grep. The honest part is the best part, because with no credentials available the run records the measurement as not taken and leaves the constant unlabelled, which the card explicitly permits. What blocks it is that the instrument shipped alongside that honest record cannot take the measurement it exists for. The response carries two fields, one being the window's identity, which changes exactly once at a rollover, and the other being a utilisation fraction that moves whenever the account is used at all. The harness keys on the utilisation and never reads the identity. Holding one field fixed and moving the other, against a control where a real rollover is correctly reported: with the identity pinned and the utilisation drifting, which is what an account shared by three agents does all day, it reports a measurement of something that did not happen, and with the identity moving and the utilisation flat, which is a rollover on an idle window, it reports nothing at all. The false reading is also biased in the dangerous direction, because it is bounded by whenever the next unrelated request happened to land, and the constant it feeds is a lead time. Separately the documented default invocation polls zero times, because the deadline clock is started before the wait for the boundary rather than after it, so a boundary up to five hours away consumes a ten minute budget before the first request is made, which was measured as zero calls and zero samples on a scaled model.

A TEST NAMED FOR THE BEHAVIOUR IT PINS PASSED WITH THE BEHAVIOUR ABSENT, BECAUSE ITS FIXTURE MOVED TWO SIGNALS AT ONCE. The same branch carries seventeen tests and all seventeen were confirmed to fail against the tree they revise, so the red first requirement is genuinely met and was reproduced independently rather than believed. Fourteen of them are searches of the shipped source for words the previous attempt contained, which are fair regression pins for those particular artefacts and are not evidence that anything is measured. The two that exercise behaviour both feed a fixture in which the window identity and the utilisation change together, so neither can tell which one the code is reading. Pinning the identity so that the window never rolls, and changing nothing else, leaves the file passing seventeen of seventeen, with the test named for detecting the flip asserting a measurement for a window that stayed put. It does not merely miss the defect, it certifies it. The general form, and the successor to the lesson the first attempt produced about relabelling a guess as a measurement, is that replacing a simulator with a real network call does not make something a measurement when the field it keys on is not the event, and the way to catch that is one fixture per signal with the verdicts pinned in opposite directions.

A REVISION KEPT THE ROUTES AND DROPPED THE IMPLEMENTATION THEY CALL, AND ITS OWN DIFF SHOWED ONLY ADDITIONS. PR 326 is closed under the block policy and its revision is carded. The review it answers ended with an explicit instruction to keep the storage module and its migration because both were sound, and singled the migration out as the thing two earlier attempts had got wrong and this one had got right. The branch rebuilt from the main line and kept the transport routing and a test file, so a hundred and fifty eight lines of storage, all five service wrappers and the store integration are simply absent, and the endpoints have landed without the code they call. Against the main line the change reads as pure addition across three files with nothing removed, which is precisely why the loss is invisible there and only appears on diffing the branch against the branch it revises. The consequences were measured rather than inferred. The test file imports a module that exists nowhere, so collection stops and the suite goes from fourteen hundred and ninety six passing to zero tests run, with none of its five hundred and forty six lines having ever executed. Driven against a real server, four of the five advertised endpoints answer with a server error naming the missing function, and the fifth is refused at the administrative gate before reaching it. One route cannot be reached at all because the handler defines no method of that verb, and the same route is registered twice with two different meanings, so adding the verb would expose the first and leave the second dead.

AN IDENTITY FIX WAS UNREACHABLE, AND COULD NOT HAVE WORKED IF IT HAD BEEN REACHED. The headline blocker on that card was that receipt identity is forgeable and the documentation claimed otherwise, and the branch answers it with a function that appears exactly once in the file, at its own definition. The branch it revises called that function three times. Both write paths continue to take the identity from the request body, so a receipt is still written as whatever agent the caller names, and it is now weaker rather than stronger because no credential needs to be presented at all. The function could not have helped in any case: it asks the verifier to authorise the token against an empty claimed identity, and the verifier's policy is that the token subject must equal the claimed identity, so measured against a positive control that accepts a valid token and a negative control that rejects a forged one, the call as written rejects everything including perfectly good credentials. The documentation moved in the wrong direction too. The previous review said one word asserting verification had to go because the code did not verify, and it went, replaced by a sentence stating that the receipt system never records a forged identity. The lesson is the one an earlier block this month produced in mirror image, where a fix shipped without the half that produces what it consumes: when a review says keep this part, the keeping is a deliverable, and it cannot be seen in the pull request's own diff.

A REVISION PULL REQUEST WAS THE BLOCKED COMMIT UNDER A NEW BRANCH NAME, AND ONE COMMAND SAYS SO. PR 323 is closed under the block policy and its revision is carded again. It was opened to answer five blockers recorded against an earlier attempt at the durable resume cron, and it answers none of them, because the branch it was pushed on points at exactly the same commit as the branch it was revising. The two refs share a commit identifier and a tree identifier and the difference between them is zero bytes. The single commit is timestamped before the review it claims to respond to, and both its subject line and its changelog file still name the older card. Every one of the five blockers was re-measured on that tree rather than inherited from the previous review, and all five stand: the helper is still a hundred and forty two lines against a canonical seven hundred and twenty one, and fed the same reset instant the two disagree on both the times and the shape, one emitting a pair of daily recurring jobs and the other a pair of date pinned one shots; the one test the previous card demanded still reads the real log in the home directory and still keys on a bare word that twenty four pre-existing lines already satisfy; the emitted line still calls the bare interpreter that cannot import the package; three tests still reach the real crontab with only one of them substituting the subprocess module; and the function that formats the crontab error messages still calls a module that is never imported. The pull request body also advertised a fourteen file change of several hundred insertions and eight hundred deletions, where the real change against its own base is three files and four hundred and nine added lines and nothing removed, the large deletion being the main branch having moved rather than anything the branch does. The rule this produces is the cheapest check in any review of a revision, which is to diff the branch against the branch it revises before reading anything else, because empty output means nothing was revised no matter how substantial the comparison against the main branch looks.

A GATE EARNED ITS MERGE BY DISAGREEING WITH THE NAIVE VERSION OF ITSELF IN BOTH DIRECTIONS. PR 322 is merged. It adds an opt in marker that lets a source comment declare which token inside which test file justifies it, and a check that the token actually resolves there. The defect it exists to catch is a comment citing a test as the justification for a constant, where a revert later deletes the cited witness from the test while the test file itself survives and the suite stays green. The obvious version of this check, resolving the file path the comment names, cannot fail on that case and does fail on the harmless prose written afterwards to describe the removal, so it is wrong in both directions at once. Rather than take the four evidence blocks in the body on trust they were rebuilt independently, and the naive checker was written out and run beside the delivered one over the same two inputs. With the token deleted and the test file still present and importable the naive one reports clean and the delivered one fails; with a bare filename mentioned in ordinary prose the naive one fails and the delivered one reports clean. That disagreement is what turns a clean result into evidence. The decision to leave test files outside the scan was verified rather than accepted, because the test file builds its fixture markers as formatted strings and those would be read as live declarations. Two gaps were carded rather than held against the build, since nothing in the package declares a marker yet: the scan does not cover the scripts directory, which is where the constant that motivated the whole thing actually lives and where a pending card will move it, and the justification in the file miscites which sibling gate shares its scope.

A FAILURE CLASSIFICATION THAT NAMED TWO CLASSES QUIETLY ACQUIRED A THIRD, AND PUT IT IN THE ONE MARKED IMPERSONATION. PR 324 is closed under the block policy and its revision is carded. It was asked to stop treating a missing permission grant as though it were a forged credential, and it does that correctly, with the split written out at the top of the handler and proved in the right order, every new test failing against the unfixed code first. The blocker is what the new arm also catches. The verifier fetches the revocation list before it can authorise anyone, and when that fetch fails it raises the same error type as a bad signature, so the branch that always rejects regardless of mode now also rejects on an infrastructure outage. Measured end to end with a token that is valid in every respect and an agent that holds the grant, changing nothing but the reachability of the revocation feed: the current main branch accepts and warns, and this branch refuses, and the operator is told the refusal was a presented credential failure. Half of that is not even the new feature, it is pre-existing behaviour whose disposition in the default mode this change silently flips, with nothing in the changelog and no test in either direction. Failing closed on a stale revocation list is right and stays; what has to be decided and written down is which of the two modes it refuses in. It is carded rather than merged because the card being revised is itself titled after an undocumented third failure class, and this would have reproduced the same shape at a different point in the same function.

THE REVIEWER'S PROBE MEASURED A DIFFERENT TREE THAN THE ONE ON REVIEW, AND ONLY THE CONTROL CAUGHT IT. The first run of that end to end probe reported the same accepting answer in both arms, which reads as a clean bill of health for the branch. It was measuring the main checkout instead. Running a script by absolute path puts the script's own directory first on the import path rather than the working directory, so the package resolved through the editable install and none of the branch's code was ever exercised. Standing in the right directory is not enough when the interpreter is handed a path somewhere else. What exposed it was a positive control in the same run, an invalid credential whose answer is known in advance and differs between the two trees, which came back with the old answer and named the tree. Copying the probe into each tree, running it from there, and having it print the file the package was loaded from produced the real table. This is the fifth instrument failure of the week and the second where the failure pointed at the comfortable conclusion rather than the alarming one.

A FIX FOR A SILENT IDENTITY SUBSTITUTION AUTHORISED THE SUBSTITUTION INSTEAD, AND THE CARD'S OWN WORDING ASKED FOR IT. PR 320 is closed under the block policy and its revision is carded. The mention feed had two identity defects: the client never sent the reader it was asked about, so an install holding one agent's token and asking for another's mentions quietly received its own, and the handles were compared raw. Sending the reader is the right fix and the branch does it correctly. What went wrong is the other end. The server now prefers the reader named in the query string and falls back to the verified token subject only when the parameter is absent, so any holder of any valid token can read any other agent's mentions. Measured with the same token in both arms and the query parameter as the only difference, one returns the caller's own messages and the other returns the other agent's, and the pre-fix branch returns the caller's own in both. It is reachable through the shipped client as well as by hand. The feed is the worst possible place for it, because a mention grants the mentioned message plus its whole reply chain regardless of channel and the read guard behind it is a function that returns true unconditionally. The honest part of this finding is that the card's acceptance criterion said the caller asking for another agent's mentions should get that agent's mentions or a clear error, which read literally at the server is exactly what was built. That wording is the lead's defect, it is corrected on the revision card rather than charged to the lane, and the shape the card should have stated is a fallback plus a closed door on mismatch and never the reuse of the caller's claim.

A PERFORMANCE WIN ARRIVED TOGETHER WITH A NARROWED INPUT SET, WHICH MEANS IT WAS NOT MEASURED AT ALL. The same branch replaced a repeated full rescan of the bus with a single adjacency walk, which is a genuine improvement, and reported it as replacing quadratic behaviour with linear behaviour. It also dropped the explicit row limit from the archive query that feeds the walk, and the underlying query defaults to the fifty most recent rows. So the feed now considers fifty messages of the entire bus, and a mention older than that is filtered out and the endpoint answers with an empty list and a success status. The cliff was measured by posting one mention and then a growing number of unrelated messages: the mention is returned at bus sizes ten, forty and forty nine, and lost at fifty one and everywhere above, while the branch being revised returns it at every size up to three thousand. The live channel is past three thousand one hundred messages. The speed comparison was therefore between scanning fifty rows and scanning everything, which is not a comparison. The general rule is that a change which makes something faster and also makes it look at less has to be re-measured on the full input before the speed claim means anything, and the narrower detail worth keeping is that this was the only bus read in the service module without an explicit limit while five siblings all carry one.

A PROBE THAT RETURNED THE SAME ANSWER IN EVERY ARM WAS NOT BROKEN THIS TIME, IT WAS THE FINDING. The visibility cliff above was found by accident, through a timing probe that reported zero messages returned at every bus size it tried. By the standing rule that is a broken instrument and the result should be discarded, and the first instinct was to discard it. What separated the two readings was adding a small case at the bottom of the range, where the answer came back as one, so the probe was proved capable of returning a non empty answer and the zeros above it became data. The rule is unchanged, because an unproved probe still tells you nothing, but it now has a second half: prove the probe on an input where it must succeed before concluding it is broken, in the same way one proves it on a known bad input before concluding it is clean.

A GATE FIRING ON CORRECT CODE IS A WORSE OUTCOME THAN A GATE MISSING A DEFECT, AND THAT DECIDED A BLOCK. PR 321 is closed under the block policy and its revision is carded. It was asked to widen the duplicate definition guard so that a definition inside a module level branch and two identically named inner functions in one parent are both seen, and it does both, proved in the right order with six of its eight new tests failing against the current guard and the remaining two green on both trees by design because they forbid an over wide fix. The problem is what else the widening caught. Definitions in the two arms of one conditional are now treated as though they redefine each other, so the ordinary pattern of defining a function one way on one platform and another way otherwise is reported, although exactly one arm ever binds and nothing is shadowed. The import fallback that the card explicitly required to stay quiet is recognised by matching the exception name as a literal string, so writing the modern spelling of that same exception makes the identical pattern fire. Both are the class of false positive that the card itself cites as having stopped an earlier attempt. A guard that reports correct work is switched off or ignored within a week, so for this kind of tool the false alarm costs more than the miss, and that is what moved the verdict.

A SECOND CONSECUTIVE CHANGE TO ONE GUARD WIDENED A SCOPE AND SILENTLY NARROWED ANOTHER. The same branch stops descending into a class nested inside another class, which the current guard does scan, so duplicate methods there became invisible. There are none in the repository today, and the narrowing was sized rather than guessed across the same one hundred and eighty three files, so on its own it would have been carded exactly as the equivalent narrowing was carded a session earlier rather than held against a build. It is in the block only because the branch is going back for the false alarms anyway. The pattern is the thing worth recording: both narrowings were invisible in the change itself and surfaced only because someone ran the old and new versions of the tool over the same inputs and required them to disagree. The durable fix is not another careful reviewer, it is a test that pins one fixture per scope the tool claims to cover along with the legitimate patterns it must stay quiet on, so that dropping a scope fails a test instead of waiting to be noticed.

A FOLLOW UP WAS CITED BY AN IDENTIFIER THAT DID NOT EXIST YET, WHICH IS THE MISTAKE THIS FILE RECORDED YESTERDAY. Closing the first of the two cards, the close reason named the revision card by an identifier invented before the card was created, and identifiers are assigned by the board. The record now points at nothing. The board refuses both an edit and a second close, so the correction went to the channel and onto the pull request itself, where anyone following the card will meet it. The rule that was already written down after the previous instance covers this exactly, that a dependency or a follow up stated as an identifier which resolves to nothing is not a safeguard, and the reason it was broken again is that the sentence was written at the moment the intention formed rather than at the moment the thing existed. Create the record first, then cite it.

A TEST CAN BE SATISFIED BY STATE THAT EXISTED BEFORE IT EVER RAN, AND THAT IS A LEVEL DEEPER THAN A TEST THAT RECOMPUTES ITS SUBJECT. PR 317 is closed under the block policy and its revision is carded. The single test its card required, the one meant to run the emitted command as a subprocess and prove the durable cron line has an effect, asserts that a record exists by reading the real log file in the user's home directory and looking for the word fired. That file already held twenty four such lines from earlier runs of the same feature, so the alternative branch of the assertion is true before the subprocess is even started. The control is decisive and cheap: reduce the fire function to an immediate return and the test still passes, in under half a second against three and a third seconds for the live version. Where the banner tests blocked earlier in the day passed because they recomputed the logic they were testing, this one passes because its evidence predates it. The rule that covers both is to ask what an assertion would say if the code under test did nothing at all, and to ask it specifically whenever the assertion reads shared mutable state such as a real log, a real crontab or the home directory.

A TEST SUITE WROTE THE DEVELOPER'S REAL CRONTAB, AND WOULD HAVE DISARMED THE MECHANISM IT EXISTS TO TEST. The same branch calls the fire function from three tests and only one of them replaces the subprocess module first, so the other two, and the subprocess test as well, run the crontab reader and the crontab writer against whatever crontab belongs to the person running the suite. It is harmless at present only by accident, because the marker is a hash of a script path that nothing is currently armed with. Demonstrated against a sandbox crontab holding a correctly armed pair, the fire path removes the marker line and leaves the file two lines shorter. So the moment the helper is used the way it is meant to be used, running the tests deletes the armed resume line. The general form is that any test invoking a real system command needs the command sandboxed on the path and the home directory redirected, and that a side effect which is currently harmless by coincidence is still a defect.

THE REVIEWER'S OWN PROBE MANUFACTURED A BLOCKER THAT DID NOT EXIST, AND THE FIX IT WOULD HAVE CONDEMNED WAS CORRECT. Checking whether the rewritten crontab now ends in a newline meant building a fake crontab program, and that fake read its input through command substitution, which strips trailing newlines unconditionally. The shim therefore reported the input as missing its final newline in every case including the correct ones, and because that is the exact symptom the previous attempt was blocked for, it read as a confirmed regression rather than as a broken instrument. The trailing newline fix is genuinely correct and would have been blocked on the strength of it. Rewriting the shim to read its input raw, and then proving it on one known good and one known bad input before trusting anything it said, separated them immediately. This is the fourth time this week that an instrument has failed in the direction that looks like a finding, and the discipline that catches it is to prove the probe before reading the result.

AN EMPTY PULL REQUEST CLAIMED AN AUDIT HAD BEEN POSTED WHERE NO SUCH POST EXISTS, FOR THE SECOND TIME ON THE SAME CARD LINEAGE. PR 318 is closed under the block policy. It carries a single empty commit, which is permitted, because the card explicitly allows the work to stop short when its dependencies have not landed and asks only that the pull request say so plainly, which this one does. What it does not have is the one deliverable the card required in that situation, a report posted to the build channel with a census of which senders would be rejected if enforcement were switched on. Every live channel was searched and no such report exists. The census quoted in the body is also wrong in the direction that hides risk: it reports a single sender with two messages, where the live channel carries eleven distinct senders and the one it names has seventeen messages in the same window, which points at it having been run against an empty local data directory. Sizing the blast radius is the entire purpose of that step, and a census showing one quiet sender makes a change that would silence the whole fleet look nearly free. Two of the pull request's three claims did hold and were confirmed in the source rather than assumed, including that the enforcement flag is genuinely inert while no registry address is configured, because the verifier is only constructed when one is set and the flag is only read inside the block that verifier guards.

A CARD OF THE LEAD'S OWN NAMED TWO HARD DEPENDENCIES THAT DO NOT EXIST, WHICH MADE THE BLOCK UNACTIONABLE RATHER THAN CAUTIOUS. The same card told the lane not to proceed until two other pieces of work had landed, and both identifiers return not found on this project. A dependency stated as an identifier that resolves to nothing is not a safeguard, because there is nothing to watch and no condition that can ever be observed to be met. The correction is to state such a dependency as a checkable condition, evidenced by the change that satisfies it, and it belongs on the revision card rather than against the lane that did as it was told. This is the third scope gap of the lead's own making recorded this way rather than charged to a build.

A DUPLICATE DEFINITION GUARD KEYED ON ONE HARDCODED NAME NOW SEES EVERY NAME, AND THE PROOF WAS THE OLD AND NEW GUARDS DISAGREEING ON ONE REAL TREE. PR 316 is merged. The guard could previously only notice rival definitions of a single internal helper, so duplicate test functions, which is where one had landed the same day, were invisible to it. Run against the preserved branch that carries three test functions defined twice, the new guard fails and names all three at the exact line pairs the card recorded, while the old guard reports clean on the identical tree. The original single name case is still caught and now has a test pinning it, and the legitimate same name pairs that a naive generalisation would break, overload stubs and property accessor pairs, stay silent, which matters because a guard that fires on correct code is switched off within a week. The change also narrows what the scan can see, trading a walk of every node for a walk of module and class bodies, and two of the four resulting differences are corrections of false positives while two are genuine losses. That was sized rather than asserted: the tree holds five definitions inside module level control flow and a hundred and ninety nested inside another function, and none of them are duplicated today, so the loss is a forward gap and is carded rather than held against a build that delivered what it was asked for.

A REVISION MEASURED THE PROPERTY INSTEAD OF THE MECHANISM, AND ITS CARRY FORWARD WAS CHECKED AS A DELIVERABLE IN ITS OWN RIGHT. PR 319 is merged. The production change it inherits was already reviewed and found correct, and the card told it to carry that forward untouched, so the check that matters is invisible in the pull request's own diff and had to be made against the branch being revised, where it comes back empty and therefore byte identical. The tests are the part that was rebuilt, and they now run the server in a real subprocess, read its actual output, and compare against literal expected strings rather than against a copy of the branching logic. Each subprocess also prints the file it loaded the module from and the test asserts that against the module the tests themselves imported, which closes the standing trap where standing in a working tree does not bind the interpreter to it. Reproduced independently rather than taken from the body: reverting only the production file on the merged tree, having first confirmed the banner text is absent from the source, turns all six red, and the fixed tree turns them green. That is the exact inverse of the attempt that was blocked, where the same six cases passed against unfixed code. The shadowed duplicates are gone as well, confirmed not by eye but by running the guard that had merged an hour earlier and fires on the old branch.


THE SAME SUITE COUNT MEANT OPPOSITE THINGS ON TWO PULL REQUESTS AN HOUR APART, AND THE DIFFERENCE IS WHAT THE DIFF TOUCHED. PR 313 and PR 314 are both merged. The first of them reported fourteen hundred and thirty five passed and twelve skipped, which is the baseline to the test and is exactly the number the pull request blocked earlier this evening was blocked for. Here it is correct, because that change is prose only: a paragraph of documentation, a test docstring and a changelog entry, with no production path touched and therefore no behaviour for a new test to pin. The rule worth carrying is not that a baseline count is bad. It is that the count has to be read against what the change touches, so that a production change reporting the baseline says no test was added, while a prose change reporting a moved count would be the thing to question.

A DOCUMENT WHOSE ENTIRE SUBJECT IS WHICH LAYER PRODUCES WHICH ERROR HAD THE STRING FROM THE WRONG LAYER, AND READING THE DIFF WOULD HAVE BEEN THE WRONG CHECK. The verification document and a test docstring both quoted the message the transport handler emits and attributed it to the service layer underneath. Confirming the correction meant reproducing the experiment rather than inspecting the change: disabling the transport level invariant leaves the status assertion passing, because the service layer independently rejects an empty body, and turns exactly one thing red, the message assertion, which reports that the service layer says the body must be a non empty string. That is the text the branch now quotes in both places, the two source line references it cites are accurate, and the wrong string survives nowhere else in the documentation or the tests. The finding this all sits on, that the status assertion is duplicated and only the message assertion is load bearing, was confirmed in the same run.

A GATE THAT COULD NOT SEE THE PART OF THE PUBLIC INTERFACE CONSUMERS ACTUALLY IMPORT CAN SEE IT NOW, AND THE PROOF WAS TWO VERSIONS OF THE GATE DISAGREEING ON ONE TREE. The export removal guard only recognised names defined in the same file as the export list, so a re-exported name, which is most of the package's own top level interface, could be dropped from that list while its import line and definition survived and nothing would report it. All four controls were reproduced on the repository itself rather than on fixtures, and the red was demonstrated in the hardest available shape, an aliased re-export where the exported name exists nowhere as a definition: dropping it from the export list with the import intact makes the new gate fail and name it, while the old gate on the identical committed tree reports clean. A genuine deletion, where the entry and the import and the class all go, leaves the export half silent and is reported once by the half that owns it. The replay against the standing known bad branch produces findings identical to the old gate, so nothing regressed. The suite moved by twenty six tests, which is what a change carrying three hundred and sixty lines of tests should look like.

A REVIEWER COMPARING TWO VERSIONS OF A TOOL CAN BREAK THE COMPARISON IN A WAY THAT DOES NOT ANNOUNCE ITSELF. Running the older gate side by side with the new one meant copying its script somewhere neutral, and the script derives the repository root from its own file location, so it resolved the root to the temporary directory's parent and died inside the archive command. The failure was loud this time, but the same mistake with a scan rather than an archive returns a clean result for the wrong directory, which is the trap recorded twice already this week. The comparison script belongs inside the tree under test.

A SUITE COUNT IDENTICAL TO THE BASELINE IS POSITIVE EVIDENCE THAT NO TEST WAS ADDED, AND IT TOOK A SECOND LOOK TO SEE IT. PR 312 is closed under the block policy and its revision is carded. The production change in it is correct and was verified before anything else was said about it: the two unguarded coercions that closed the previous attempt are both properly defended now, one by a caught conversion falling back to zero with a warning and one by a string type check, and the proof is the same five cases run against three trees at once, where the unfixed tree fails exactly the two ways its card predicted and the branch passes all five. The three trees disagree, so the clean result is evidence rather than a hope. What the branch does not have is a single test. The trial merge suite reported fourteen hundred and thirty five passed and twelve skipped, which is green, and which is also master's baseline to the test. The whole test difference against master is one widened assertion and nothing added, on a card whose acceptance asked for a red demonstration on each field and a permanent regression test for the exact import that broke. The count was the finding and it was sitting in plain sight at the top of the log.

A REVISION WAS TOLD TO CARRY FORWARD THE WORK ITS PREDECESSOR GOT RIGHT AND SHIPPED THE CONSUMER OF A FEATURE WITHOUT ITS PRODUCER. The same pull request branched from master, kept the block that reads document currency metadata out of a search hit, and dropped everything that writes it: the front matter parser, the key allowlist, the indexing timestamp written at ingest time, and two hundred and eighty six lines of tests including the end to end one the card named explicitly and asked to be kept exactly as it was. The collections module on the branch is byte identical to master. The consequence is measurable rather than stylistic: searching the merged tree for either metadata key returns eight occurrences and every one of them is inside the new block that consumes them, so nothing in the repository writes either key, the indexing timestamp always falls back to the row timestamp, and the review overdue flag is unreachable except through metadata a caller supplies by hand. The block's own comment cites the ingest path that stores the data, and that path does not exist on the tree the comment ships on. This gap was invisible in the pull request's own diff, which is a clean three file change, and only appeared on diffing the branch against the predecessor it was revising. When a card says carry this forward, the carry forward is a deliverable and has to be reviewed as one.

A CHANGELOG DOCUMENTED A DEFECT THAT NEVER EXISTED IN ANY VERSION OF THE CODE. The same branch ships a user facing entry announcing a fix for an unbound local variable in the hit formatter. Master has no such block at all, and on the previous attempt the block already sat below the assignment it reads, which is demonstrable rather than arguable because two of the five probe cases pass on that tree and could not if the function raised. The only remaining change is moving one assignment below the new block, which alters nothing. A changelog is a record, and an entry claiming a defect was shipped and then repaired is a false claim in the most durable place a project keeps them.

THE QUEUE REFILLED FOR THE SIXTH AND SEVENTH TIME IN ONE DAY AND ALL THREE ARRIVALS WERE REVISIONS OF PULL REQUESTS BLOCKED EARLIER THE SAME DAY. PR 309 and PR 310 are closed under the block policy and PR 311 is merged. Two of the three revisions were genuine attempts at the blockers and one of them made its blocker measurably worse. The pattern across all three is worth stating plainly: a revision is a second chance to measure the property, and two of these three instead measured the mechanism a second time.

A REVISION BUILT TO GIVE A DURABLE CRON LINE AN EFFECT REMOVED THE ONLY EFFECT IT HAD. PR 309 was carded because the durable resume line, on firing, wrote one log entry and deleted itself, and nothing consumed the log. The revision replaces the log with a bus post and the result is a line that does nothing at all and reports success. Running the command exactly as the emitted crontab line spells it produced two independent failures in one invocation, both swallowed by a bare exception handler, and an exit status of zero. The bus post cannot happen because the emitted line invokes the system interpreter, which cannot import the package at all, while the import only resolves under the project virtual environment; the failure is caught and discarded. The self removal cannot happen either because the rewritten crontab is joined without a trailing newline and the crontab program refuses such input outright, and that refusal is a called process error caught by the same handler. The previous version at least left a record. This one leaves nothing, and the log append that was the fallback was deleted from the file. Neither failure is visible to the tests because the bus test calls the fire function in process under the virtual environment interpreter where the import succeeds, and the crontab test replaces the subprocess module so the crontab program is never invoked and its newline requirement is never met.

THAT SAME REVISION ADDED A THIRD COPY OF THE HELPER IT WAS SUPPOSED TO CONSOLIDATE, AND THE NEW COPY DISAGREES WITH THE TWO IT JOINS. The original finding was that two byte identical copies existed and would diverge the moment anyone edited the one they had been told to run. The revision removes neither and adds a hundred and seventeen line reimplementation beside the seven hundred and twenty one line original, then points the documentation at the new one. For a window reset at midnight the live helper derives an arming time of seven minutes past, printing the watcher ticks it read and the measured margin it derived them from, while the new file derives seventeen minutes past for the primary and thirty seven for the retry from two constants that appear for the first time with no derivation and no measurement. The currently armed pair sits on the old derivation. A divergence that was hypothetical is now shipped, in a third implementation, while the copy the installed lines actually execute remains untouched. The marker that the same documentation instructs leads to deduplicate on is built from the built in hash of the script path, which is salted per process, so three consecutive runs of the same script on the same path produced three different markers and a re-arm can never match or replace its predecessor. The documentation sentence describing that marker asserts it contains the full path, and it contains no path.

A PULL REQUEST WHOSE PRODUCTION CHANGE WAS CORRECT WAS BLOCKED BECAUSE ITS TESTS PROVED NOTHING ABOUT IT. PR 310 fixes the authentication banner exactly as asked. Measured on the real server rather than read, the banner reports off with no registry configured, enforce with a registry and the flag on, and warn with a registry and the flag off, and the adjacent line stops asserting no authentication only in the enforcing case on a network bind. Every one of those is right. The tests attached to it reimplement the same three way branch inside each test body and then assert against the string the test itself just computed, so not one of them reaches the server. Run against unfixed master, where the new banner text occurs zero times in the source, all six pass. They would pass with the feature deleted. Three of the nine test functions are also defined twice in one module, so the earlier three bindings are silently discarded and never run, which is why six were collected from nine definitions. The card required a red demonstration for each blocker and nothing here is capable of being red.

A REVISION FINALLY FOUND SOMETHING ITS OWN REVIEW HAD MISSED, AND IT WAS REPRODUCED BEFORE IT WAS BELIEVED. PR 311 replaces an all or nothing revert with eight per rule reverts and that granularity paid immediately: two already merged validation tests were passing for a reason unrelated to the rule in their own names. The input string used to trigger them is ten characters long, so the maximum items guard rejected it first, and it iterates into characters, so the item type guard rejected it first, and the is a list check under test was never reached. The discriminating control is the same disabled code run against both inputs, and it separates them cleanly: with the rule disabled the original inputs leave both tests green while the corrected non iterable inputs turn both red, and restoring the rule returns both to green. That is a real coverage hole in merged code, invisible to the aggregate revert, found by the granularity the review asked for. Its narrower claim about the row whose status assertion is duplicated at the service layer also holds exactly as stated: disabling the invariant leaves the status at four hundred and moves only the message assertion, at one failed and thirty four passed.

THE MERGED REPORT'S OWN FILE TABLE CREDITED IT WITH THREE OTHER PULL REQUESTS' WORK AND ITS SUITE COUNT CAME FROM A TREE IT WAS NEVER BUILT ON. The body advertises ten files and nine hundred and eighty eight insertions including both gate scripts, both of their test modules and a changelog entry named for an entirely different card, all of which are already merged work belonging to two other pull requests. The actual change against the branch's own merge base is two files and three hundred and eight insertions. The reported suite count is likewise from that foreign tree and differs from this one by exactly the tests those merged pull requests added. This is the same shape as the pull request blocked earlier in the week for advertising an audit that existed nowhere, with the difference that here the deliverable is present and was verified by measurement rather than by reading the table, so the record was corrected in the squash commit message rather than the merge being held. A file table is read as evidence of work, and a stale one is a claim like any other.

THE REVIEWER'S OWN INSTRUMENT PRODUCED A FALSE FINDING AND IT IS RECORDED HERE BECAUSE IT NEARLY WENT INTO A REVIEW. A full suite on one trial merge reported a failure whose traceback wrote to the real user data directory rather than the test's isolated one. It was contention: two suites were running against two worktrees at once and collided on a shared registry path. Serially the test passes on the branch and on clean master alike. A reviewer's flake reported as a defect of the work under review is its own class of false claim, and the correction belongs in the same place as the finding would have.

A CONTROL ONLY DISCRIMINATES WHERE THE RIGHT ANSWER AND THE WRONG ANSWER DISAGREE. Carried from the sibling agent and immediately applicable to this session's own work: a control set that stays silent proves nothing unless silence under the wrong hypothesis would have differed from silence under the right one. The duplicate helper gate was run against the standing known bad branch here and reported clean, which was the correct result and no evidence at all, because that branch's defects are conflict markers and a shrunken export surface rather than duplicate definitions. It did not change either verdict, since neither pull request touched handle normalisation, but the control was not a control. The version that does discriminate is the one used on the merged report above, where the same perturbation is run against both candidate inputs and they disagree.

TWO GATES LANDED AND A THIRD PULL REQUEST WAS BLOCKED FOR REINTRODUCING THE EXACT DEFECT CLASS IT WAS SENT TO REMOVE. The queue that had been declared empty refilled for the fifth time in one day, with three execution pull requests waiting and none of them reviewed. PR 306 and PR 308 are merged and PR 307 is closed under the block policy. The blocked one is the interesting case: it discharged three blockers and two defects cleanly, and while fixing the polymorphic field defect it added an unguarded numeric conversion to the search request path, which is precisely the failure mode the assertion blocker in the same card existed to remove. The assertion was correctly deleted at one line and an unguarded conversion was added thirty seven lines below it. A caller who imports a row through the documented bulk migration path carrying an ordinary date string in its timestamp gets an unhandled conversion error out of search, where master returns the hit. The sibling comparison beside it has the same weakness against any review date that is not a string. The tests never saw either because every one of them builds those fields from well formed literals, so the conversion is only ever handed input it can convert. That is the third green suite in one session to ship a live defect on the request path, and all three share one shape: the tests exercise the branch that already worked.

THE EXPORT GATE MET ITS CARD IN FULL AND THE GAP THAT REMAINS BELONGS TO THE CARD, NOT THE LANE. PR 306 extends the deleted symbols gate to catch a name removed from the export list while its definition survives. All four acceptance items were reproduced independently rather than read: the behavioural red and green pair on real repository input rather than merely the collection error, the false positive control where a name removed from both the list and the file reports the definition violation alone, the clean run against master, and the replay against the preserved regression branch, which named exactly the three symbols the card filed and no others. Classifying the full export delta on that branch showed the remaining two removals are genuine deletions already reported by the definition half, so coverage on the known bad input is complete. The gate is nonetheless blind to a re-export dropped from a package initialiser, because the membership filter requires the definition to live in the same file, which is exactly what the card specified. A census puts that at one hundred and nine of two hundred and sixty five export entries, including seventy two of the seventy four in the package's own top level initialiser, which is the part a consumer actually imports. The card was never widened to cover it despite an intention to do so, so the missing half was filed as its own card rather than held against a pull request that met its brief. Blocking a lane for a requirement that was never written down is how a fleet teaches its lanes to ignore the written card.

THE DUPLICATE HELPER GATE WAS PROVEN AGAINST THE BROKEN VERSION OF ITSELF. PR 308 fixes both blockers from the closed attempt, and each was measured by running the closed pull request's own gate as the red control in the same command on the same input. A legitimate overload pair fails on the old gate with three definitions counted and passes on the new one. A file with genuinely invalid encoding kills the old gate with an unhandled decode traceback and is skipped with a warning by the new one, and the test that covers it uses real invalid bytes rather than a permission change, which was the precise hole that let the defect through the first time. All four required controls were verified live as permanent tests, the wiring from the previous attempt was kept rather than rebuilt, and the two rival lane branches still merge with no conflict and are still caught. Two blind spots were recorded rather than fixed, both outside this card: duplication across two modules passes because the rule is per file, and a file that fails to parse goes silent, so a merge that leaves conflict markers alongside duplicate definitions reports clean.

A MEASUREMENT ARTIFACT ALMOST BECAME A FINDING, AND THE ONLY REASON IT DID NOT IS THAT THE OUTPUT CONTRADICTED ITSELF. A control run reported the gate printing its failure message and exiting zero at the same time, which would have been a serious defect in a gate. The exit status being read was the status of the display command at the end of the pipeline and not the gate's own. Re-running with the real process status showed exit one, correctly. Two probes earlier in the same session failed in the related way: a commit was silently refused by the repository hook, so the check ran against an unmodified tree and reported clean, and a gate executed from outside the tree resolved its scan root from its own file location and therefore scanned the wrong directory entirely, reporting zero definitions of a helper that was sitting in front of it. All three are the same discipline: a clean result is only evidence once the instrument has been shown to be pointed at the thing under test.

THE TASK BOARD RETURNS ITS LIST UNDER A DIFFERENT KEY THAN THE ONE THE OBVIOUS GUESS USES. A sweep for a card returned zero results across the whole board while the card was plainly there, because the collection endpoint returns its rows under an items key and the code was reading a tasks key, yielding an empty list rather than an error. Fetching a single card by identifier works and returns the body directly. The same family as the bus channel parameter diagnosed in the previous session: a silent empty result from a wrong key reads exactly like a true absence, and neither one announces itself.

A SECOND GREEN SUITE IN ONE SESSION SHIPPED LIVE DEFECTS, AND THIS TIME THE BROKEN GUARD WAS THE ONE BUILT TO CATCH THEM. PR 305 wires the duplicate helper gate into continuous integration and passes fourteen hundred and twenty six tests with twelve skipped and none failing on a trial merge against current master, with two defects live on that same tree. The gate fires on a legitimate typing overload pair, counting one logical helper written in the ordinary typed style as three definitions and failing the build, which is the precise case the open cross pull request card names as a control and forbids, for the stated reason that a guard which fires on correct code is switched off within a week. The abstract syntax tree walk cannot distinguish an overload stub from a rival copy because it matches on the node type alone. The import fallback half of that same control already passes and stays silent, so only the overload half is wrong and the fix is to skip any definition whose decorator list names overload.

THE GATE ALSO CRASHES ON THE ONE INPUT ITS OWN DOCSTRING PROMISES IT SURVIVES. The counting function catches only the operating system error class, so a file with invalid encoding raises an unhandled decode error and kills the gate with a traceback, while the docstring directly above states that such files are skipped with a warning rather than allowed to crash. The decode error is a value error and not an operating system error, so the handler cannot reach it. The reason this passed inside the pull request is the more useful half: the only test written for that behaviour makes the file unreadable by removing its permissions, which raises a permission error, which is an operating system error, so the test exercises the branch that already worked and never touches the broken one. This is the same shape as the earlier pull request in this session where all four tests of a helper used the default flag and missed the single branch that was broken. When a test and a docstring disagree about which failure mode is covered, the docstring is the claim and the test is the evidence, and here the test quietly proved a different proposition than the one its name announces.

WHAT THAT PULL REQUEST GOT RIGHT WAS MEASURED FIRST AND THE REVISION CARD PROTECTS IT. The blocker its card actually carried, that the gate was documented but nothing invoked it, is genuinely discharged. The workflow runs the script on every pull request and a non zero exit fails the step, the test module is collected by the existing continuous integration test run, and the red and green halves were both proven with the workflow's exact command rather than by reading it. Better than a synthetic fixture, the gate catches the real defect it was carded for: the two rival lane branches merge with exit zero and no conflict, and the gate then reports two definitions. The revision card names all of this explicitly so the next lane keeps the wiring instead of rebuilding it, because a revision that discards good work is how this fleet has previously spent a lane for nothing.

THE GATE AND AN OPEN CARD ARE THE SAME ARTIFACT, WHICH IS THE DUPLICATE SHAPE ARRIVING AGAIN BEFORE ITS OWN FIX HAS LANDED. The guard half of the cross pull request card filed earlier in this session and the gate delivered by this pull request are one and the same piece of work, discovered only by reading both bodies rather than by matching identifiers. Flagged on the board and on the bus so that when the revision lands the older card narrows to landing the one shared helper, rather than sending a second lane to build a guard that already exists. This is the third instance in one session of a collision that every automated check is structurally unable to see, and it is the case the card creation guard was filed to cover.

EVERY CLEAN VERDICT IN THIS REVIEW WAS PROVED ON A KNOWN BAD INPUT BEFORE IT WAS BELIEVED. The conflict marker scan, the export surface comparison at two hundred and sixty five symbols unchanged, the suite, and the gate run against master all came back clean, and none of that counted until the preserved regression branch was run through the same commands and reproduced both its three docstring markers and its two hundred and sixty five to two hundred and sixty one export regression naming all five lost symbols. An absence is evidence only once the query has been shown capable of producing a presence.

THE BUS READ PATH WAS BROKEN FOR THIS SESSION AND THE FIRST DIAGNOSIS WOULD HAVE BEEN WRONG. Reading the bus returned an authentication failure, which reads immediately as an expired credential. The control settles it: the same token succeeds against the task board, and the failing endpoint returns the identical failure with no token at all, so the credential was never the problem. The message endpoint requires a channel parameter and returns a bad request rather than a failure when given one, and the since parameter is quietly ignored because message identifiers are strings while the sort field is a timestamp, so a request for recent messages silently returns the oldest instead. Diagnosing a missing parameter as a dead credential is the same error class as diagnosing a missing export as a broken tool, which cost four days earlier this month.

A FULLY GREEN SUITE SHIPPED A REVOCATION BYPASS AND A FEATURE THAT RAISES ON ITS ONLY REAL INPUT. PR 304 passed fourteen hundred and five tests with none failing on a trial merge against current master, and two defects were live on that same tree. The service module calls a regular expression function inside the identity helper without ever importing the regular expression module, and the mentions store passes the flag that reaches that branch at all three of its call sites, with the send path calling it on every message that carries a data directory. A plain body sends cleanly and a body containing an at mention raises a name error, which is the input the feature exists to handle. All four tests of that helper call it with the default flag, the single branch that never touches the missing import, and no test anywhere sends an at mention through the send path. The one line import is the easy half and the missing test is the point.

THE SECOND DEFECT IN THAT PULL REQUEST QUIETLY UN-REVOKES A REVOKED PRINCIPAL. The revoked check was changed to normalise its lookup key, but the revoked set is populated raw from the registry feed and is not normalised at parse time, so any published identifier that is not already lowercase and bare stops matching. Measured with the same script against both trees with the resolved module path printed in each: a mixed case identifier that master correctly rejects is accepted on the branch, while an all lowercase control is rejected on both, which is what proves the probe can see a working revocation and isolates the change as the cause. The card asked for normalisation at the identity comparison and did not ask for it on the revocation lookup, so the fix is to revert that line or to normalise the feed at parse time as well, but not one side alone.

A PROBE PLACED IN THE TEMPORARY DIRECTORY TESTED THE WRONG TREE AND PRINTED A FALSE CLEAN. The first run of that revocation probe lived outside the worktree, so the first entry on the module search path was the temporary directory and the editable install resolved the package to the main checkout instead. It reported the branch as correct when the branch is not correct. Re-running from inside each tree with the resolved module file printed produced the real result. This is the standing probe discipline lesson landing on a new surface: a worktree is not tested merely because the interpreter was invoked while standing in one, and the binding has to be printed rather than assumed.

THE OTHER PULL REQUEST DELIVERED NONE OF ITS THREE BLOCKERS AND ADDED A SECURITY REGRESSION. PR 303 carried the earlier bus auth work forward correctly and stopped there. The at spelling still cannot verify because the raw sender is still passed into the authoriser while the authoriser still compares exactly, and adding the single assertion the third blocker asks for turns the branch's own test red while the bare spelling control with the same token passes. A token minted from an untrusted key still publishes its unverified subject into the field named verified and still reports the sender as consistent, so a forgery reads as clean on exactly the rows the next stage exists to scrutinise. The test that hides the first blocker still asserts everything except the authentication result. On top of that the pull request deletes the arm that hard rejected a human principal claiming an agent handle, and because that error class subclasses the general one it now falls through to the warn path and is accepted, which fails two existing tests on a trial merge while both pass on master alone.

BOTH PULL REQUESTS ADD A DIFFERENT COPY OF THE SAME HELPER TO THE SAME FILE AND THEY DO NOT CONFLICT. The card that was supposed to land one shared identity helper is closed and its work never reached master, so each lane wrote its own, which is what both cards forbade. Merged in either order the result is exit zero with no conflict and two definitions of the same function in one module, with the interpreter keeping whichever happens to sit last in the file. Only one of the two accepts the keyword the mentions store passes, so which definition survives is decided by where each lane happened to place it. In the tree measured the superset definition lands last and the call sites survive, which is precisely why nothing fails and why this reaches master unnoticed. Carded with a red proof and two controls, since a duplicate definition is invisible to the suite, to the documentation gate, and to the deleted symbols gate, which is keyed on names disappearing and cannot see one appearing twice.

A CARD FOR WORK THAT HAD ALREADY MERGED WAS STILL OPEN AND CLAIMABLE, AND AN IDENTIFIER KEYED SWEEP COULD NOT SEE IT. The configuration setter card was filed twice, four hundred and thirty one seconds apart, and both were open at once. One was built and merged, the other sat claimable describing work already on master and would have produced a second no operation pull request. Verified before closing it that all ten setters it names already carry the keyword only marker on master, with the merged change covering an eleventh, so the landed work is a strict superset. The first sweep for this class compared every open card identifier against the landed changelog entries and returned no hits across thirty three cards with its positive control passing, and that zero was still not evidence, because the landed work carried the duplicate's identifier and was structurally invisible to an identifier keyed scan. Only a content keyed comparison found it.

SIMILARITY ALONE CANNOT SEPARATE A DUPLICATE FROM A DELIBERATE REFILE, BUT TIMING CAN. The content keyed sweep surfaced six near duplicate pairs and only one was a defect, with two of the false ones matching on title exactly. In every legitimate case the twin was already closed when the new card was created, in two cases within the same instant, which is the signature of the close and refile pattern the board uses on purpose. The one real collision is the only pair where both cards were open simultaneously. Comparing a new card only against cards that are open or claimed right now, and never against closed ones, makes all five refiles non events and still catches the collision. Filed against the card creation tool, which today validates only the shape of a batch's own bodies and never reads the board at all.

THREE GUARDS NOW SHARE ONE SHAPE AND IT IS WORTH NAMING RATHER THAN FIXING PIECEMEAL. The duplicate pull request guard is keyed on card identifier and is blind to a lead's pull request on a branch outside the execution namespace. The deleted symbols gate is keyed on definitions and is blind to a name leaving the export list while its definition survives. The card creation tool is keyed on nothing and is blind to a second card for one defect. Each is correct inside its own alphabet and each collision lives outside it.

THE QUEUE REFILLED WITH TWO PRS AND BOTH ARE NOW DISPOSITIONED, ONE MERGED AND ONE BLOCKED. PR 300 merged as bf76ae6 and PR 301 is blocked, closed and carried by a union card, with both of its relevant branches preserved. Open exec PRs are back to zero and the review queue is empty, measured rather than inherited.

A CONFIG SETTER WAS SILENTLY DELETING THE KEY YOU ASKED IT TO SET, AND THE FIX IS SOUND. PR 300 makes the clear flag keyword only across eleven setters. The defect reproduces on master against an isolated data directory: setting a registry URL and then calling the setter with a path as the second positional argument returns no error at all and leaves the key unset, because the path bound to the clear flag and every non empty string is true. On the merged tree the same call raises a type error and the stored value survives. The master control is what makes this a defect report rather than a note that a signature changed, since without it the type error proves only that the signature moved.

THE RISK IN THAT CHANGE IS WIDER THAN ITS CARD DESCRIBES AND IT WAS MEASURED RATHER THAN ASSUMED. The keyword only marker also makes the data directory argument keyword only, and one of these setters is public API exported from the package root, so any caller passing a data directory as a third positional argument now raises. Searching every call site across this repository and the three external consumers found that all of them already pass both arguments by keyword, so the break is real in principle and empty in practice. Completeness was checked with an abstract syntax tree walk rather than by eye, because a false completeness claim is what the previous session blocked another PR for: fifteen setters take a data directory, the eleven with a clear flag all changed, and the four without one are all correctly left untouched. The description names three of those four, so the code is right and the prose is incomplete.

A SUITE THAT EXITS NON ZERO BECAUSE THE INTERPRETER HAS NO TEST RUNNER IS A RED FOR THE WRONG REASON. The first full run against the trial merge failed for exactly that reason and would have read as a broken branch. The real figure, from the virtual environment and after confirming with a signature inspection that the worktree shadows the editable install, is fourteen hundred and one passed with twelve skipped and none failed. All three of the checks this repository proved load bearing were clean on the tree that would actually land, each with its control in the same command: the conflict marker scan against a planted marker file, and the export surface diff against a deliberately planted ghost export that the tool correctly reported.

TWO PULL REQUESTS EXISTED FOR ONE CARD AND NEITHER SIDE HAD ALL THE WORK. Card tsk-qrp3sz produced PR 296 in the morning, written by the lead, and PR 301 from a lane seven hours later, both rewriting the same cron prompt file wholesale so neither can land beside the other. The measurement that settles the choice is the cost the card exists to reduce: the always read file is twenty four thousand and eleven bytes on master, fourteen thousand five hundred and ninety six under 296 which is a thirty nine percent cut that depends on nothing, and twenty six thousand eight hundred and fourteen under 301, which is an increase. The saving 301 claims only materialises for a reader that honours an instruction to read only its index, and the consumer here is an agent whose default read is the whole file. Three of its nine index anchors also fail to resolve because those headings carry a trailing parenthetical the links omit, while the other six resolve exactly, which is what identifies the links rather than the slug rule as wrong.

THE RESOLUTION IS A UNION BECAUSE EACH BRANCH HOLDS SOMETHING THE OTHER LACKS. The lane's zero loss claim is true and was verified by set diffing every code span, path, URL, script name, measured quantity, bus identifier and date out of the original, with a control token to prove the diff can fire. More importantly it is the only side that covers part one of the card, the bus thread index result that replaces raw body reads, which the lead's PR does not touch at all. Discarding that branch would bin the half of the card nobody else has done, so card tsk-nzh7fa specifies branching from the lead's structure, porting the lane's step four content into it, fixing the anchors, and accepting only on a file strictly smaller than the original together with a set diff that carries its own positive control.

THE DUPLICATE GUARD DID NOT FAIL HERE, AND THE FIRST WRITTEN EXPLANATION OF WHY IT SHOULD HAVE WAS WRONG. The initial review comment placed this alongside the outage driven fails open in the same guard, on the reasoning that the guard searches PR bodies and the lead's PR body names the card. Running the guard's own predicate instead of reasoning about it showed the opposite: the predicate is silent and correct, because it keeps a PR only when the branch name matches the card's execution branch or the title contains the card identifier, and the lead's PR satisfies neither. That branch discriminator is the same fix that removed twelve false stops, so it is working as specified. The real and narrower shape is that a lead's own PR on a non execution branch is invisible to the duplicate guard even when its body names the card, and the remedy is on the lead's side rather than in the tooling: put the card identifier in the PR title. A correction was posted on the PR and to the bus so the tooling owner does not chase a defect that is not theirs.

A BUS MESSAGE FROM THE OTHER LEAD ARRIVED AS THE LITERAL TEXT OF A COMMAND LINE FLAG AND ITS CONTENT IS LOST. Message 3057 is a file flag followed by a path rather than the file's contents, so a post from a file invocation put the argument on the wire instead of the message. It is worth knowing because the sender has no way to see it from their side and will believe the message landed.

OPEN EXEC PRS WERE ZERO EARLIER IN THIS SESSION TOO. All four remaining were reviewed and dispositioned this window: 298, 297, 255 and 274 are each blocked, closed, and carried by a revision card, with all four exec branches deliberately preserved so no lane rebuilds work that already exists. The revision cards are tsk-xe3h5v for 298, tsk-2umdob for 297, tsk-vw6gq7 for 255 and tsk-hjm3vg for 274, and each originating card was closed in the same action naming its carrier. Verified after the fact rather than assumed: zero open exec PRs, all four originating cards closed, all four revision cards open and satisfying the real claimable predicate, all four branches still on the remote.

FOUR GREEN CHECKS AND FOURTEEN HUNDRED PASSING TESTS HID A CONFLICT MARKER IN SHIPPED SOURCE, AND THAT IS THE FINDING OF THE SESSION. PR 298 carried unresolved merge markers at lines 108, 113 and 118 of the http server module, left there by its carry forward squash. They sit inside the module docstring, so they are string content: the parser accepts them, every test passes, and all four checks including the deleted symbols gate went green. A scan of the head found them and the same scan against master found nothing, so the pattern was proven to work before the result was believed. The general form is that a test suite cannot see anything inside a string literal, so a green suite is not evidence about docstrings, comments or any other inert text, and the only check that reaches them is a grep run deliberately.

THE SAME BAD MERGE ALSO DROPPED THREE PUBLIC NAMES FROM THE EXPORT SURFACE, AND THE GATE THAT EXISTS FOR THAT DID NOT FIRE. On 298 the service module's export list went from thirty nine entries to thirty seven, losing three functions that are still defined in the file, so a star import silently stops providing them. Measured with an abstract syntax tree walk rather than a grep, because the first grep pattern returned zero on master as well and was therefore not evidence at all. The deleted symbols gate passed, because it covers a removed definition and not a removed export while the definition survives, which is exactly the shape a mechanical merge resolution produces. Reported so the gate's owner can decide whether to widen it.

RESOLVING A CONFLICT BY PICKING A SIDE LOSES DOCUMENTED LIVE BEHAVIOUR, SO THE INSTRUCTION HAS TO SAY UNION. Both sides of 298's marker carried rows the other dropped. One side documented two thread endpoints whose handlers are still routed and the epoch validation notes for two more, where the validating constant still exists in the file; the other side documented the new import endpoint. Deleting either side would have left the published endpoint table describing a system that is not the one running. The revision card spells out which rows come from which side rather than saying remove the markers, because remove the markers is the instruction that produces the silent loss.

AN APPROVAL FROM THREE DAYS AGO IS NOT A MERGE DECISION TODAY, AND THIS ONE WAS RETRACTED. PR 255 was approved on 14 August against a head that has not moved since, with all five checks green. Trial merged into current master, its own test fails: the remote routes gate test hard codes the number of interpolated call sites in the live repository, there were three when the branch was cut and there are four now, because the A2A chat read work added one afterwards. The control that makes this diagnosis rather than a guess is that the same single test passes on the branch alone and only fails once master is in the tree. Those five green checks ran on 14 August, so the green was stale evidence, and running the suite against the tree that will actually land is what caught it. The card explicitly forbids bumping three to four, because that number encodes no invariant and would rot again on the next interpolated route; the invariant worth asserting is the one on the line above it.

A TEST THAT PASSES ONLY BECAUSE THE ENVIRONMENT IS MISSING SOMETHING IS THE SAME DEFECT THE CHANGE WAS FIXING. PR 297 corrects a benchmark that exited zero when its model was absent, and its new test calls the real availability check with nothing mocked. That helper swallows every exception and reports absent, so the test passes on continuous integration purely because no model server is running there. Monkeypatching the check to report the model present makes the shipped test body fail, with the positive control passing in the same run, so on any machine that actually serves the model the test fails, and on the bench host it would run a real benchmark with model loading and network calls inside the unit suite. The production one liner is correct and its red witness is clean, so the branch is preserved and only the test is pinned. Its scope claim was also wrong: two more sites of the same class remain in the event question runner, both exiting zero after printing an error, with no exit code plumbing in its entry point, so the issue it claims to close stays open.

EVERY PROBE THIS SESSION CARRIED ITS CONTROL IN THE SAME COMMAND, AND THREE OF THEM WOULD HAVE MISLED WITHOUT IT. The export list grep returned zero on both trees and was discarded rather than reported. The red witness for the benchmark fix ran an abstract syntax tree parse first, so a failure could not be a syntax error wearing the right costume. The 255 diagnosis rested entirely on the branch alone control passing. This is the standing lesson from previous sessions applied rather than restated, and it changed the answer each time.

GITHUB'S GRAPHQL LAYER 503ED THROUGHOUT AND ITS REST LAYER MOSTLY DID NOT, WITH ONE EXCEPTION WORTH RECORDING. The convenience commands for closing a pull request and posting a comment are GraphQL and failed repeatedly; each failure was verified against a fresh read rather than assumed either way, and in every case the action had genuinely not happened. Once, REST itself 503ed on a close, the verify showed the pull request still open, and a bounded retry closed it on the second attempt. Verifying after every write rather than trusting the return is what kept the record straight, and it is the same discipline that caught a merge that had not merged earlier in the day.

OPEN EXEC PRS WERE DOWN TO FOUR AND THE BOARD HAD FOUR SLOTS OF HEADROOM. Merged this session: 299 as 9d4fe4e, 244 as c15ec4b and 258 as cf5b2d0. Remaining open are 255, 274, 297 and 298. All three merged branches are deleted and all of their originating cards are closed.

MERGING WITHOUT KILO WAS A DECISION AND IT IS WRITTEN DOWN ON BOTH PRS RATHER THAN LEFT TO BE INFERRED. Kilo Code Review hung on every run started after 16:58Z, at two hours fifty on 297 and thirty six minutes on 244, against a same day completed band of eight to ten minutes, so a permanent hang was a proven state and not a slow one. The honest half of the decision is that this repository has NO required status checks at all, which was measured rather than assumed: required_status_checks is absent from the branch protection object and rulesets returns empty. Kilo has always been advisory here, so the bar being applied was mine and the choice was to meet it a different way rather than to skip a gate. What stood in its place for 244 was a trial merge into current master with the full suite on the exact tree that would land, at one thousand three hundred and ninety seven passed and zero failed, plus a diff of thirty two insertions and zero deletions, that last being the independent check that no existing behaviour moved on a file in the auth path.

THE UN-EXCLUSION HAZARD PAID OUT AGAIN, THIS TIME BEHIND A MERGED PR, AND THE EXECUTOR'S OWN GUARD IS WHAT CONTAINED IT. Card tsk-xhm3ln was still open and claimable behind MERGED PR 256. A lane claimed it at 17:17, ran a paid model for twenty six minutes, rebuilt the entire revision and committed it, and was then stopped at the push by the one PR per task guard, which refused to open a duplicate. Twenty six minutes of lane time burned and zero corruption. The sweep that followed found three more cards in that state behind merged PRs and all are now closed, while tsk-uyznqh was deliberately left open because PR 298 still holds its exclusion.

THAT GUARD FAILS OPEN ON A GITHUB OUTAGE, WHICH IS THE THIRD FAILS OPEN IN THE SAME GUARD. Its query ends in a redirect of standard error to the null device, and the underlying call is GraphQL, which has been returning 503 intermittently all session. The same query run three times in a row returned the PR number, then a 503 with an empty result, then the PR number again, so roughly one run in three during today's flakiness cannot tell an outage from an absent PR and would let a duplicate through. The script's own comment block already documents two previous fails open in this exact guard. It is environmental, so no code review would ever surface it, and it has been reported to the lead who owns the tooling at bus 3046 with the suggested shape of failing closed on a non zero return rather than on an empty result.

THE POSITIVE CONTROL FAILING FOR THE WRONG REASON WAS AGAIN THE ACTUAL FINDING. The first probe of that guard carried the same swallowed standard error, so a 503 came back looking like a silent guard, and the conclusion one step away was that the guard does not protect merged PRs at all. It does. Once the outage was retried out, the real shape is that it fires for OPEN and MERGED PRs and is silent for CLOSED ones by design, which means close on block is precisely the case where nothing stops a rebuild, and closing or refiling the card in the same action is what carries the safety.

THE THROTTLE IS RELEASED. Open exec PRs went nine to seven against a cap of eight, so cards dispatch again. Confirmed from the dispatcher's own log rather than by arithmetic: the last throttled line is 16:12:38 and the four cycles after it printed none. AND THE ENTRY BELOW IS WRONG ABOUT WHAT IT TAKES. One merge does not unlock a board sitting at nine. The predicate is greater-than-or-equal, not greater-than (dispatch_loop.sh line 259, and its own log line reads throttled followed by the cap in a greater-than-or-equal form), so eight against a cap of eight is still throttled and you have to get BELOW the cap. That correction was inherited wrong from the previous checkpoint and would have left the board locked while reporting it open. Note also that the effective cap is not always eight: throttle_check.py, the Observatory dial, can override it lower for an iteration, and it returns empty at present.

This hour merged 256 (c2a3e6a, the 236 revision) and blocked-and-closed 260 under the close-on-block policy with revision card tsk-dvfhq3. Closing a blocked PR frees a throttle slot exactly as a merge does, which is the honest outcome when a PR should not ship. Remaining open exec PRs are 235, 244, 255, 258, 274, 297 and 298; the best value left is rebasing 244 onto 235's branch first, then reviewing 235, which is two merges from one review pass.

PR 260 IS THE FINDING WORTH CARRYING. It was revising the documentation section that teaches probe discipline, it fixed the original absence-as-proof defect correctly, and then its own NEGATIVE control could not fail: the procedure starts the watcher with a bounded count so it exits once it fires, fires it with the positive control, and then asks the reader to observe no trigger run against a process that had already exited. Measured live rather than reasoned about. As written, the positive control woke it and the process died, then the negative control observed silence from a dead process. Reordered so the negative control runs first while the watcher is still armed, the silence means something because the same process demonstrably fires one message later. The fix is a pure swap and the swap was tested before it was recommended. The drift-guard test shipped alongside asserted only that the phrases positive control and negative control were PRESENT, not that the procedure was SOUND, so it passed on the broken ordering and would have locked it in. That is the fifth sighting on this repo of a check whose PASS is reachable from zero data.

Open exec PRs on this repo went from thirty two to nine against a cap of eight earlier in the day. Two merged, 290 and 287, both approved in review with all checks green. Twenty one closed, each carrying a comment that says why. The previous entry's line that the queue is empty was true of REVIEW work and was read for two days as though it meant there was nothing to do, which is how thirty two finished-and-judged PRs accumulated while every freshness run reported zero unreviewed. Reviewed is not the same as dispositioned, and only one of those two numbers was ever on screen.

CLOSE ON BLOCK IS NOW POLICY, RULED BY JAY THIS SESSION, AND THE MEASUREMENT CAME FIRST. A blocked PR in this repo is never revised in place. Every revision so far has been a new PR branched off master that re-does the original's full file set, which was verified on seven pairs by comparing changed file sets rather than by reading titles: 255 carries 249 plus two test lines, 256 carries 236 plus five documentation lines, and 289 is a strict superset of 284 in every shared file. So a blocked PR holds a throttle slot from the moment it is blocked and can never use it, and seven of the thirty two slots were held by PRs whose replacements were already open. The policy is that blocking a PR closes it in the same action and the revision is carried by a card.

THE ORDER OF OPERATIONS IS THE LOAD BEARING PART AND IT NEARLY WENT WRONG. Closing a PR makes its card claimable again, because next_card.py excludes any card whose exec branch has an open PR, so a lane re-dispatches it from master and rebuilds the same defects the review just rejected. All twelve originating cards were still open and claimable at that moment. The sequence per PR is therefore to file the revision card, then close the originating card, then close the PR, and ten new revision cards were filed that way. What makes the closes lossless was checked rather than assumed: review comments survive closure, so each card links back to the full review, and closing a PR does not delete its branch, so every card carries the fetch command for the previous attempt and all fifty exec branches are still present.

A ONE PR ARITHMETIC DISCREPANCY IS WORTH CHASING, AND THIS ONE PAID FOR ITSELF IMMEDIATELY. Thirty two less two merged less seven closed should have left twenty three and the live count said twenty two. The missing row was PR 289, closed by nobody's decision: its lane finished a run, force replaced its remote branch, and GitHub auto-closes any PR whose head branch is deleted, so the close and the head_ref_deleted event share a timestamp to the second. One second later the lane's own guard read that PR 289 already existed and refused to open a replacement, then released the card. The lane's log line called the branch orphaned with no PR attached, and that false belief is what made deleting it look safe. This matters beyond the one PR because 230 and 284 had just been closed pointing at 289 as their live successor, so a silent close would have made those closing comments false; corrections are posted on both, and the work self healed as PR 298. Filed as tsk-jsqrq2. A silent close of a reviewed PR is indistinguishable from a deliberate one afterwards, because every fleet process authenticates as the same GitHub identity and the timeline can only ever name that identity.

TWO SIGHTINGS OF THE ZERO DATA GATE IN ONE HOUR, ONE OF THEM FOUND INDEPENDENTLY BY THE SIBLING LEAD. fleet_health.sh skips a repo silently when its query fails, substituting a question mark that the following test filters out, and the script's own documented contract is that silence means healthy. With all five repos failing, no problem line is emitted, the problem counter stays at zero, and the summary prints FLEET-HEALTH OK, so a GitHub outage reads as a healthy fleet; the same summary line also reports the last loop iteration's count as though it were the fleet number. Filed as tsk-r5dgyb and raised to the sibling lead whose loop it is. Independently, at bus 3026, that lead reported reconcile_claims.py reading a failed call as no PR and releasing claims whose PR was in review. The family now has four sightings in two days and the general form holds in both directions: a gate whose pass is reachable from zero data is not a gate, and a gate whose fail is reachable from zero data manufactures work.

THE PREVIOUS CHECKPOINT'S HEADLINE DIAGNOSIS WAS WRONG AND IS WITHDRAWN. It recorded that fleet_health.sh reported a different population depending on how it was invoked, and filed that under the count against its consumers class. It is not that. Commit 14c1c53 landed between the two runs and widened the script from one hardcoded repository to a loop over five, so the two readings are the same script at two versions. Settled with git log on the file. Relatedly, that checkpoint's timestamps were an hour ahead of the clock, and local time on this box is UTC, so relative times in the handoff file are worth checking against date before they are trusted.

MERGE ORDER IS A REAL BLOCKER AND IS NOT THE SAME AS A CONTENT BLOCKER. PR 244's revocation fix is correct and proven to discriminate, but 244 is cut from master as a parallel reimplementation of 235, and in either merge order the docstring describing the fix conflicts while the guard that undoes it auto-merges silently. It needs rebasing onto 235's branch first, which makes reviewing 235 the best value left on the board: one review pass yields two merges. Separately PR 258 complied with its blocker exactly, dating the revision log row to the day it was written, and has since gone conflicting with a four day stale date. That drift belongs to the locked queue and not to the lane, so it should be rebased rather than sent round again for a date.

THE QUEUE IS EMPTY AND HAS BEEN RE-VERIFIED LIVE, so a waking session should not start by hunting for review work. Thirty three open PRs, thirty three reviewed, zero unreviewed, with the denominator control passing and the loop confirmed to have run once per open PR. The thirty fourth, 296, is mine and is the only one carrying no review, which is correct rather than an omission. Of the batch reviewed today, 290 is APPROVED, while 289, 291, 293, 294 and 295 are BLOCKED with their blockers carded. The bus is at floor 3010 with nothing addressed to us outstanding.

TOKEN DISCIPLINE IS NOW A RULING RATHER THAN A PREFERENCE, and it landed in the same hour it was broken. Jay's directive at bus 2995 puts the terse register on all inter-agent traffic, with full prose reserved for Jay facing text, public GitHub and docs, and it restricts wakes to mentions, alarms and stream health. The eater was diagnosed and it was not what anyone assumed: not backlog replay, not checkpoint re-derivation, but three PR reviews in one window, each a full source read plus four to six probe rounds re-billing the whole context. Two of the cheaper causes were carded and both are now closed out. The first was that identifying a bus thread had no metadata only affordance, so the only route was a raw five hundred message page with bodies printed. The figure carried in the checkpoint for that read was roughly eight thousand tokens, and it was inherited rather than measured, so it was re-measured in the retrospective and it is wrong by more than an order of magnitude: the page is NINE HUNDRED AND FIFTEEN THOUSAND NINE HUNDRED AND SEVENTY FIVE BYTES, which at any plausible tokenisation exceeds an entire context window rather than costing a slice of one. The affordance is therefore not a saving, it is the difference between a lookup and a destroyed session. The second was that the canonical hourly monitor prompt was a single paragraph of twenty three thousand four hundred and sixty six characters, re-read in full on every fire.

BOTH ARE FIXED AND THE SECOND IS THE MORE INSTRUCTIVE. The bus tool gained a metadata only thread index that prints name, count, id range and last sender per thread, emits no body at all, and deliberately does not advance the watermark, because a lookup must never mark messages handled the way a drain does. It returns five hundred and thirty eight bytes across five threads and five hundred messages. The monitor prompt was split so that the file keeps its name, which means the cron, STATUS and the handoff docs all still resolve and the cron itself needed no change, with every rule now carried in imperative form under nine step headings and the original paragraph preserved verbatim beside it as an evidence archive. That is PR 296.

THE SPLIT WAS CHECKED FOR LOST RULES MECHANICALLY RATHER THAN BY READING, and the check earned its place immediately. Every path, script name, URL, field name, numeric threshold and command token was extracted from the original and diffed against the rewritten file. Five were absent on the first pass. Three were prose only measurements that belong in the archive, but two were real losses, the CronCreate tool name and the qualified path of the usage publisher, and both are restored. Reading the file over and judging it complete would have shipped both, silently, into the document that governs every subsequent session. One deviation is deliberate and flagged in the PR rather than buried: the original told sessions to pass a durable flag that is a documented no-op and that the same paragraph then contradicts, so the rewritten file states the flag does nothing instead of carrying the contradiction forward.

THE TWO LONG PARKED QUESTIONS ARE ANSWERED BY JAY DIRECTLY AND MUST NOT BE RE-ASKED. Staging in the shared fleet directory is settled: never add everything, explicit paths only, carded as `tsk-jouq63` with a pre-commit guard specified red first, because the existing hook gates tooling by lead status rather than by authorship and would wave another lead's document straight through. The evidence arrived within the hour of the ruling, when a routine commit found five modified files of which three belonged to other writers or other units of work. The adversarial thread with the third lead is capped, carded as `tsk-n4w5me`, with the cap to be posted naming its seven for seven hit rate so it does not read as a quality judgement, and with the watch item recorded that the thread was catching invalid probes, so degraded probe validity afterwards is the signal to reopen it. NEITHER IS EXECUTED. An interactive prompt answer authorises carding and never executing, which is why both are cards rather than actions.

THE DAMAGED SHARED CONFIG IS STILL DAMAGED IN ONE FIELD. The registry token was lost a second time to the same footgun, where binding a temporary data directory does not sandbox the setter because the path resolves to the default directory for both read and write, and it was restored byte exact from the value this project's own handoff file had recorded, which is a restore and not a guess. The registry url remains missing and unrecoverable, the holding is released, and it is confirmed not to belong to the sibling lead, so the remaining owners to ask are the taosc and website seats. The footgun itself is carded across all ten setters with the proof required to set the data directory through the environment.

THE UNIT OF A CHANGE IS NOT THE UNIT OF ITS EFFECT, AND BOTH LEADS SIZED THE SAME CHANGE WRONG BY IGNORING THAT. A lane PR had cut `RETRY_LEAD_SECONDS` in the resume helper from 900 to 60, and it was broadcast to the fleet as a fifteen times reduction in a live safety constant. Fifteen times is correct about the constant and wrong about the system. `retry_after()` does not place the retry at the primary's fire plus the lead; it walks forward to the next watcher tick whose fire clears the lead, so the tick lattice quantises the result. Measured against the production spec, every lead from 0 to 600 seconds yields an identical 600 second gap, and there is exactly one boundary in the whole range, at 601. The change therefore moved the armed gap from 1200 seconds to 600, which is two times and not fifteen, and the worst observed wake to delete latency of 49.729 seconds sits 12.1 times inside the gap the cut actually produces rather than on a ten second margin. The peer lead reproduced the table independently and withdrew the original framing.

THE DISPROOF HAD BEEN SITTING GREEN IN THE SUITE FOR A WHOLE SESSION AND BOTH OF US READ PAST IT. The degenerate cases written in an earlier round assert that a lead of zero and a lead of minus 900 both land the retry in the same place, which is lattice dominance stated outright. Two readers ran that suite, saw those lines pass, and each spent a round arguing about the wrong quantity anyway. A finding can be passing in your own test output and still never reach you, and that is worse than an absent test, because the output looks like diligence.

THE DEFECT THAT SURVIVED WAS MANUFACTURED BY THE FIX. The revert restored the test file but left the source carrying a comment that cited a RED witness inside it, and the tokens that witness was built from had zero occurrences in the reverted file while the citation still stood, with the suite returning zero the entire time. So a partial revert can produce the exact defect class the revert was performed to remove: the original test asserted nothing because its two checks were tautologies, and the half revert replaced a justification that asserted nothing with one that was not there at all. Neither state fails a gate.

THE GATE PROPOSED FOR IT WAS WRONG IN BOTH DIRECTIONS AT ONCE, AND THE CORRECTION IS THE MORE USEFUL HALF. Asserting that a comment's cited test file resolves cannot fail on the case that motivated it, because the file existed throughout and only its contents changed, and it simultaneously fires on ordinary prose that mentions a filename while describing its removal. The assertion has to name a grep-able token inside the file, not the path. The general form is that the assertion must be written at the granularity of the evidence that found the defect; the token level counts were already on screen when the path level check was proposed, and dropping one level is invisible because the check still names the right file, the right layer and the right concern. Carded as `tsk-s2keyh` with an explicit witness marker and four acceptance blocks, the load bearing one being a red that deletes only the token while leaving the file present.

A THIRD WRITER IN THE SHARED FLEET DIRECTORY IS NOW CONFIRMED BY TWO LEADS AND ATTRIBUTED BY NEITHER. The peer's revert was whole and verified, and a later write re-applied the constant while dropping the fabricated comment. The standing posture, agreed between us, is that a further reappearance is a measurement of the writer rather than of the value, to be reported and not re-reverted. The helper's current verified state is the constant restored to 900, the dangling citation removed, and the suite passing.

QUEUE AND CADENCE. Twenty six open PRs with four unreviewed, numbers 284 through 287, all four carrying bot reviews that need triage as part of the review. Note the pair 284 and 286 conflict by construction: 284 executes card `tsk-sgoilz` while 286 asks to close and refile that same card because its body lacks the blocker its predecessor was closed on. Master unchanged at 9be5fd8, nothing merged this window, tree clean. Fleet directive adopted this hour: leads orchestrate and delegate, one to one traffic moves to direct session messages rather than bus broadcasts, and hands on implementation that a card could carry gets carded instead.

THE RESUME PAIR'S RETRY WAS STILL A CONSTANT, AND A CONSTANT NEXT TO A DERIVED VALUE IS WORSE THAN TWO CONSTANTS. The primary arm time was made a function of the usage watcher's real cron spec earlier today, while the retry stayed a flat resets_at plus 22 minutes. Card `tsk-fd3kes` had recorded that asymmetry as a known limit rather than a defect. It is a defect, and it was RED-tested before being fixed: under a `*/30` watcher the derived primary moves to 08:31 while the flat retry sits at 08:21, so the retry fires FIRST. The safety net becomes the primary, at a time derived from nothing, which is the original staleness bug rebuilt. The reason it survives review is that nothing looks wrong: the primary sitting next to it is visibly derived. The positive control ran in the same command and the real 10 minute crontab ordered correctly, so the inversion is a property of the widened cadence and not of the harness.

Both cron lines now come from ONE invocation and therefore one crontab read, because deriving them separately would rest the pair's ordering on the crontab not changing in between, which is an unnamed dependency of exactly the kind the derivation was written to remove. An inverted pair is refused outright rather than emitted. The ceil to the minute rule is now shared by both halves so they cannot round differently, which would otherwise be two individually correct definitions that no gate we run could see diverge. `RETRY_LEAD_SECONDS` states what it is a function of, the primary's fire to delete latency, and is labelled UNMEASURED in the source rather than dressed as a derivation; it is anchored to the smallest primary to retry gap observed sufficient in production and set above it, with the procedure to measure it written down. Prompt sites updated in commit 59eaceb, and card `tsk-fd3kes` closed in the same breath.

A FIFTH ROUND OF ADVERSARIAL REVIEW FOUND THREE MORE DEFECTS IN THE SAME FILE, all reproduced before fixing. First, minute specs that the watcher regex ADMITS but the parser never modelled fell through to the message "the crontab WAS read, but it contains no watch.sh line", which is false, because the line is present and matched; `*/0`, `-`, `,,` and `1--2` were bare tracebacks with no diagnosis. Enumerating the regex's language rather than my own examples then found two more that nobody had listed: `99` and `6-99` were silently ACCEPTED and produced tick lists containing minutes that can never fire. Second, the emitted cron line is a lossy representation of the instant it was derived from. Measured across the 2026-11-01 fall back with the zone set to America/New_York: derived fire 06:07:00Z, emitted line `7 1 1 11`, which denotes 05:07:00Z. Sixty minutes early, that is, before the tick it was derived from, which is the staleness bug with a correct derivation sitting upstream of it. It is now round trip checked by rebuilding the instant from the four fields actually printed and refusing on mismatch, which models nothing about daylight saving. Third, the margin argument was an unguarded `int()`, so a non numeric value was a traceback and a negative value was caught only by the fire versus tick assert, telling the operator the computed fire was wrong when what happened is that their input was.

THE RULE GAINED ITS SECOND HALF, and this is the part worth carrying off this page. Enumerate the surface the code exposes, AND enumerate each element to its DEGENERATE value rather than its plausible one. The margin set used previously was 0, 1, 30, 59, 60, 61 and 600, and the single degenerate member found the single bug; the minute spec set was four specs the parser models and none that it merely admits. The same enumeration generated both the tests and the error handling, so the code was blind and the suite was blind in the same shape, and cross checking one against the other cannot find it. The proof is that an existing test had to change: the margin zero case asserted the old wrong reason, so it passed while pinning it. A suite drawn from the same generator as the code does not merely miss the gap, it certifies it. Degenerate values are mechanical too: for an integer argument they are zero, negative and non numeric, and for a regex admitted field they are the strings the regex accepts and the parser does not.

A CORRECTION AGAINST MY OWN FIX, recorded because it was found by writing the test rather than by design. The round trip guard's docstring claimed it catches the daylight saving fold, the spring forward gap, and any dropped field. The gap is NOT reachable from this direction and is not covered, because converting an instant to local time always yields a wall clock that exists, so no derived instant can land in the skipped hour. The source is corrected and the test records the gap as unreachable rather than as covered. A guard advertising coverage it does not have is the same defect as a gate whose pass is reachable from zero data. Separately, the claim that a yearless cron line re-fires next year is true of the five fields but not of the wake, since the one shot semantics live at the scheduler layer rather than in the cron line; the tool now prints that dependency explicitly instead of implying the representation is complete.

The helper's suite went from 53 to 163 assertions and all pass. The bench host, node linstation, was re-probed and is still down with 100 percent ping loss and an ssh TCP timeout, this time with a positive control: the same probe shape against the Pi returned its hostname and exit zero, so it is not a route problem on this end. No benchmark work can run and E-030 remains blocked on three preconditions regardless.

QUEUE AND BUS BOTH CLEAN ON RESUME, so this entry is about the one thing that was not. Freshness predicate re-run live: 21 open, 21 reviewed, 0 unreviewed, controls passing and `SOURCE: LIVE gh pr list` on the run that produced the number. Bus swept and the returned range was 2743 to 2772 against a floor of 2772, so nothing arrived since the checkpoint and nothing is owed. All four crons survived the context clear and none were recreated.

FORK FRESHNESS DISCHARGED, the item the checkpoint recorded as owed after it was skipped under the hard stop. All three numbers re-measured rather than quoted from the 24 hour old note, and all three are unchanged: rknn-llm at release-v1.3.0, qmd latest tag still v2.5.3 so the rebase stays held, and rkllama `jaylfc/main` diverged from `notpunchnox/main` at ahead_by=9 behind_by=13, still deliberately not actioned.

E-030 COULD NOT HAVE STARTED EVEN WITH THE DATASET IN HAND, AND THAT IS THE FINDING. With the band lifted and the queue empty, the highest value open item was E-030, pre-registered on 2026-07-21 and never run. It pre-registers two blocking preconditions, the second being to obtain LongMemEval-S and verify it against the sha256 pinned in `benchmarks/data/README.md`. Doing exactly that does not unblock it. The instrument E-030 names, `benchmarks/longmemeval_runner.py`, does not load the pinned file at all. It loads `benchmarks/data/longmemeval_oracle.json`, hardcoded at line 30 and read only at line 273, with no CLI flag and no environment override, and that filename has no identity block, no sha256, no question count and no documented way to obtain it.

A census over the 13 dataset loaders in `benchmarks/` found three distinct filenames in use and exactly one of them pinned: `longmemeval_s_full.json` in 9 loaders, `longmemeval_oracle.json` in 3 including this instrument, and `longmemeval_s_cleaned.json` in 1, the last of which is referenced nowhere else in the repository.

THE GENERALISABLE HALF IS THE SENTENCE, NOT THE FILE. That README exists so that, in its own words, a missing file never means a lost or ambiguous dataset, and it says "Used by: benchmarks/longmemeval_enhanced.py and the other longmemeval_* runners". That claim is false for 4 of the 13 loaders, and it is precisely why the gap survived this long: a scope claim written as an et cetera reads as blanket coverage, so nobody counts it. This is the same shape as Friday's cached denominator, where the ritual was satisfied and the artefact still lied, and it fails in the quiet direction again by implying full coverage. The rule worth carrying is to count the population against its consumers rather than trusting the sentence that claims to cover them.

Second order, and worth as much: `docs/superpowers/plans/2026-06-14-e012-score-up-ablation.md:197` already hit this exact wall in June and wrote a workaround, "on a machine without it, skip this step". A documented skip is where a blocker goes to become invisible.

The discriminator was run before any of this was recorded. Nothing in the repository creates, downloads, symlinks or aliases the oracle file, and the positive control for that search is that the pinned name is found in two docs, so the empty result for the other two names is a real absence rather than a broken grep.

ACTIONS. Card `tsk-yssnei` filed on prj-qigv7w, claimable and `@any`, requiring identity blocks for both unpinned files, a corrected "Used by" sentence, an overridable dataset path in the three unpinned loaders, and a gate that fails when a loader references a filename with no identity block, with the RED direction required on that gate. Research report taken to v1.61 in commit 79726f1: a third precondition added to E-030 and the second one corrected in place. No arm was run and no threshold, gate, arm definition or kill criterion was touched, so E-030 remains pre-registered and not run. Posted at bus 2773 and read back token by token, 8 content probes and a negative control, clean at 2919 bytes.

THE BENCH HOST IS DOWN. Fedora, node linstation, is unreachable from lead-linux: ssh times out at the TCP level and ping returns 100 percent loss. Two independent signatures, so this is not the August 14 case where curl was dead while ssh worked fine and the service looked down from the wrong side. No arm of E-030 could have run today regardless of the preconditions. Asked @taOS-dev on the bus to say whether they can reach it, which would make it a route problem on this end rather than a host outage.

Last updated: 2026-08-16 02:4x UTC (@taOSmd-dev, primary resume wake; 5h 13%, 7d 1% - BAND LIFTED, working normally).

THE WEEKEND HARD STOP IS OVER. The 7d allowance went 99.0 to 0.0 at the 02:00Z reset. Two of my own predictions were falsified and both are worth carrying. First, the weekly allowance ZEROES at reset, it does not shed a day, so my warning that we would wake into the 90-97 orchestration band was wrong; plan weekly resets as hard bucket resets. Second, the stale-file trap fired a FOURTH time and it fired on the reset read itself, which is the worst possible moment: at 02:02:09Z the file read `seven_day 99.0` with a `resets_at` nine seconds in the past, and the live re-fetch said 0.0. Trusting the file would have cost another silent hour. The generalisable part is that the dangerous moment is when you EXPECT good news, because that is exactly when a stale number gets waved through.

THE FRESHNESS PREDICATE REPORTED A CLEAN QUEUE WHILE IT WAS BLIND, AND THIS IS THE FINDING OF THE SESSION. It printed three passing controls, `DENOMINATOR: open PRs fetched = 19`, and `unreviewed=0`. The true open count was 21. PRs #282 and #283 had been open and never reviewed for about fifteen hours. The cause was at line 22: it loaded a CACHED `open_prs.json`, last written 2026-08-14 21:02, and never refetched, so the denominator was measuring the age of a file rather than the size of the queue.

The reason this matters beyond one script is that we adopted "print a denominator or a positive control" at bus 2679 precisely so that a zero could be trusted. A denominator computed from a cached population satisfies that ritual completely and still lies, and it lies in the quiet direction by saying all clear. The three controls passed because they only ever exercised the verdict function on synthetic rows; nothing exercised the population fetch. Controls that test the classifier but not its input are theatre. Anyone with a gate whose input list is cached, memoised or fixture-backed should check it for the same shape. Fixed: the script now fetches live every run, falls back to the cache only with a loud `SOURCE: STALE CACHE ... DENOMINATOR IS NOT THE LIVE QUEUE` label, and prints the POPULATION itself rather than only its size. Re-ran clean: 21 open, 19 reviewed, 2 unreviewed. Worth noting separately that my own handoff doc had described this script as regenerating the list from `gh pr list`, which was simply false, and it would have crashed if the cache were absent. Do not trust a tool description you have not read the source of.

#282 BLOCKED (registry auth hardening, `exec/tsk-fo7lxp`). The blocker is an undocumented THIRD failure class. The diff hard-403s GRANT failures in verify-and-warn mode, which is the DEFAULT mode, while its own stated rationale only justifies rejecting credential failures: bad signature, issuer mismatch, revoked id, and sub not matching from. A valid, correctly signed token whose sub matches from, presented by an agent with no active grant, is none of those. The identity is proven and nothing is being impersonated; it is an authorisation gap. The concrete regression is an operator mid-rollout whose agents already carry tokens but whose grants are not yet provisioned: they go from 200 plus a warning to 403 on every send, caused by a flag documented as non-enforcing, during exactly the migration this PR calls itself a prerequisite for. CI-proven rather than inferred: 2 failed, 1360 passed, with master green, so the blast radius is exactly the two warn-mode tests. Also required: the card asks for tests and the diff touches none. Minor: `staleness_bound` is a knob nothing can turn, since the only construction site never passes it. Verified good: the staleness logic itself is correct and None-safe, and enforce mode is genuinely unchanged as claimed.

#283 BLOCKED (promote `_normalise_handle`, `exec/tsk-pgtl4b`). The helper itself is the best specified thing reviewed this week and satisfies both of the card's constraints, including the docstring that names it a slug match rather than an identity check. The blocker is the second half: it vendors a copy of #233's unmerged `taosmd/mentions.py`. That file is absent from master and added by BOTH PRs, so whichever lands second takes a whole-file add/add conflict, and nothing in `taosmd/` imports #283's copy (control: the same grep over `tests/` returns three hits), so landing it first would put a mention index on master that nothing writes to. The card had asked for #233's store to call the helper and for local spellings to be DELETED; vendoring adds one instead. Required change: identity comparison is now normalised but the revocation check one line below still uses the raw sub, flagged with its precondition stated honestly because there is no mint path in this repo and I did not test it.

CARD FILED: `tsk-djcab7`, a guard that fails unless `def _normalise_handle` appears exactly once under `taosmd/`, with the RED direction required. #241 and #283 each define a copy that is behaviourally compatible with the other, which means no test, no bot and no CI run can see the duplicate, because both halves are individually correct. If #241 rebases without deleting its copy, master silently acquires the two definitions this line of work exists to prevent.

A CORRECTION TO A RULE I HAD RECORDED AND REPEATED: `assignee_id` IS a stored field and `@any` IS a real value. Census of open `fleet:claimable` cards on this board: 24 of 39 carry `@any` and 15 carry null, so both spellings are live and `@any` is the majority. The earlier claim that 37 of 37 were null and that `@any` was prose shorthand was wrong, and the cause was censusing `assignee`, which is not the field name.

MONITOR PASS OTHERWISE CLEAN, with the null results stated as measured rather than assumed. Backup watch alive in the system crontab (path-precise grep). Master unchanged at cd44441 with nothing merged in three hours and no unpushed local commits. Six `exec/*` branches are pushed with no PR ever opened, and all six were checked: every one is ahead=0 with zero changed files, so there is no unsalvaged lane work, 6 checked and 0 carrying content. No new issues on jaylfc/taosmd since 07-28, and jaylfc/taos-agent-commons carries only its readme issue, so no relay is owed. Research report is current at v1.60 with E-030 still open as a pre-registered experiment awaiting its run; nothing has landed in benchmarks/results since 07-21. Fork freshness was skipped at the 09:xx fire yesterday under the hard stop, so those three numbers are over 24 hours stale and must be re-measured before being quoted.

Last updated: 2026-08-14 20:1x UTC (@taOSmd-dev, retrospective + hourly monitor; 5h 61%, 7d 97% - WIND-DOWN).

WINDING DOWN AT 7d 97%, one point off the hard stop. Going quiet deliberately rather than being killed mid-task at 98, because a session that dies at a hard limit never runs its handoff. Resume pair armed for the 21:00Z window (primary 96de2644 at 21:02Z, retry 537c3e51 at 21:22Z). The 7d does not reset until 2026-08-16T02:00Z, so the waking session comes back into the same orchestration band. Posted at bus 2746 tagging @taOS-dev and @hermes.

CURRENT STATE. Master at 88afb05 plus this entry. 19 open PRs, 0 unreviewed, predicate controls passing. Four merges today (#278, #275, #280, #281), every one with its card closed in the same breath and every residual carded rather than left on a merged PR.

TWO CORRECTIONS TO THINGS I PUBLISHED EARLIER TODAY, both caught by the retrospective rather than by anything failing.

FIRST, AND IT REVERSES A REPORTED RESULT: I told @taOS-dev at bus 2714 that the dispatch-counter reset did not move this board. That was wrong, and the error was in the observation window rather than the measurement. I checked eight minutes after the reset, saw three lanes idle, and reported that as the outcome. Re-checked at 20:0xZ: `tsk-pgtl4b` was offered and claimed at 17:51, seventy-three minutes after the reset. So the cap WAS binding for at least one card and @taOS-dev's remedy is vindicated here too. A negative result needs its observation window justified exactly as a positive one needs its control; I had the control and skipped the window.

What that run then did is worth more than the correction. From the executor's own `.out` file rather than the CLI log: the lane claimed at 17:51:23 after recovering from a WEDGED state where it still held `tsk-mtiosy`, pinned its base, ran the full 2400 seconds, and was killed by the NEW watchdog at 18:31:24 (`rc=142`). It then BOUNCED on a merge commit and was released to the pool, so even the killed run's output was rejected. The card burned an offer and produced nothing. This is a clean instance of the population @taOS-dev described as unbounded and not reachable by a bigger number: doubling the watchdog from 1200 to 2400 did not save it. Note also that this kill does NOT match the "log ends mid-sentence" signature - the CLI log ends on a clean pytest failure (1 failed, 6 passed in 2.31s), so that signature identifies some kills but not all, and the `.out` file is the reliable discriminator.

SECOND, AND IT RETRACTS A MECHANISM I RECORDED: I wrote in the previous entry that an explicit `limit` on `/a2a/messages` returns the OLDEST N of the window. That is FALSE and has been retracted from the memory file too. The real cause was a parser fault of mine: the bus response key is `messages`, while the BOARD API uses `items`, and an ad-hoc readback written as `d.get("items", [])` returns an empty list against a completely healthy bus. I saw the empty list twice and invented a mechanism to explain it instead of checking the payload shape. Retested with the correct key: `limit=3` returns ids 2744 to 2746, the NEWEST three. The durable lesson is the one that generalises: an empty parse and an empty result are indistinguishable, so a readback must print its own returned id range as its control, and a verification step that can fail for its own reasons needs a control of its own.

CARD FILED: `tsk-aggpiw`, the `tsk-sgoilz` refile promised to @taOS-dev at 2714, carrying the unregistered-migration blocker (`_ARCHIVE_INDEX` stops at 2, so a fresh DB has no `source_id` and `find_source_ids()` raises), #230's original blockers, and the carry-forward sequence with its explicit `git commit -m` step. Worth recording that `spec_cards.py` REFUSED the first draft for a real defect: a bare `git commit` after `git merge --squash` adopts SQUASH_MSG, so an em dash anywhere in the carried history would bounce the lane at the attribution guard. It then 401s on this identity because it authenticates as @taOS-dev's side, so the card went in via the API with `claimable` + `fleet:claimable` to match the shape it emits.

FLEET HEALTH prints THROTTLE LOCKED at 9/8 open fleet PRs with PR 2396 CLEAN and green. That is jaylfc/taOS and therefore @taOS-dev's to merge, not ours. Reported, not actioned.

Last updated: 2026-08-14 16:5x UTC (@taOSmd-dev, hourly monitor; 5h 21%, 7d 94%, orchestration band).

CURRENT STATE (16:5x UTC). Master is at 82cf9a7. 19 open PRs, 0 unreviewed. The two residual cards filed at 16:2x were built, reviewed, merged and closed inside forty minutes, which closes out both of the merges from the previous entry completely: nothing from #278 or #275 is now carried only on a merged PR.

#280 MERGED, d317c6e, card `tsk-myj4ey` closed in the same breath. Its acceptance bar was RUN rather than read, which under the bus 2679 ruling is orchestration because the bar was pre-specified in the card. Three trees, identical injected regression (`last_ts = None` at http_server.py:1661, i.e. validation removed from the stream handler), each under a hard external `timeout 60`: this PR with the regression gave four failures on their own assertion (`assert 200 == 400`) in 3.34 seconds with the accept test passing as an in-run positive control; this PR restored gave five passed in 3.09 seconds; and pre-#280 master under the same regression was killed by the external bound at 60 seconds. That third row is the one that makes the other two mean anything, because it shows the tests this PR replaced hang where these fail. Verified on master afterwards: five `_stream_rejection` references and zero remaining `_get` calls against `/a2a/stream`.

#281 MERGED, 82cf9a7, card `tsk-4f7hsa` closed in the same breath. Verified against the imported object rather than the diff, because a substring match on a patch does not prove a docstring is attached to its function. Controls on the rendered `__doc__`: ordering sentence present, and no description of `principal` as a membership filter, so the fence in the card body held and that question correctly stays with `tsk-c4fnf5`. The automated REVIEW WARNING on that PR was a false positive: it fires on the token "test" appearing in a card, and this card mentioned the suite only to ask for a no-op confirmation, so any docs card will trip it.

CARD FILED: `tsk-ocjut3`, priority 0. `_stream_rejection` discards the bytes it has already read when it starts a fresh header buffer after the status line, so it works only because real 400 responses are longer than one 128-byte read. Recorded as a non-blocking residual on #280 rather than held for another round, and carded rather than left on the merged PR, because a residual that lives only on a merged PR is exactly as unreachable to a lane as a review that lives only in a comment. The card requires the fix to be proven insensitive to the chunk size by running at both 128 and 65536.

THE DISPATCH CAP HOLDS STALE VERDICTS, AND IT COMPOSES WITH CLOSE-TO-REDISPATCH. @taOS-dev found (bus 2709) that `next_card.py`'s five-offer cap had eaten their entire dispatchable pool with counters burned under a 1200-second watchdog regime they had already fixed, while `fleet_health.sh` blamed the quarantine, which cost 10 of 116. The same pattern is here: of 28 counters belonging to live cards on this board, 12 were capped and 10 of those were last offered before the 11:47:32Z fix. The important part is what the remedy needs before anyone applies it again. Of those 10, only 5 were safe to reset: 4 were already shielded by an open PR, so a reset changes nothing, and `tsk-sgoilz` would have redispatched against a body carrying #230's blockers and not #266's, the unregistered migration that the closed PR was blocked on. The cap was accidentally protecting us from the close-to-redispatch defect. Rule: never reset a counter on a card whose PR was CLOSED without first checking the body carries the blocker. Manifest at `~/.taos-team/dispatch-counter-reset-taosmd-20260814-1640.txt`.

AND THE RESET DID NOT WORK HERE, WHICH IS REPORTED RATHER THAN QUIETLY DROPPED. Eight minutes after resetting the five counters, all three lanes still logged "idle: no claimable cards". So on this board the cap was NOT the binding constraint, unlike theirs, and whatever is actually strangling the selector is still unexplained. Not chased further: the dispatcher is @taOS-dev's subsystem and they are actively working it, so re-deriving their diagnosis here would burn lead context on a problem that already has an owner. Reported at bus 2714.

TWO PROBE FAILURES OF MY OWN THIS HOUR, BOTH CAUGHT ONLY BY THE OUTPUT LOOKING TOO TIDY. The first counter census `cat`-ed files with no trailing newline and concatenated 311 values into a single meaningless string. The second reported `exit=0 wall-clock=0s` on a red run that never executed, because `python` is not on PATH on this box and `$?` after a pipeline had captured `tail` rather than pytest. A confident zero-second pass and a real one are indistinguishable from the exit code alone. Both are the same family as the two @taOS-dev reported in the same hour, and the generalisation stands: absence of evidence read as evidence of absence, where the check answers a question adjacent to the one being asked.

READBACK VERIFICATION NEEDS A WINDOW THAT CONTAINS THE TARGET. Verifying a posted bus message with `limit=3` returned nothing and looked like an empty body, because an explicit `limit` on `/a2a/messages` returns the OLDEST N of the window rather than the most recent N. The message was intact at 4,992 bytes. A verification step that can fail for its own reasons needs its own control.

Last updated: 2026-08-14 16:1x UTC (@taOSmd-dev, post-clear #8; 5h 4% on a fresh window, 7d 93%, orchestration band).

CURRENT STATE (16:1x UTC). Master is at 1e79f08. 19 open PRs, 0 unreviewed. Both PRs that were waiting on a decision are MERGED, both cards are closed, and both residuals are refiled as claimable cards. The board shows 105 tasks, 46 live, 17 shielded by an open `exec/*` PR and 29 dispatchable.

THE REVIEW QUEUE IS EMPTY AND IT IS MEASURED, NOT ASSERTED. The freshness predicate now lives at `~/.cache/taosmd-review/freshness.py` rather than being retyped each session, and it prints its own controls before it prints a verdict: `never-reviewed -> UNREVIEWED`, `stale-review -> UNREVIEWED`, `fresh-review -> REVIEWED`, so both verdicts are demonstrably reachable and a silent all-REVIEWED result cannot come from a broken comparator. Result: `open=21 reviewed=21 unreviewed=0`, with the loop counter proving it ran 21 times over a denominator of 21. Re-run it rather than trusting this line; the queue refills roughly every 20 minutes.

A BOUNDED PRE-SPECIFIED PROBE IS ORCHESTRATION, RULED AT BUS 2679. This answers the question left open in the previous entry, and it is the rule that governs how verdicts get made in the band from here. Executing an acceptance bar that already exists (in a card, in a prior review, in a gate's own criteria) is orchestration; inventing the bar now is hands-on. The condition is not optional: a probe that runs under the band MUST carry a positive control or a printed denominator. The reasoning behind the condition is worth keeping, because it is the specific way a probe fails at 91 percent, @taOS-dev asked how many cards carried an unenforced `needs:` label, got 0, and nearly published a fleet-wide all-clear when the fetch had actually returned nothing (`.get("tasks")` against an endpoint serving `items`). An unverified all-clear and an unverified alarm are the same failure, and the quiet one ships more easily. Without a control, abstaining beats probing; with one, probing beats abstaining every time.

THE AUTOMATED USAGE NOTICE STILL CONTRADICTS THE RULED BAND, AND IT FIRED TWICE THIS SESSION. `usage-watch` posted "5h usage 100% - HARD STOP" at 14:52Z and again at 15:02Z. The ruling retired `five_hour` as a work gate (bus 2646) and gates on `seven_day` only, so the notice is overridden and was correctly ignored, but the watcher's thresholds live in a different place from the ruling. That is the same two-documents defect that cost the fleet two days when three leads were operating on two different bands. Raised with @taOS-dev at bus 2694 as a fix to make on the watcher rather than a caveat for every reader to carry.

#278 MERGED, 762982d, card `tsk-q6fjux` closed in the same breath. Verified on master rather than on the merge button: `_stream_status_code` at tests/test_a2a.py:323 reads only the HTTP status line over a raw socket and RAISES on timeout instead of counting the non-return as an accept, which is precisely what #276 lacked.

#275 MERGED, 1e79f08, card `tsk-qthbox` closed in the same breath. Verified on master: service.py:765 sorts `_last_ts` reverse, and the return at :766-768 rebuilds each item with exactly four keys, so the internal sort key sorts but never reaches the wire. Both `a2a_threads` and `a2a_thread_messages` are in `__all__` at :1262. Merged with the blast radius measured at zero consumers, so it is safe to land because the endpoint has no audience yet, not because the change is compatible.

BOTH RESIDUALS WERE REFILED WITH THE DIAGNOSIS IN THE CARD BODY, which is the lossless form. This is the close-to-redispatch rule from the previous entry applied at the merge boundary rather than the close boundary: a residual that exists only on a merged PR is as unreachable to a lane as a review that exists only in a comment, because `executor.sh` builds the lane prompt from the card body alone. `tsk-myj4ey` carries #278's residual: its four rejection tests still route through `_get` to `_send` to `urlopen(timeout=10)`, and because the SSE keepalive resets a per-recv timeout, a regression in `since` validation would make them hang rather than fail. The card requires the red control to fail FAST under an external `timeout 60` and says plainly that a red taking about 100 seconds is a hang wearing a failure's clothes. `tsk-4f7hsa` carries #275's required docstring sentence with the exact wording, and explicitly fences the lane off `principal` so nobody describes it as membership; that stays `tsk-c4fnf5`. Both cards were claimed by lanes within minutes of filing.

`assignee: "@any"` IS NOT A STORED FIELD, and the census is the reason to believe it. New cards come back with `assignee: null`, which looks like a filing error until you count: 37 of 37 open `fleet:claimable` cards on this board have `assignee: null`. So `@any` is shorthand we write in prose and the null is the convention. Recorded because the alternative was an hour spent "fixing" a non-problem, and because a census settled in one query what a single card could not.

THE PI WAS UNREACHABLE FROM LEAD-LINUX FOR ABOUT 13 MINUTES AROUND 14:49Z, then recovered on its own. `ping` failed and both the board port and the raw bus port returned 000, while `scp` to the same host had succeeded 30 seconds earlier and `ssh` kept working throughout. Both ports read 200 again by 16:0xZ. The useful part is the shape rather than the outage: a working `ssh` alongside a dead `curl` to the same host is not "the bus is down", and reading it that way would have sent the next hour into the wrong subsystem. When a direct fetch fails, run the probe from the far side over `ssh` before concluding anything about the service.

A HEREDOC CANNOT READ A PIPE. `curl ... | python3 - <<'PYEOF'` silently feeds the SCRIPT to stdin and discards the piped data, so the JSON never arrives and the failure surfaces as a JSONDecodeError on the script's own first character. This is the mirror of the backtick trap in the previous entry and it belongs beside it: write the script to a file and pipe into that. Both failures share a cause, which is assuming stdin is available when the shell has already spent it.

Last updated: 2026-08-14 14:5x UTC (@taOSmd-dev, hourly monitor; 5h 78%, 7d 91%, orchestration band).

CURRENT STATE (14:5x UTC). 21 open PRs. #278 is reviewed and OPEN awaiting a merge decision; #275 is OPEN awaiting one required docstring sentence. Master is at 031b401. Both closes from the 13:4x pass worked: lanes rebuilt `tsk-q6fjux` as #278 and `tsk-e4cpto` as #279 within about forty minutes.

CLOSE-TO-REDISPATCH IS NOT LOSSLESS, AND THIS IS THE FINDING TO CARRY. Closing a blocked PR frees its card (`next_card.py:32`) and DISCARDS the review in the same motion, because the review lives on the PR and `executor.sh` builds the lane prompt from the card BODY only. Measured: #279 came back carrying the identical blocker to #277, forty minutes after the close. The lane could not have known. Its card named the fix as `comments.totalCount > 0 in the GraphQL selection` and never said the script must stop using `gh pr view --json reviews`, whose payload has no `comments` key at all, so two lanes in a row dropped the right expression into a dead payload. A close-to-redispatch is only complete when the blocker is written into the card BODY: close AND refile, never close alone. Closed #279, closed `tsk-e4cpto`, refiled as `tsk-b6tel3` with the live key listing pasted in, the exact GraphQL replacement, a required fixture key-path diff (`comm -23` of fixture paths against live paths, which must print nothing), and the acceptance bar stated as four of six PRs flipping with the explicit warning that all six passing means the check has gone permissive.

RELATED TRAP, worth knowing before writing any gate: `gh pr view --json <field>` validates the TOP-LEVEL field name and rejects unknown ones, so `--json reviews` looks blessed, while nothing validates the shape inside it. A nested key that is never present reads as a permanent default and the guard is inert.

#278 IS A REAL FIX, with one residual. `_stream_status_code` reads only the HTTP status line over a raw socket with a bounded timeout and RAISES on timeout rather than treating it as an accept, which is exactly what #276 lacked, and the accept test asserts 200 so accept and reject differ by status code rather than by absence of a 400. The duplicate definition and the misnamed test are gone: it adds 85 lines to `tests/test_a2a.py`, which already exists on master at 776 lines, with zero test-name collisions against `test_http_server.py`. RESIDUAL, flagged on the PR as a reasoned concern rather than a measured finding: the four rejection tests still use the `timeout=10` urllib helper, whose timeout is per socket read and is reset by the stream's keepalive, so if the `since` validation ever regresses those four would hang rather than fail. Harmless today because a bad `since` is rejected before the SSE headers are written. IF #278 MERGES WITHOUT THAT CHANGE, CARD THE RESIDUAL, or it is lost with the PR.

BOT AVAILABILITY IS A SHARED EXHAUSTIBLE RESOURCE, which is a correction to my own earlier line. I reported that two of three bots were not reviewing; that was scoped to this repo's last five merged PRs, where qodo was billing-blocked 5/5 and coderabbit posted a rate-limit notice. @taOS-dev measured coderabbit reviewing normally on jaylfc/taOS. Both are true: coderabbit's limit is per-account and burns down, so our repos compete for one budget. The right statement is that coderabbit's availability is not a signal any single gate can assume, which is a stronger argument for their INSUFFICIENT DATA ruling on the bot gate than my version was.

BOARD IS CLEAN FOR THE INERT-LABEL CLASS (@taOS-dev's 2667 sweep, run here with the denominator printed as a positive control): 45 cards fetched, label prefixes in use are `fleet:` 36 and `blocked-on:` 2, cards carrying any `needs:` label 0. The prefix census matters more than the zero, because a zero from a `needs:` grep and a zero from a failed fetch look identical.

OPEN CARDS FILED TODAY, all `fleet:claimable`, all with negative controls specified: `tsk-c4fnf5` (`/a2a/threads` accepts `principal=` and ignores it locally; sender-derived filter named honestly, never "membership"), `tsk-bgqv52` (restore the three assertions #272 deleted by accident), `tsk-b6tel3` (the merge-gate data source). `tsk-q6fjux` stays open until #278 merges, and closes in the same breath as that merge.

AWAITING A RULING FROM @taOS-dev (asked at bus 2675, with a recommendation): under the ratified hands-on/orchestration cut, a test run is hands-on, but on this repo green CI and green bots have repeatedly meant nothing until probed. The question is whether a BOUNDED, PRE-SPECIFIED verification probe counts as orchestration. My recommendation is that it does, because the degradation-prone part is deciding what to measure rather than running it. Until it is ruled, verdicts here are diff-level and say so on the PR.

Last updated: 2026-08-14 13:5x UTC (@taOSmd-dev, hourly monitor; 5h 62%, 7d 90% CRITICAL, orchestration-only band).

USAGE BAND, RULED AND SINGLE-SOURCED (Jay via @taOS-dev, bus 2646/2650). Three leads were operating on two rules because the thresholds lived in different documents, and that ambiguity was the defect rather than either reading. One rule now: below 90 work normally; at 90 wind down your OWN hands-on work; from 90 to about 97 KEEP ORCHESTRATING (monitor lanes, adjudicate PRs, feed the board, direct agents); 98 is the hard stop. The `seven_day >= 50` monitor-only band and `five_hour >= 70` as a stop are both retired. The reasoning is arithmetic and worth keeping: the lanes run free models and the burn is lead context, so a lead going dark at 90 saves its own context and idles three lanes and a review queue to do it. I read 90 as "stop and go quiet" and was corrected; this file now carries the rule so the next session does not re-derive it.

CURRENT STATE (13:5x UTC). 20 open PRs, 0 unreviewed, predicate re-run with counts (`open=22 reviewed=19 unreviewed=3` before the pass, then all three reviewed). Master is at d3a381a. The queue refills roughly every 20 minutes: re-run the predicate rather than trusting this line.

REVIEWED AND DISPOSED THIS SESSION. #275 (A2A thread list ordering) is APPROVED with one required change: the `a2a_threads` docstring must state the new ordering, because the endpoint appears in no doc, README or CHANGELOG, so after merge the only record of the contract is one `sort` line and one test. Its ordering test is proven red on master by its own assertion and green on the branch, `_last_ts` is verified stripped on the wire rather than only in the diff, and the full suite shows the same 10-failure set as master with exactly the 6 new tests added. Worth noting for whoever merges: only one of its six new tests defends the change, the other five pass on master too and are regression coverage.

#276 and #277 are CLOSED, not rejected, so their cards can redispatch. Both branches are untouched and both reviews stand. `next_card.py:32` excludes any card whose `exec/*` PR is open, so a blocked PR has no revision route and closing is the only way to get the fix built; measured precedent on this repo is that closing blocked PRs produced real revisions (#260, #261).

TWO GREEN-BUT-EMPTY TESTS, AND THEY ARE DIFFERENT DISEASES. #277's merge-gate fix reads `comments.totalCount` from `gh pr view --json reviews`, which carries no `comments` key at all, so the new expression is 0 for every review on every PR and the gate's behaviour is unchanged: all six live PRs still fail, the same six as before. CI was green because `tests/fixtures/merge_gate/pr_inline_only.json` hand-writes a field the real `gh` never emits, so the old-versus-new differential in its test measures the fixture rather than the gate. The positive control that makes those six FAILs verdicts rather than noise: #230, #241 and #244 print SUCCESS in the same run. The logic itself is right and GraphQL does carry the field (`reviews{nodes{comments{totalCount}}}` returns 7 inline comments for #267), and running the PR's own logic against that payload flips four of six while #274, #275 and #277 correctly still fail. @taOS-dev has adopted the generalisation fleet-wide: a fixture that emits a field the real tool does not is a green light wired to nothing, so any gate whose fixture was hand-authored rather than captured from a live run is unproven until someone runs the real command once and diffs the keys.

#276's SSE tests are the other disease: they pass against a correct server and, against the defect they exist to catch, they hang rather than fail. Measured on stable storage with a hard timeout, both halves in the same worktree: unmodified head passes in 0.75s; with `_parse_since` removed from `_handle_a2a_stream` the single test was killed at 100s having produced no output at all. The mechanism is that the accepted stream writes a keepalive every second (http_server.py:1689) while urllib's `timeout=10` is per socket read, so each keepalive resets it and `read()` never returns. In CI that is not a red test, it is a job that consumes the whole workflow timeout and reports nothing. Two further defects there: a test function defined twice (only one collected), and `test_a2a_stream_since_valid_epoch_unchanged` never passing `since` at all despite its name and docstring.

A CONTRACT CHANGE WITH NO AUDIENCE. #275 changes `/a2a/threads` from alphabetical to most-recently-active ordering, which is consumer-visible in principle. Measured rather than cautioned: zero consumers today. The dashboard calls `/a2a/channels`, `/a2a/members`, `/a2a/messages` and `/a2a/stream`; a filesystem sweep across the development tree and the fleet scripts finds no external caller of `/a2a/threads`, with the same sweep for `/a2a/messages` returning 8 files as the control that the sweep works. So it is safe to land because the endpoint has no audience yet, not because the change is compatible. The warning is in the PR body, not only in review notes.

CARD FILED: tsk-c4fnf5. `GET /a2a/threads` accepts `principal=`, http_server.py:1697 reads it from the query string and passes it in, and the local path never uses it, so a request asking to be narrowed is answered with everything at 200 and no signal the filter was dropped. Jay's call is to implement it as a sender-derived filter named honestly ("threads this principal has sent to"), never as membership, per the binding line in `docs/specs/a2a-bus-auth-transition.md`. The card carries the no-membership finding inline so nobody rebuilds the impossible ask, and requires a negative control proven red.

JOB PACK REFRESHED (weekly gate at 7d 90%). All four open jobs re-verified against current master rather than assumed: 120 em dashes still in docs/benchmarks.md (JOB-001), the relative `models/cross-encoder-onnx` default still at cross_encoder.py:21 (JOB-002, still ON HOLD, do not start), the dead `_pkg_files` import still at http_server.py:193 with its single user at :210 (JOB-003), issue #205 still OPEN (JOB-004). JOB-005 is closed out in the index. Added JOB-006: repair the mangled Part 2 comment block in tests/test_ref_fetch.py, a residual from #273 that landed at column 0 with mixed indentation and pastes review prose including a sentence the same PR made false. Comments only, zero behavioural effect.

TOOLING GOTCHAS FOUND TODAY, all cost time. `gh pr edit --body-file` fails on this repo with a Projects-classic `projectCards` deprecation error and leaves the body unchanged while printing only the error, so use `gh api repos/jaylfc/taosmd/pulls/<n> -X PATCH --input <json>` and re-read the body to verify. The board API is on a different port from the raw bus, `TAOS_API` is not in the cred file (it lives only in the fleet scripts), task `priority` is an integer where a string 400s with `int_parsing`, and task lists come back as `{"items": [...]}` rather than a bare array. `/tmp` is being reaped mid-run on this box: a scratch worktree was deleted while pytest ran inside it and produced a fake hang that I nearly reported as a finding, so review worktrees now live under `~/.cache/`.

Last updated: 2026-08-14 08:15 UTC (@taOSmd-dev, hourly monitor; 5h 16%, 7d 84%, both normal).

CURRENT STATE (10:4x UTC). 23 open PRs, 4 unreviewed (#274 #275 #276 #277), 0 stale, predicate re-run with all three verdicts forced on control inputs. Master is at 66b8865. All four unreviewed PRs are green on all five checks, which on this repo means nothing until probed: #264's test was green and vacuous, and #269 was green while not fixing the defect in its own title.

FORK FRESHNESS (checked 09:4x, daily): rknn-llm unchanged at release-v1.3.0, so no new NPU model support; qmd's latest tag is still v2.5.3, so the @jaylfc/qmd rebase stays on HOLD. **jaylfc/rkllama is DIVERGED from notpunchnox/rkllama: ahead_by=9, behind_by=13.** Not actioned - the Pi and its runtime are @taOS-dev's, and upstream's lock-removal (#152) conflicts with our per-model locking, so it is an architecture decision rather than a merge. Note for whoever checks next: `pushed_at` said our fork was NEWER and is not an ancestry measurement; use `gh api repos/<upstream>/compare/main...<fork>:main`.

MERGED TODAY: #273 (66b8865) makes the redirect-leak test fail on its own assertion rather than on an incidental HashMismatchError, so the token guard is now both defended and correctly diagnosed; #253 (e3ea7c3) `_parse_since` with a finite guard and a 1e9 epoch floor, wired into both `/a2a/messages` and `/a2a/stream`; #264 (ac8f714) the `_NoRedirect` opener so a bearer token can no longer follow a 3xx to a second origin, plus a real `POST /refs/fetch` route; #272 (0bce8c6) which replaces a vacuous redirect test with one that asserts the absent `Authorization` header on the wire, proven red-then-green. #267 (6de8798) was merged by @taOS-dev: three merge-gate scripts under `scripts/merge-gate/`, wired into NO workflow, so they are inert today. Do not wire `check_bot_anchoring.sh` into CI until its inline-comment signal changes: the condition is `body.strip() or has_inline` and `includesCreatedEdit` means "the review was edited", so an inline-ONLY review (CodeRabbit's common shape) is judged non-substantive and the gate blocks correct PRs. The replacement counts `comments.totalCount > 0`. That card (tsk-zgbkal) is @taOS-dev's, now fix-forward-on-master.

CLOSED AS SUPERSEDED: #234 and #242 (bases whose revisions merged; STEP 0 squash means no auto-close, so an open base silently reverts what just landed), #246 and #269 (identical change sets, hash-verified with a discriminating control; neither fixed the nan/inf defect in its own title because `nan < 1e9` is False, and both are already satisfied by master), #271 (duplicate rebuild whose only new line was a regression: `quote(path, safe="")` breaks every nested ref path while traversal controls reject identically on both trees, so it bought no security).

BLOCKED, NOT MERGED. #270 (receipts) carries all three of #232's original defects unchanged: `_get_authenticated_agent_id` still decodes with `verify_signature: False` and a forged token returns `'taOS-dev'` while `decode_and_verify` rejects the same token, and its new docstring now falsely claims the token is "verified"; `_handle_admin_a2a_prune_receipts` is dispatched at :1090 and never defined (6 of its 7 sibling handlers ARE defined, which is the control); 5 of 9 `remote.a2a_*` methods are missing, all 5 the ones it adds; zero tests. Its migration IS registered correctly in both REGISTRY and DB_FILES, the thing #230 and #266 got wrong. #268 is HELD rather than rejected, and the reason is a defect in my own card, described next.

THE FINDING THAT MATTERS MOST TO A RESUMING SESSION: there is no channel membership in taOSmd, so a card asking to measure it cannot be built. `service.a2a_channels()` sets `members` from `data["from"]`, `a2a_members()` is documented as "distinct sender names observed", and no membership, join, roster or subscription table exists in archive.py or migrations.py. My card asked for the impossible and no rebuild would have fixed it; closed and refiled as tsk-knncne. The second-order lesson is worth more: that measurement tool's conclusion is `n3 == 0 and n4 <= 1`, i.e. absence of counterexamples, so `measure([])` prints "safe", and the real default run extracted 27 pairs from a 14,397-line spool and still printed SAFE. A gate whose SAFE verdict is reachable from zero data is fails-open. Every gate now states what counts as INSUFFICIENT DATA and exits non-zero there, or it is not a gate. @taOS-dev adopted the same rule and found the identical shape in their own `gate_merge.sh` (an empty card body skipped the whole red-proof branch), proved it red, fixed it, proved it green.

CARD LIFECYCLE RULE, learned by breaking it: close the card in the same breath as the merge. Leaving tsk-7xcsh5 open after #264 merged sent a lane to rebuild already-merged work as #271, and it consumed @taOS-dev's lane capacity rather than ours. An audit of every merged-PR card afterwards found two more still open. Open cards filed today: tsk-q6fjux (stream since-tests), tsk-knncne (corrected measurement), tsk-k55tvk (make the redirect-leak test fail on its own assertion rather than on HashMismatchError).



THE FINDING THAT MATTERS MOST TO A RESUMING SESSION: "blocked, goes back to its own lane" is a mechanism that does not exist, and I had been closing reviews with that phrase all week. Measured three ways. `dispatch_loop.sh:169-176` collects every open `exec/*` PR across the five repos and `next_card.py:32` excludes those cards from selection, so a card whose PR is open is unclaimable BY DESIGN. `review_loop.sh` posts a comment and stops, and is hardcoded to `jaylfc/taOS`, so taosmd never had even that half. `executor.sh` pins BASE per repo with no per-card override, so a replacement card rebuilds from master and DISCARDS the branch's work. Empirically, of 27 taosmd `exec/*` PRs that ever carried a review from me, 2 were revised afterwards and 25 were not, and both of the 2 were revised by jaylfc committing directly rather than by a lane. A lane has never once revised a PR in response to a review here. Seven PRs were sitting in that state. @taOS-dev verified all three mechanisms in their own source and carded the fix as tsk-bvfejd; they named a fourth (their tsk-4z33eb), the linear-commits guard at `executor.sh:584` that auto-bounces the `git merge` every supersede card mandates.

THAT FOURTH MECHANISM WAS A LIVE DEFECT IN MY OWN CARDS. tsk-cco6uy and tsk-3tyecd both opened by telling the lane to `git merge` the old branch, which `rev-list --merges "$BASE_SHA"..HEAD` bounces. Cards written to stop work being discarded would themselves have bounced and burned a dispatch. Neither had been claimed. Closed and replaced by tsk-aeu77i (revise #241) and tsk-6icvd4 (revise #233), using `git merge --squash` plus an ordinary commit, both labelled `blocked-on:tsk-pgtl4b`, with an acceptance line requiring `git rev-list --merges` to be empty before push. Known limit, stated in both cards: `--squash` preserves the WORK but not the ANCESTRY, so it cannot satisfy @taOS-dev's proof requirement for the real fix. That is the point of their criterion, and when the BASE override lands both cards should drop step 0 rather than keep both paths.

MERGED TODAY: #240 (f6b5a17) the deleted-symbols gate, proven red on the real incident and green on a benign branch, and #243 (53d147b) which surfaces its `Removes-Intentionally:` waiver in the failure output. #240 is deliberately NOT in the CHANGELOG yet because #236 is an open PR editing that exact section; fold it into #236's revision.

OPEN PRs (13, replacing the stale three-PR list further down this file): #245 is mine, the A2A bus-auth design and three-stage transition plan, docs only, awaiting @taOS-dev's review. #241 #233 are blocked and now have revision cards. #230 #232 #234 #239 #242 are blocked with no path to a lane and are deliberately waiting on tsk-bvfejd. #235 and #244 both wait on a rebase: #244 is cut from master and is a parallel reimplementation of #235, and in either merge order the docstring describing the revocation fix conflicts while the guard that undoes it auto-merges silently, taking the weaker version. #236 #237 are reviewed. #204 #185 are long-held and Jay's.

A CHECK WHOSE DATA SOURCE CANNOT CARRY THE CASE: the controller does not publish human principals on the revocation feed and structurally cannot, since `list_revoked()` selects from `agent_registry` whose insert writes the agent's OWNER as `user_id` rather than creating a principal row. So #244's docstring, which frames this as pending data, must be corrected before merge and the card must not close as though human withdrawal works. Live risk is bounded today only because with no `human_principal_ids` configured the guard's condition is always true; the hole opens the first time the feature is used for its purpose.

Last updated: 2026-07-29 00:35 UTC (@taOSmd-dev, wind-down at 5h 95%, reset 01:40Z).

A DIAGNOSIS I GOT WRONG, corrected here because I broadcast it. I reported that tonight's controller password rotation had SEVERED my board write path (task comments 403, cookie login 401). Wrong. The 403 was the ACTOR-STRING bug: the board wants my canonical registry id in `author_id`, not my `@handle`. Proven by posting cmt-4lkngk on tsk-zgbkal with the same token that had been 403ing. The cookie path authenticates as a session user, so a wrong actor string sits harmless for weeks and only starts failing once the cookie dies and the token becomes the only path; the rotation UNMASKED the defect rather than caused it. I had the tell in my own message (card CREATE worked while COMMENT failed, and create carries no actor field) and reasoned about credentials anyway. What survives: task PATCH/PUT return 401 and carry no actor field, so agent tokens appear simply to have no task-edit route. That is separate and is not the actor bug.

RESOLVED 2026-07-29: THE close_reason "CONTROLLER BUG" WAS MINE. Retested with a disposable card (tsk-paqi2z): closing with the key `reason` persists perfectly under my agent token, response and stored value both correct. The controller takes `reason` on INPUT and exposes it as `close_reason` on OUTPUT; I had been sending the output name, which is an unrecognised field and is silently ignored. Fourteen of my closes lost their justification because of my key, not the platform's route. The comment-mirror workaround is UNNECESSARY and is dropped. Credit to @taOS-website-dev, whose board showed 10/10 agent-token closes keeping their reason and who spotted that closer-type and key-name were perfectly correlated in my sample.

Last updated: 2026-07-28 23:05 UTC (@taOSmd-dev, resumed after a context checkpoint-and-clear).

A MERGED SECURITY DEFECT, FOUND BY THE FIRST SWEEP UNDER THE NEW GATE. PR #212 was merged with CodeRabbit RATE LIMITED - its only artifact on that PR is the rate-limit warning, not a review - so a PR that looked reviewed had one fewer reviewer than the merge record suggests. Qodo did flag the finding at high relevance and nobody triaged it. Issue #221: secrets inside A2A `refs`/`blocks` bypass redaction entirely. `Archive.record()` redacts five TOP-LEVEL string keys and does not traverse nested structures, while `service.a2a_send()` stores refs/blocks verbatim. Proven live on master, not inferred: one message with the same synthetic token in body, block and ref came back with the body redacted and the other two not, and the raw token landed in the archive JSONL and the sqlite WAL. The archive is append-only, so anything written unredacted stays written. Issue #222 is the same root cause on the validation side: the ref-kind enum, max count and 64KB cap live only in the HTTP handler, so the MCP tool and any direct `service.a2a_send()` caller can write payloads the HTTP boundary would have rejected. Both carded together as tsk-gnxuge (p4, fleet:claimable) with a red-first test list.

MERGED-PR BOT SWEEP, remaining triage. #217 widened: `tests/test_collections_db_connect.py` has two assertions and both are wrong in opposite directions - `timeout > 0` cannot fail (SQLite defaults to 5000), and `journal_mode == "wal"` cannot pass on a filesystem where `_db.connect` deliberately falls back with a warning. #213 carries one untriaged Qodo finding, deliberately NOT carded: `capabilities._build_stamp()` publishes `COMMIT`/`BUILT_AT` from `_build_info.py` without checking the documented shapes (40-hex sha, ISO 8601 UTC). Real but low value, since that file is written by our own packaging step and the only victim is a monitoring client parsing `/version`. Revisit if we ever ship a container build. #220 had no bot review at all; defensible, it was a Dependabot postcss bump verified from the lockfile by hand.

TWO-STAGE PR VERIFICATION ADOPTED. Jay's fleet directive (A2A build, msg 1850) splits PR verification into a cheap mechanical evidence-gathering stage that is forbidden from concluding anything, and a lead adjudication stage that issues MERGE or BLOCK. Written up in docs/pr-verification.md. The three checks that need no model at all (bot-anchoring against current head, CodeRabbit fake-green detection, a red-first runner) are carded as tsk-zgbkal, fleet:claimable, p3. Blocked on nothing; I asked @taOS-dev for their tsk-3oaabh spec rather than reinvent it, since my registry token is project-scoped to prj-qigv7w and cannot read their board.

BOARD STATE: 25 open cards, 15 of them fleet:claimable, and NOTHING has been claimed since the pytest blocker cleared. Lanes can now run tests but have not picked up tsk-zp3csa (the #212/#213 revert-and-rerun sweep) or tsk-twljv4 (README dev-setup + CHANGELOG gap). PR #218 and PR #185 have no new commits since my reviews, so nothing is owed on either yet.

STALE LOCAL WORKTREES, flagged not deleted: .claude/worktrees/agent-* (three of them, dated Jun 30 and Jul 10) are orphaned from the pre-migration Mac. Their .git links point at /Volumes/NVMe/..., so git cannot see them and nothing in them is recoverable or unpushed. They each carry a STALE copy of .claude/audit-cron-prompt.md still naming the dead Pi usage path /home/jay/.taos-usage.json. Harmless to the repo (gitignored) but a grep for the audit rules hits them, so read the top-level copy.


THE PYTEST BLOCKER IS FIXED: PR #219 merged (b357ce42), card tsk-leewzc closed. A bare `uv sync` now installs pytest, pytest-asyncio, pyjwt and cryptography via a PEP 735 [dependency-groups] dev group. Verified by reproducing the defect on master minutes before merging (bare `uv run pytest` -> "Failed to spawn: pytest") against the branch reaching 10 failed / 1279 passed / 2 skipped with no flags - identical to master-with-deps-forced. This also closes the root cause of #214: the registry-auth surface now RUNS (37 passed) instead of silently skipping. Every taosmd PR merged BEFORE this was built by a lane that could not execute a single test.

CORRECTION TO A CLAIM I REPEATED: taosmd's runtime is NOT stdlib-only. Core [project] dependencies are onnxruntime, numpy, transformers. The principle (auth/test deps stay out of the core install) held; the description was wrong and appeared in card contracts.

CARDED THIS SESSION: tsk-zp3csa (revert-and-rerun sweep of the #212/#213 test files, unblocked now that lanes can run tests) and a docs card (README dev-setup still says `pip install -e .`, which leaves a contributor with no test runner; CHANGELOG missing #212/#213/#215/#219).

RETRACTED (see the 2026-07-29 block at the top of this file): I reported this as a controller bug that silently DROPS close_reason. It was my own error. The close route takes the key `reason`; I was sending `close_reason`, the output field name, which is silently ignored. Proven by probe on 2026-07-29. Fourteen (not twelve) of my closes lost their justification, and the cause was mine. No workaround is needed.

Last updated: 2026-07-28 12:40 UTC (@taOSmd-dev, before a context checkpoint-and-clear).

PI IS CURRENT AND VERIFIED. Deployed 76f72ff -> d0392a7 on 2026-07-28 00:31-00:34 UTC under Jay's decision dec-bicg4y. Migrations probed and stamped four databases without re-applying, applied knowledge_graph_baseline. Verified content-type application/json (not the SPA catch-all) plus capability membership, and smoke-tested the #212 envelope round-trip live. Backup at /home/jay/taosmd-data-backup-predeploy.tar.gz. NOTE: the Pi's correctness depends entirely on WorkingDirectory=/home/jay/taosmd in the systemd unit; from any other cwd `python3 -m taosmd` resolves to a stale pip 0.3.0 in ~/.local. That pip copy is deliberately NOT removed yet (fallback until daylight, Jay aware).

MERGED SINCE JUL 26: #201 migrations (84d40bb), #191 graph-expansion default-off (427edb2), #173 preset ladder (82ffb10), #184 granite factual-recall (03c5e09), #207 agent-job pack (1cdfaca), #212 envelope refs+blocks (98b13dd), #213 /version + capabilities (5b024fe), #215 CollectionStore WAL (b1e56b4). #200 closed superseded by #213.

OPEN PRs: #204 (LongMemEval retrieve wiring, held), #185 (external contributor task-edge endpoint, my security review still owed), #218 (a2a/import - CHANGES REQUESTED, see below).

THE WEEK'S THROUGH-LINE, and it is the thing a resuming session most needs: a long catalogue of checks whose failure mode is indistinguishable from success. Filed as issues #197 #199 #205 #206 #208 #214 #216 #217. Highlights: the registry-auth test surface SILENTLY SKIPS without pyjwt/cryptography so every suite comparison I ran was blind to it (#214); a merged test asserting `busy_timeout > 0` can never fail because SQLite's default is already 5000 (#217); a bare GET /a2a/messages returns only ~50 and looks complete (rulebook v1.20); bot verdicts can predate the head commit and read as corroboration (v1.29). ALWAYS pass limit=500 on bus reads and install pyjwt+cryptography in BOTH sides of any suite comparison.

ROOT CAUSE FOUND JUL 28, FLEET-WIDE: pyproject declares dev and registry deps under [project.optional-dependencies], and `uv sync` installs NO extras. So lanes across taOS, taosmd and taOSc have NEVER been able to run pytest, and the auth test surface never ran. One defect, two symptoms I had been treating as unrelated. Fix carded as tsk-leewzc (p3, PEP 735 [dependency-groups], keeps the extras).

DEFERRED DELIBERATELY: the revert-and-rerun sweep of #212/#213 test files waits for tsk-leewzc. Handing a red-before-green card to a builder who cannot run tests asks for proof written blind, which is the condition that produced these defects. Technique: worktree at master, revert the BEHAVIOUR (not the whole diff - a full revert breaks collection when a PR adds symbols its tests import), run only that PR's new tests, assert red.

# STATUS

## RESTART HANDOFF (read first) - 2026-07-09

Identity: this session is @taOSmd-dev (the taOSmd dev agent). Session-only monitor cron dies with the session; disk, git, memory persist. Master clean + fully pushed.

ON A NEW SESSION (on-arrival checklist):
1. RE-ARM the hourly monitor cron: CronCreate `13 * * * *`, durable, prompt = run .claude/audit-cron-prompt.md (usage gate, docs freshness, research report, memory, ALL-channel a2a bus sweep, repo PR/issue/bot-review sweep, and the new STEP 6 daily fork-freshness check at 09:xx). The old cron ids are dead.
2. Check the bus first for @taOS-dev items: the bus-auth ENFORCE-FLIP signal (still held, verify-and-warn is the live default) and any rkllm/NPU follow-up. Read this STATUS.

JULY BATCH (all SHIPPED + merged to master):
- Jul 5 deep audit (8 parallel agents) -> fix wave, all merged: #182 zero-loss integrity (embedder-failure surfacing, archive day-boundary rollover, batch archive/claims linkage, reconcile provenance carry); #183 model-resolution (split_provider strips ollama: at every HTTP boundary) + agent-registry data-dir unification; #186 + #187 SECURITY (token binding + non-enumerating project scoping on all task edge/update endpoints); #188 TTL (forget_after honored via ingest_batch); #189 KG add_entity non-destructive merge (zero-loss). Docs honesty batch (purged the pre-N-017 74.6% everywhere incl the controls.py UI string, tier-table alignment, a2a-comms registry-auth section). research-report rev 1.50.
- BENCH: granite4.1:8b LME-S full-500 54.8/60.4 = PARITY at 4.9GB Apache-2.0 but FAILED the LoCoMo kill criterion (0.475 vs qwen3.5:9b 0.515, E-024) = factual-recall-only. PR #184 (granite factual-recall) is OPEN + HELD by Jay: balanced default STAYS qwen3.5:9b; 12GB factual-recall STAYS gemma4:12b. lfm2.5:8b rejected (N-018); nemotron-3-nano:4b = viable lenient 3rd judge (F-017).
- NPU: rkllm runtime 1.3.0 is LIVE on the Pi (@taOS-dev deployed rkllama feat/rknpu-1.3.0 @ dadea41 via a new systemd rkllama.service, boot-persistent; my 2 rerank ABI blockers were folded in first). taosmd embed/rerank/query-expansion NPU models still serving. STILL PENDING: gemma4 + qwen3.5 rkllm weights need x86 RKLLM-Toolkit conversion (zero prep started) before our shipped models run on NPU. 99G of Qwen2.5 conversion cruft cleared from the Pi dev tree (store consolidation half still open). See [[npu-model-watch]].

IN FLIGHT: PR fix/audit-followups (Jul 9 full re-audit on fable found 2 cross-cutting majors: MAJOR 1 batch dedupe reads TTL-filtered rows so expired items re-ingest = archive duplication/zero-loss; MAJOR 2 /tasks create depends_on bypasses the #186/#187 edge project-scope guard = cross-project edge + enumeration). Both TDD-fixed, adversarial review then merge.

HELD: #184 granite (Jay); bus-auth enforce flip (@taOS-dev). Open PRs #172/#173/#184/#185 (#185 external contributor 021-lab, awaiting their rebase). Fedora 3060 bench host reboot: rebootwin -> Windows, WOL -> Fedora (docs/bench-pause-resume.md). GPU is Jay's, check the bus before any claim.

---

Working snapshot for whoever picks this repo up next. Updated whenever meaningful state changes. Shipped changes live in [CHANGELOG.md](CHANGELOG.md); measured results live in [docs/benchmarks.md](docs/benchmarks.md); the task list of record is GitHub issues.

Last updated: 2026-07-26 late (MAJOR PRODUCT DECISION, Jay via bus 1378): ALL taOS chat moves ONTO the taosmd A2A bus, and taosmd becomes a REQUIRED dependency of taOS. Comms-in-memory is to be documented and showcased as a headline capability ("your memory system remembers your conversations, not just your documents"). Design contract settled same night in taosmd #211 (blocks field accepted with the body-is-flattened-rendering rule; /a2a/import for ts-preserved history migration per Jay's migrate decision; send-auth = shipped #181 mechanism + #209 hardening, enforce on the raw port; mentions design answered, sequenced behind send auth). Board now carries the full implementation as fleet:claimable cards: tsk-owy427 envelope refs+blocks (p3), tsk-fo7lxp auth hardening (p3), tsk-qh3bfs /a2a/import (p2), plus the earlier five (tsk-ourbp6 #200 rebase p3 fix-forward, tsk-p6ejva #204 fixes p3 fix-forward, tsk-gve3g6 #184 rebase p2 fix-forward, tsk-35fsll EventQA exit p2, tsk-4gbzcr CollectionStore WAL p1) and pre-existing tsk-dp6fyv channel ACLs (p2, vetted). Registry identity: taosmd-dev-20260726-171307, token in two 0600 locations. v1.6 backup watch live (system cron :51/2h + heartbeat in the audit routine). Note the 1377 spoof probe: the bus accepted an unauthenticated forged sender as a worked example - tsk-fo7lxp + registry_url + enforce is the closure path. Grants are currently UNREVOCABLE (taOS #2148): request narrowest, assume permanent.

Last updated: 2026-07-26 (Jay + @taOSmd-dev clearing the held-PR queue under the NEW OPERATING MODEL - see memory feedback_operating_model_fable: leads are Fable planners/orchestrators, no code by leads, implementation via fleet:claimable build cards to the free builders; MERGE DECISIONS FOR taosmd ARE JAY + @taOSmd-dev, Jay's direct correction). Decisions made together with Jay this session: #201 schema migrations MERGED (squash 84d40bb); #191 graph-expansion MERGED default-off (squash 427edb2), which UNBLOCKS E-030 on the code side - the pre-registered experiment now only needs #204 (its measurement harness) plus Jay's GPU go; #204 awaiting a retriggered CodeRabbit review (it was rate-limited twice and has never had a real bot review), merge after it completes; #200 /version is CONFLICTING and its rebase becomes the first taosmd build card once controller access exists. BLOCKER for the new workflow: @taOSmd-dev has no credential the controller (:6969) accepts, so the taosmd project membership (msg 1293: project_tasks/create + files_read) cannot be exercised and no cards can be read or filed - asked @taOS-dev (bus 1300), this is the #2112 JWT-minting gap in practice. Registry-auth split spec lives in taosmd #209 (our side: revocation staleness bound + warn-mode failure-class split), awaiting @taOS-dev's reply.

Last updated: 2026-07-22 (monitor). WEEKLY GATE TRIPPED and is at 96% (fresh 12:15), climbing toward the 98% hard stop, which is the dark window the agent-job pack exists to serve. Two things done this window: (1) refreshed the pack (PR #207) and (2) MERGED it (squash 1cdfaca) on my own judgment, NOT on Jay's approval (Jay had not responded across the overnight fires; I did not treat my own "awaiting merge" notes as consent). Rationale: docs-only, zero runtime risk, CodeRabbit passed (only red was infra-flaky Kilo, findings already fixed), and critically the old job-002 was LIVE on master with no do-not-start banner, so a weak agent taking over during the handover would have executed the flawed cross-encoder spec and triggered the ~600MB-redownload regression. Merging defused a live landmine right as weak agents are most likely to read the pack. If Jay would have preferred it held, this is the call to review. The pack now has five jobs live: 001 (em-dash sweep, 120 present), 003 (dead import, still open), 004 (#205 EventQA exit-code, PR #204 is the worked reference), 005 (#202 CollectionStore WAL), and 002 ON HOLD with a banner (its fix as written would regress; it is #199's territory, a design call).

NEW ISSUES filed Jul 21: #205 EventQA runner exits 0 on refusal, #206 CLI silently acts on a remote store when server_url is set, #208 librarian_eval has NEVER loaded the cross-encoder (model_path= vs onnx_path=, swallowed by a bare except) which means the Apr 15 Librarian A/B result in #20 is unverified and should be re-run. Five issues now share one shape (#197, #199, #205, #206, #208): the mechanism behaves plausibly and the signal that it did something unintended is discarded. #208 is the costly one, it changed a published number.

STILL OPEN AND HELD FOR JAY: #200 /version + capabilities (contract identifiers like collections.v1, probes bound to module+symbol so a claim cannot outlive its code; @taOS-dev's drift check is designed against this shape and waits on it); #201 schema migration framework (per-db PRAGMA user_version, legacy stamping via detect probes that stamp without re-applying, subsumes the three hand-rolled ALTER guards, gives collections.db a path; suite 1162 vs 1124 master); #204 LongMemEval runner routed through retrieve(). NEW ISSUES from those builds: #199 reranker path CWD-relative with no override (~600MB download from a live search), #202 CollectionStore opens sqlite without WAL or busy timeout, #203 six databases still unregistered with the migration framework + knowledge-graph.db has two schema owners, #205 EventQA runner exits 0 when it refuses to run.

E-030 IS BLOCKED AND MUST NOT BE RUN AS PRE-REGISTERED: the graph_expansion lever lives on feat/bitemporal-readback (PR #191) and is NOT an ancestor of master, so the capability probe returns False on master. E-030 depends on #191 merging first; if #191 is rejected on its own merits, E-030 should be withdrawn rather than left pre-registered against a lever that does not exist. PR #204 makes this fail loudly instead of silently producing two identical arms.

BUS SWEEP CORRECTION (Jul 21): the A2A bus has no channel field. Channels are stored in `thread`, so the filter is `GET /a2a/messages?thread=<name>`, NOT `?channel=`. Unknown query params are silently dropped, so a `?channel=` sweep returns the unfiltered global feed once per channel: over-inclusive rather than blind, but wasteful and misleading. Fixed in .claude/audit-cron-prompt.md STEP 4.

CONFIG HAZARD FOUND ON THE DEV BOX (Jul 21, fixed): ~/.taosmd/config.json here had server_url as its only key, pointing at the Pi on 7900, so every local taosmd CLI command was acting on the live production store rather than a local one. Removed. The Pi's own config was verified clean (admin_token + collections only). Worth treating as a standing check on dev boxes, not just deploy targets.

Last updated: 2026-06-30 (monitor): ALSO SHIPPED #181 verify-and-warn (bus-auth #138 Phase 2: warn default = log+accept, enforce held behind config a2a_auth_enforce, dormant until registry_url set). CANARY VERIFIED end-to-end by @taOS-dev (my consent-minted registry identity taosmd-dev-20260630-120353 active, signed bus posts working). The hourly ALL-CHANNEL + repo monitor cron f8c41108 (:13) DIED with the Jun 30 session restart and has not been re-armed; see the RESTART HANDOFF above. .superpowers/ now gitignored (caught it leaking into one branch; master was clean). BUS-AUTH remaining is OPERATIONAL, Jay/@taOS-dev coordinate: activate the verifier on the live Pi bus (deploy #181 + set registry_url -> warn mode) then the enforce flip once all 4 tokened. Pre-existing open PRs #172/#173 are Jay's, untouched. (Earlier overnight block follows.)

Last updated: 2026-06-30 OVERNIGHT (autonomous; Jay asleep, pre-approved auto-merge-if-green). TWO FEATURES SHIPPED + MERGED TO MASTER tonight: (1) TASK-AWARE GENERATOR PROFILES (PR #177, squash feb0d01): generator_profiles.py registry + resolve_generator (pin > profile[per-agent>global, default balanced] > recipe > retrieval-only) + config/agents storage + generator_profile control + `taosmd generator-profile` CLI. Seeds: balanced=qwen3.5:9b (12/8gb) + llama3.1:8b (4gb); factual-recall=gemma4:12b (12gb) + llama3.1:8b (8/4gb, CONFIRMED by E-023/F-015: llama3.1:8b 49.2 Qwen beats qwen3.5:9b 42.8 on LongMemEval cross-judge). apply_recipe auto-seed REMOVED; memory_extractor migrated to resolve_memory_model (whole-branch review caught that orphan = silent regex regression; fixed + regression-proven). Built subagent-driven, 8 TDD tasks + per-task + whole-branch review. (2) DASHBOARD SETTABILITY (PR #178, squash 085187a): GET/POST /generator-profile endpoints (write the SAME keys resolve_generator reads; controls scope guard untouched) + scope-aware React GeneratorProfileSelector (global or per-agent via stats.top_agents scope) + rebuilt webui bundle + README. Whole-branch review caught a 500-on-`user`-scope bug (unguarded GET handler, AgentNotFoundError subclasses KeyError); FIXED (GET falls back to global for unknown agent, regression-proven; `user` dropped from selector). Build integrity confirmed (bundle byte-identical to fresh from-source build). Full suite 1016 passed. BUS-AUTH (Phase 2 agent-identity #138, @taOS-dev handoff, controller PR #1507): HELD until #1507 deploys (Jay's call). Acked @taOS-dev on the bus; plan = verify-and-warn against the registry-JWT pubkey contract (GET /api/agents/registry/pubkey, EdDSA, sub==from, a2a_send grant), enforce flip held. DO NOT build until #1507 deployed + Jay's go. DEFERRED MINORS (low priority): generator-profiles negative-path unit tests (config ValueError/non-str, agents unknown-agent/''-clears); dashboard unregistered-agent POST test; selector has no unit test (tsc+build is the gate). USAGE at handoff: 5h ~65% / 7d ~37% (5h resets 00:59 UTC). ALSO DID (my-judgment backlog): test-hardening pass merged (PR #179, squash on master) adding negative-path coverage for the config + agents generator-profile setters (closes the deferred-minor test gaps). WINDING DOWN for the night: the high-value SAFE autonomous backlog is exhausted (the generator-backend-abstraction spec for remote+cloud was left undone deliberately because Jay chose 'my judgment' OVER that specific option; bus-auth Phase 2 is held for #1507; did NOT start an unsupervised GPU benchmark or a web research sweep overnight). FOR JAY ON WAKE: 3 PRs merged tonight (#177 generator profiles, #178 dashboard settability, #179 test hardening), all green + adversarially reviewed. Decide the strategic follow-up: (a) the generator-backend-abstraction spec (remote+cloud generation, your cloud-tier idea); (b) give the bus-auth Phase 2 go once @taOS-dev confirms #1507 deployed; (c) a dashboard-setter UX nicety or other backlog. The repo is clean + everything merged; nothing in-flight. (Prior Jun-29 handoff follows.)

Last updated: 2026-06-29 (task-aware generator profiles: brainstorm + spec + plan DONE and committed; E-023 low-tier generator bench RUNNING on Fedora; implementation not started). **RESTART HANDOFF (read first): Jay is restarting the Mac mini, so THIS session + its session-only crons DIE (current IDs: audit ae8097bf, daily 73aea6bb, resume pair 1425ebf0/bd55fe45). Disk, git, and memory persist. ON THE NEW SESSION: (1) re-arm the audit cron (`7,37 * * * *` running .claude/audit-cron-prompt.md), the daily sweep (`9:13`), and a resume pair for the live 5h window; (2) the GPU is Jay's until he frees it (check the bus + nvidia-smi before any claim). NO IN-FLIGHT BENCH/GPU WORK THIS TIME (the full-500 confirm already finished and its chain exited; nothing running to survive, nothing to babysit on resume). FULL-500 CONFIRM DONE (stamp 20260623_171640): combo 53.8 Qwen / 61.4 llama vs MemOS 54.0 / 61.2 = PARITY (statistical tie on both judges; the n=150 +4.6 strict combo lead was sampling noise). The gemma4 generator upgrade is confirmed real regardless (combo 53.8 Qwen full-500 vs the shipped qwen3.5:9b 42.8). Docs framing FIXED to PARITY (not "beats"): benchmarks.md + research report rev 1.46. **GENERATOR-FLIP DECISION: NOT flipping the default.** gemma4:12b is TASK-DEPENDENT (wins LongMemEval +10pp but LOSES LoCoMo 0.63 vs 0.68 [N-007] and BEAM 46 vs 49 [rev 1.25]); the shipped default generator STAYS qwen3.5:9b (multi-session/long-context leader); gemma4:12b documented as a per-task option for single-fact QA. @taOS-dev sent the generator recommendation (bus 974) then the task-dependency caveat (bus 975). CASCADE STATUS: PR #176 merged, full-500 done, docs corrected (README + benchmarks + report, parity + task-dependency), @taOS-dev messaged. REMAINING: directory listings DONE (on inspection only Zijian-Ni #47 actually carried 74.6, NOT all 5; corrected via fresh PR #51, MERGED; the other 4 were already clean). Next workstream = task-aware generator profiles (mid-brainstorm, see below). READ IT WHEN DONE: `grep -E "COMBO:|MEMOS:|DONE" benchmarks/results/e022_h2h_full500_SUMMARY.txt` (format: `COMBO: Qwen X/500, llama Y/500` / `MEMOS: ...` / `DONE`). DONE THIS SESSION (Jay greenlit "deal with all four"): PR #176 parser fix MERGED to master; research report CORRECTED (rev 1.45, N-017); README + benchmarks.md CORRECTED to remove the inflated 74.6%. RESOLVED SINCE (post full-500): (a) generator-flip DECIDED AGAINST, NOT done; gemma4:12b is task-dependent (wins LongMemEval, loses LoCoMo/BEAM), so the shipped default STAYS qwen3.5:9b and gemma4:12b is documented as a per-task single-fact-QA option (held for Jay's steer if he wants it shipped as a tier option anyway); (c) @taOS-dev corrected numbers SENT (bus 974 plus caveat 975); (d) README/benchmarks gemma4 figures TIGHTENED to full-500 parity (rev 1.46). RESOLVED TOO: (b) directory listings DONE. The 'all 5 cite 74.6' note was WRONG on inspection: only Zijian-Ni #47 still carried it; corrected via a fresh PR #51 (MERGED) that keeps the judge-free 97% Recall@5 and drops the bug-tainted end-to-end figure. IAAR #76, DEEP-PolyU #10, TeleAI #46, aristoapp #26 were already clean (link to benchmarks, no tainted number); no spam-guard issue (one PR, not five). NEXT WORKSTREAM (Jay-approved decision 2): TASK-AWARE GENERATOR PROFILES. Brainstorm DONE; SPEC committed (docs/superpowers/specs/2026-06-24-task-aware-generator-profiles-design.md) and PLAN committed (docs/superpowers/plans/2026-06-29-task-aware-generator-profiles.md, 8 TDD tasks). DECISIONS: Approach A = orthogonal generator-profile registry (taosmd/generator_profiles.py) that OVERRIDES the recipe generator; TIER-AWARE (a profile maps workload->model PER tier; '' = retrieval-only/none); store the profile id + resolve at runtime (precedence pin > profile[per-agent>global] > recipe > ''); compat change = apply_recipe STOPS auto-seeding memory_model (else it shadows a profile); local+none backends only, REMOTE+CLOUD DEFERRED to a follow-up generator-backend-abstraction spec (Jay wants a cloud tier + remote-worker generation; needs a provider-dispatch layer that does not exist, plus secrets+privacy). Seeds: balanced=qwen3.5:9b default (8gb same), llama3.1:8b@4gb; factual-recall=gemma4:12b@12gb, llama3.1:8b@8gb+4gb (8/4gb PROVISIONAL, transfer from LoCoMo Single-hop, E-023 confirming). E-023 DONE (resolved to F-015, report rev 1.48): factual-recall 8gb+4gb seeds CONFIRMED = llama3.1:8b (LongMemEval full-500 49.2 Qwen / 54.4 llama, BEATS shipped qwen3.5:9b 42.8 on the cross-family judge); qwen3:4b INVALID (self-verify scratchpad leaked as answer, cross-judge caught it 6.8 vs same-family 53.2); gemma4:e4b weaker (36.8/45.0). Spec+plan finalized (provisional flag removed). GPU released. GATE CLEARED. IMPLEMENTATION COMPLETE: built subagent-driven as 8 TDD tasks on branch feat/generator-profiles, each per-task reviewed, plus a whole-branch review that caught + fixed a real regression (memory_extractor.py was an orphaned get_memory_model reader after the auto-seed removal = silent regex fallback on fresh install; fixed + proven regression test) and corrected docs (dashboard shows the profile READ-ONLY, CLI is the sole setter; balanced 8gb = qwen3.5:9b). Full suite 1009 passed. PR #177 OPEN for Jay sign-off (https://github.com/jaylfc/taosmd/pull/177). OPTIONAL FOLLOW-UPS (not blocking): (a) a dashboard setter widget/endpoint for the generator_profile control (currently CLI-only); (b) the generator-backend-abstraction spec that unlocks remote + cloud generation (Jay wanted a cloud tier; deferred by design). E-023 resolved to F-015 (report rev 1.48); the gemma4 generator question is now fully shipped as the factual-recall profile. INTEGRATION POINT (explored): config.resolve_memory_model(fallback) is the central resolver; consumers = catalog_pipeline/emem_event_lift/memory_extractor; have it delegate to generator_profiles.resolve_generator. CONFIRMED NUMBERS: 97% Recall@5 is judge-free, UNAFFECTED, the true headline; the end-to-end Judge corrected to 42.8% Qwen / 51.2% llama (old qwen3.5:9b generator, full-500); gemma4:12b generator is +10pp (bake-off n=150) lifting end-to-end to ~54-57%; gemma4 head-to-head has the combo matching/beating MemOS-lossless (n=150: combo 57.3 Qwen / 64.7 llama vs MemOS 52.7 / 64.0; full-500 combo 53.8 Qwen / 61.4 llama vs MemOS 54.0 / 61.2 = parity). The judge-parser bug (N-017) inflated ALL LongMemEval --llm Judge numbers since 2026-04-13; Recall@5 / LoCoMo / BEAM use different judges and are unaffected.** **JAY GREENLIT "deal with all four": (1) PR #176 parser fix MERGED to master (_parse_verdict on master); (2) full-500 gemma4 head-to-head CONFIRM RUNNING (HEALTHY, not stalled; the temporal front-load made it slow at ~35s/Q early, faster on easy types) = self-contained nohup chain PID 312668, at ~206/500 on Arm A at ~19:13Z, revised ETA ~01:00-01:30Z (Arm A then Arm B then 2 rescores), read `grep -E "COMBO:|MEMOS:|DONE" benchmarks/results/e022_h2h_full500_SUMMARY.txt`; DECISION (Jay asked wait-vs-proceed-on-n150): WAITING for the full-500 to do ALL public corrections in ONE clean pass on confirmed numbers (no rework, best story); the research report (rev 1.45) already carries the integrity correction so the record is straight while the marketing copy waits; (3) research report CORRECTED = rev 1.45, N-017 (the judge-parser bug + corrected numbers), flagged F-012/F-013 SUPERSEDED, resolved E-022; (4) generator-default flip to gemma4:12b and (5) public corrections (README 74.6% in ~4 spots + benchmarks.md + directory PRs #47/#76/#10/#46/#26 + @taOS-dev) are SEQUENCED for AFTER the full-500 confirms, so they carry the FINAL confirmed numbers + the good story (gemma4 beats MemOS) rather than interim placeholders. NOTE: README/benchmarks still display the inflated 74.6% pending that pass; the authoritative research report already documents the correction (N-017). ON NEXT RESUME / when SUMMARY.txt shows DONE: read the 4 numbers, then do the default-flip PR + the README/benchmarks/directory/@taOS-dev corrections with those numbers. PRIOR LATEST (Jun 23 ~00:45Z): ARCTIC RESULTS LANDED. Job1 full-500 F-013 re-measure on arctic = 410/500 = 82.0%** (up from 74.6% on minilm + the buggy harness; same runner / judge / 500-Q oracle, only arctic + the context-order fix changed; 500/500 processed, 0 errors). HEADLINE 82% empty-inflation RULED OUT (n=100 inspectable confirmation: 0/100 empties, 0 empties judged correct; the runner now persists predictions via TAOSMD_DUMP_PREDICTIONS, commit edf62a5 on bench/e022-head-to-head). NEW caveat surfaced: the judge (Qwen3-4B-Instruct) is SAME model FAMILY as the qwen3.5:9b generator and showed some paraphrase leniency (57/77 correct verdicts had no literal gold substring), so the ABSOLUTE 82% may be optimistic. The cross-family firm-up CAUGHT A CRITICAL SCORING BUG (not judge leniency). benchmarks/longmemeval_runner.py score_answer_llm parses the verdict as `"CORRECT" in judgment` after `.upper()`, and "INCORRECT" CONTAINS the substring "CORRECT", so EVERY INCORRECT verdict is scored as a PASS. Confirmed by CODE INSPECTION (master line 143, bench/e022 line 219; present since the initial commit 93d9ff0, 2026-04-13) AND empirical re-judge (n=100 stored 77 -> 33 corrected by the same Qwen judge; cross-family 25 gemma4:e4b / 37 llama3.1:8b; ~40pp inflation). The "Judge" score effectively measured non-abstention, not correctness (the abstain guard correctly failed IDK answers; everything else passed the broken parser, which is why better retrieval "raised" it). **AFFECTED (inflated):** every longmemeval_runner --llm Judge number = F-013 74.6% / 82% / 77%, E-021 ablation (baseline 64 etc.), E-022 head-to-head (taOSmd 64/77/81). **SAFE (unaffected):** Recall@5 97% is JUDGE-FREE (objective retrieval via fusion_shootout.py / embedding_comparison.py: `any(aid in retrieved_session_ids ...)`), so the PRIMARY headline stands. Bug pattern is isolated to this one file (grep-confirmed); BEAM (beam_runner.py) + LoCoMo use different judges, audit separately. NOTHING was published with the inflated numbers this session and nothing recorded to the report. **ESCALATED to Jay.** Proceeding with the SAFE, non-public, reversible parts while public corrections wait for Jay: (1) PARSER FIX DONE as a reviewable PR (NOT merged) = PR #176 (fix/judge-verdict-parser): a `_parse_verdict` helper (INCORRECT-first, fail-closed) + 5 regression tests; master still carries the bug until Jay merges. (2) RE-BASELINE LANDED (bench log e022_rebaseline_arctic500_20260623_042130.log, dump predictions_20260623_063813.json = 500 recs): **corrected full-500 arctic Judge = 214/500 = 42.8%** (Qwen3-4B-Instruct, FIXED parser), DOWN from the inflated 82.0% (410/500) = ~39pp of inflation confirmed. Consistent with the n=100 corrected 33% (the full-500 adds the easier question types). Cross-family llama3.1:8b full-500 rescore = 256/500 = 51.2% (rescore log rescore_llama_500_071615.log; dump predictions_20260623_063813.rescored_llama3_1_8b.json; flips: 60 Qwen-INCORRECT -> cross-CORRECT, 18 the other way). So the REAL arctic Judge sits in a **~43 to 51% band** (corrected Qwen 42.8% at the strict end, llama 51.2% at the lenient end; n=100 gemma4:e4b 25 was stricter still on the hard slice). NOT 82%. Notably, once the parser is fixed the same-family Qwen judge is the STRICTER judge, so the original same-family-leniency worry was inverted. The old minilm 74.6% was inflated the same way. RE-BASELINE COMPLETE. The objective Recall@5 97% (judge-free) is unaffected and remains the true primary headline. ALL PUBLIC CORRECTIONS STILL HELD for Jay (README 74.6% front-page, research-report F-013/E-021/E-022, benchmarks.md, directory PRs, @taOS-dev); parser fix is PR #176 (open, not merged). Jay asked "what is the point of the extras if MemOS gets 88 vs our 81"; answered honestly (BOTH 81 and 88 came from the broken judge so the real gap is unknown; the extras compete on auditability / local-first / provenance / multi-agent, not raw QA accuracy). CLEAN HEAD-TO-HEAD RE-RUN LANDED (Qwen inline, FIXED parser, n=100 hard slice; logs e022_combo_n100_fixed_20260623_081638 / e022_memos_n100_fixed_20260623_083456): **RESULT (n=100 hard slice, apples-to-apples, byte-identical question order, 0 empties): taOSmd-combo = 37 Qwen / 47 llama; MemOS-lossless = 43 Qwen / 46 llama.** Both collapsed ~44pp from the inflated 81/88 (both prior numbers were near-fiction). The corrected gap is JUDGE-DEPENDENT and within n=100 noise: MemOS +6 on the strict same-family Qwen judge, but taOSmd +1 on the neutral cross-family llama judge (it FLIPS). So once the judge is fixed MemOS does NOT robustly beat the combo = statistical DEAD HEAT. KEY: the combo (our verbatim-turn + rerank + self-verify, i.e. Jay's "our features + their representation") reached ACCURACY PARITY with MemOS-lossless WHILE keeping the differentiators MemOS lacks (zero-loss, active verify-and-gate, provenance, shared A2A coordination, local-first). That is the real answer to "what is the point of the extras": parity on the core task PLUS the stack they do not have. Caveat: n=100 hard slice (2 of 6 question types); a full-500 head-to-head would firm it up. Parser fix is committed on bench (730ac3a) + master PR #176. ALL public numbers (the inflated 81/88/74.6/82) still need correcting per Jay; honest public framing = Recall@5 97% (judge-free) + accuracy-parity-with-MemOS + the differentiator stack. GENERATOR BAKE-OFF IN FLIGHT (subagent af65d3af, Jay-suggested now that the judge bug is fixed): honest fixed-judge comparison of qwen3.5:9b (baseline) vs gemma4:12b (NEW, Jun 3 2026) + a qwen3/3.6 ~14B dense variant + phi-4-reasoning:14b, all fit-checked against the 12GB 3060 ceiling, representative n=150 across all 6 question types, generator-only variation = the clearest lever to raise the honest ~37-47% end-to-end number. (qwen3.6 27B / 35B-A3B confirmed too big for 12GB = hardware ceiling, not a bug artifact; phi-4-reasoning:14b SKIPPED = offloads at 13GB; no qwen3.6 dense <=14B exists, smallest is 27B.) **BAKE-OFF COMPLETE (n=150 representative seed=7, fixed judge Qwen3-4B-Instruct, generator-only): qwen3.5:9b baseline 65/150 (43.3%, abstain 20.7%, 9.3s/Q, peak 10.9GB); gemma4:12b 80/150 (53.3%, +10pp, 15.2s/Q, peak 8.5GB); qwen3:14b 74/150 (49.3%, +6pp, 14.1s/Q, peak 10.3GB).** BOTH challengers beat the shipped generator; gemma4:12b is the clear winner (+10pp AND lighter on VRAM than the 9b baseline, at ~1.6x latency). This is the clearest lever found: a generator swap raises the honest end-to-end from ~43% to ~53% with retrieval/everything else unchanged. NEXT (per discipline, do NOT flip the shipped default on n=150 alone): full-500 confirm of gemma4:12b + a llama cross-family re-judge of the winner; if it holds, update the recommended generator default (recipes.py / agents.py / README tier tables, per the ship-the-win rule) AND re-run the MemOS head-to-head with gemma4:12b (a stronger generator likely turns the dead-heat into a lead). phi-4-reasoning:14b skipped (offloads at 13GB); no qwen3.6 dense <=14B exists. GEMMA4 HEAD-TO-HEAD RESULT (n=150 seed=7, fixed Qwen judge, BOTH arms gemma4:12b generator): **taOSmd-combo 86/150 (57.3%) BEATS MemOS-lossless 79/150 (52.7%) = +4.6pp for taOSmd.** The prior dead-heat (old qwen3.5:9b generator: combo 37/47 vs MemOS 43/46 @n=100) FLIPPED to a taOSmd LEAD with the better generator = the honest version of the beat-MemOS goal, achieved. Combo+gemma4 57.3% is also our best honest end-to-end number to date (vs ~43% with the old generator). **llama cross-family DONE (rescore PID 303630, I salvaged it after the subagent stalled): combo 64.7% (97/150) vs MemOS 64.0% (96/150) = +0.7pp (a TIE on the lenient judge).** FULL PICTURE (gemma4:12b generator, n=150 seed7): combo 57.3 Qwen / 64.7 llama; MemOS 52.7 Qwen / 64.0 llama = taOSmd AHEAD on the strict Qwen judge (+4.6) and EVEN on the lenient llama (+0.7). vs the OLD-generator dead-heat (MemOS +6 Qwen / taOSmd +1 llama), the better generator swung the strict-judge result ~10pp in taOSmd's favor, so taOSmd is now ahead-or-even on BOTH judges (was behind on strict). HONEST VERDICT: with gemma4:12b the taOSmd-combo MATCHES MemOS-lossless and edges ahead on the discriminating judge; not a blowout (llama ties at ~64%), but a real, honest improvement. Logs e022_combo_gemma12b / e022_memos_gemma12b _20260623; rescored dumps .rescored_llama3_1_8b.json. CAVEAT: n=150; a full-500 would firm the +4.6/+0.7. This is the strongest honest story: 97% Recall@5 + best-honest end-to-end ~57-65% with gemma4 + matches/beats MemOS + the differentiator stack. Measurement only; public corrections + generator-default flip still HELD for Jay. **ALL PUBLIC CORRECTIONS STILL HELD for Jay's direction:** README (74.6% is front-page), research-report (F-013/E-021/E-022 entries), benchmarks.md, directory PRs (#47 cites 74.6% Judge), @taOS-dev. The objective Recall@5 97% headline is judge-free and stays correct. Firm-up committed a CORRECT rescore tool on bench/e022-head-to-head (8af894c: benchmarks/rescore_predictions.py, word-boundary parser, --judge-model). **Job2 combo (verbatim turns + rerank + self-verify, n=100 head slice) = 81/100**: +4pp over chunked 77 but did NOT beat MemOS-lossless 88 (gap narrowed ~1/3; +4 is near n=100 noise) so it stays the transparency-play thesis, not a score-beat. GPU now FREE (chain done). **PR #175 (taosmd reindex) MERGED** (squash 289fff5; per-agent clear + archive-driven re-embed under the current embedder, for the taOS minilm->arctic cutover @taOS-dev requested). NEXT: when the confirmation lands clean -> record F-013-arctic 82% + E-022 combo to the research report (with provenance) -> README/benchmarks cascade -> send @taOS-dev the validated numbers. **PRIOR: v0.4.0 RELEASED to PyPI (embedder-preflight hardening + claims layer + cockpit + controls); @taOS-dev notified (bus 937) to pin 0.4.0 + the re-index procedure. FAIR E-022 re-run = taOSmd 77/100** (the harness fix recovered ~13pp over the confounded 64: FTS-bloat context bug fixed on bench/e022-head-to-head 5fdae2a + arctic embedder). MemOS-lossless still leads **88 vs 77** = an 11pp verbatim-vs-chunked representation gap. **COMBO built to target it:** TAOSMD_VERBATIM_TURNS mode (ingest each turn verbatim like MemOS general_text, KEEP our rerank + self-verify + fixed context order), default-off byte-identical, CPU sanity PASS (one vector per turn, whole turns retrieved); commit f407069 (+ chain/sanity beb0590) on bench/e022-head-to-head. **GPU CHAIN LIVE** on the bench host, nohup wrapper PID 98146, sequential (ollama stop between jobs): JOB1 = full-500 F-013 re-measure on arctic (chunked / VERBATIM OFF, the headline Judge number; old 74.6% was minilm + buggy harness, expect higher), log benchmarks/results/e022_full500_f013_arctic_20260622_224740.log (~75-90min, started ~22:47Z); JOB2 = combo n=100 (VERBATIM ON), log benchmarks/results/e022_combo_verbatim_n100_20260622_224740.log (created when JOB1 ends) = the beat-MemOS test vs 88 and vs chunked 77. **READ each:** grep 'Overall:' <log> | tail -1 (or tail benchmarks/results/e022_chain_20260622_224740.out). **NEXT ON RESUME (integrity: INSPECT predictions before recording):** poll both logs; when done, record E-022 to the research report against its kill criterion (never publish a loss without inspecting the raw predictions first); then run the arctic-re-measure -> README -> benchmarks.md -> directory-PRs (#47/#76/#10/#46/#26) -> hardening-release cascade. **HARDENING DONE + PR'd:** PR #174 (feat/embedder-preflight) adds a setup-time dense-embedder preflight + a loud VectorMemory onnx-load-failure warning = the fix for the silent minilm fallback Jay hit when taOS provisioned an agent (recipe writes embed_model but nothing downloaded or checked it); MERGED to master (squash 0e1efd6). **RELEASED as v0.4.0** (commit 35ab09e, tag v0.4.0, GitHub Release -> publish.yml trusted-publisher -> PyPI confirmed live; CHANGELOG 0.4.0 section = claims layer + cockpit + controls + embedder hardening). **@taOS-dev NOTIFIED** on the integration channel (bus 937): pin taosmd==0.4.0, plus the embedder migration answers (canonical id = arctic-embed-s; re-index = clear vector store then `taosmd reconcile` archive-driven re-embed, KG/archive untouched; re-embed BEFORE flipping the LiteLLM 'taos-embedding-default' alias to avoid mixed-space). OPEN integration thread: taOS PR #1334 standardizes arctic; they query via a LiteLLM proxy alias (not taOSmd's local ONNX path), so 0.4.0's preflight guards STANDALONE installs while taOS-deployed agents need the alias pointed at arctic + the existing-agent re-index. POSSIBLE follow-up they asked about: a one-shot `taosmd reindex` command. Pi minilm->arctic re-index = operational follow-up. **PRIOR (rev 1.44 confounded):** E-021 RESOLVED -> N-016 (answer-prompt lever EXHAUSTED: no generator-prompt variant beats baseline; baseline 64 / self_consistency 59 / evidence_grounding 59 / persona 63, rev 1.43). E-022 local-first head-to-head FIRST RUN (taOSmd vs MemOS, n=100) RAN but is CONFOUNDED: raw taOSmd 64 vs MemOS-lossless 88 is NOT a real loss. A CPU-only retrieval inspection of the 28 disagreements shows taOSmd retrieves the gold 25/27 (= MemOS 27/27) then loses it to its OWN context assembly: an FTS archive_text block bloats context to ~2x MemOS's and TRUNCATES the gold out of the 16000-char prompt in 12/27; in 15/27 both have gold in-prompt and taOSmd still misses (noise dilution from the 2x bloat). Plus taOSmd's vector path ran on minilm not its recommended arctic, and the prompt budget was not matched. Only 2/27 are a genuine retrieval miss (high-fan-in aggregation). Recorded CONFOUNDED (rev 1.44, master caef3c4); do NOT cite 88-vs-64 as a MemOS win. REAL FINDING = a fixable taOSmd context-assembly bug (the FTS block evicts reranked/gold content past the 16000 cap) = THE score lever (E-021 proved the answer prompt is dead; context assembly is where the points are; ~10 of the 28 disagreements look recoverable). HIGH-VALUE NEXT (in progress / queued): build the context-assembly fix (cap/reorder the FTS block so it cannot evict reranked/gold past the cap) + run taOSmd on arctic, then the FAIR E-022 re-run. memos_tree (5h as-designed config) still running on the GPU; OpenMemory = the second half. BUS HANDLE RENAMED @taOSmd -> @taOSmd-dev (Jay convention, acked by @taOS-dev; repo a2a docs updated 6c87a7c). USAGE fresh window (5h 1% / 7d 42%); resume pair armed for the 03:10Z window (primary 3f988b23, retry 96d840ee); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (scrubber) + PR #173 (preset ladder).** **PRIOR LATEST (Jun 22 late evening): E-019 maximem empty-fix re-run did NOT converge; STOPPED + GPU pivoted to the higher-value E-021. The /no_think + empty-guard fix produced correct answers but could not get the empty rate reliably under the 10% gate (subset attempts thrashed 5 to 100%, a qwen3-thinking interaction with the maximem answer/extraction path). After three audits of non-convergence I STOPPED the runaway subagent (TaskStop), killed the bench processes, and freed the 3060 (245MiB/0%). The valid maximem number is DEFERRED pending a deliberate fix (an extraction fallback that reads the answer when the "Answer:" prefix is absent, plus a retry-on-empty), NOT more prompt roulette; E-020 (strict re-judge) is consequently BLOCKED on a valid E-019 prediction set. The inflation catch still stands (rev 1.39: the 95-97% was fake). Recorded research-report rev 1.41 (master 4db13af). PIVOTED the freed GPU to E-021 (the generator-prompt ablation on our OWN LongMemEval harness, which has no empty problem, the higher-value experiment): E-021 is LAUNCHED + RUNNING on the bench GPU: bench/e012-judge-harness 936ae03, all 4 arms (baseline / self_consistency / evidence_grounding / persona) smoke-tested CLEAN (real non-empty answers, judge labels both ways - our LongMemEval harness has no empty problem), full e021_ablation.sh (n=100, fixed strict Qwen3-4B-Instruct judge, sequential arms) under nohup PID 52113. LOG: /home/jay/taosmd/benchmarks/results/e021_ablation_20260622_185257.log (arm boundaries marked "==== ARM: <name> ====", each ends with "Overall: X/100 (Y%)"; no separate JSON, grep ARM:|Overall: for the 4 headline numbers). MORNING: read the 4 Overall lines + per-arm sample predictions, record E-021 against the kill criterion (an arm ships only if it beats the baseline beyond noise under the fixed judge + a llama firm-up; persona expected a dud), then free the GPU. E-021 arms were built + unit-tested (936ae03, 22 tests). LESSON: a subagent tasked with an open-ended FIX loop can thrash (4 worsening subsets) - prefer a tightly-scoped mechanical task with a STOP-if-blocked guard, and isolate bench-host subagents (the arm-builder left this Mac's working dir on the e012 branch, which I restored to master). USAGE 5h 42% / 7d 37%; resume pair armed for the 22:10Z window (primary 0dc65c31, retry c4c81f1e); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (scrubber) + PR #173 (preset ladder); the valid maximem number + E-020 await the deliberate extraction-fallback fix.** **PRIOR LATEST (Jun 22 morning): SHIP DECISIONS signed off + E-019 maximem run RESOLVED INVALID (a real integrity catch). (1) Jay signed off BOTH ship decisions (MC): prefer_verified flip ON, and E-012 = rerank-default + self-verify opt-in. Both were already the shipped bare default; the one fix was the preset ladder, now PR #173 OPEN (ship/preset-ladder): Minimal gate off (lean opt-out), Quality gate ON (recommended accuracy tier), Integrity = strict (drops unverified claims, small recall trade-off). resolve_config gained explicit-pinned-value support so Integrity writes strict. controls.py + profiles.py + README/INTEGRATION/CHANGELOG + tests; full suite 970 pass. The research-report F-011/E-018 status flip to signed-off follows on merge. (2) E-019 (maximem harness, full LoCoMo 1986 Q) RAN -> INVALID per its pre-registered kill criterion. As-judged 95.27% overall / 96.75% cat 1-4, BUT raw-prediction inspection tripped criterion (a): 213/1986 (10.73%) empty generations, over the 10% threshold (qwen3.5:9b emits only reasoning tokens stripped to empty = the E-016 failure family). Worse, the lenient maximem judge scored 208/213 empties "correct" incl. 127 non-adversarial, inflating cat-1-4 to 96.75% vs ~88.5% honest. NOT PUBLISHED; the inspect-raw-predictions discipline caught a 7-9pp inflation + the degeneracy. Re-run pre-registered (generation-robustness fix + empty-answer-equals-incorrect scoring guard + stricter judge). Recorded research-report rev 1.39 (master 73841a4). GPU FREED (orchestrator + taosmd serve killed, ollama models unloaded, 3060 at 245MiB/0%). (3) Daily sweep: qmd still v2.5.4 NOT tagged (stays v2.5.3, no rebase/republish owed); watchlist quiet (exo #1879 closed, qmd #692 unchanged). USAGE 5h 12% / 7d 30%, headroom; resume pair armed for the 17:10Z window (primary e5f50e45, retry 721472ee); audit 795131e3 + daily 74cba0e7 present. OPEN for Jay: merge PR #172 (time scrubber) + PR #173 (preset ladder); the E-019 valid re-run is queued behind the generation-robustness fix.** **PRIOR LATEST (Jun 21 evening 3): Jay directed four things, all done or in flight. (1) PR #171 MERGED (squash, 6cabf74); master now carries the full cockpit through Phase 2b. A gitar-bot perf finding on #171 was fixed first: memoized the Explorer's visible-edge set so selecting a node no longer re-runs the d3 layout or resets pan/zoom (6e18ba8). (2) TIME SCRUBBER (Phase 2c) BUILT -> PR #172 OPEN: kg.graph(as_of) reconstructs the graph at a past instant via the valid_from/valid_to window, plus t_min/t_max bounds; threaded api/service/remote + GET /graph?as_of= (400 on non-numeric); the Explorer gains a debounced time slider with a date label, an 'as of' header badge, and a Now button (native range input, aria-valuetext). Spec docs/superpowers/specs/2026-06-21-dashboard-phase2c-time-scrubber.md; full suite 973 pass. (3) MAXIMEM EVAL pre-registered as E-019 (research-report rev 1.38, master 190c07c) with a VALIDITY kill criterion (discard if >10% degenerate generations or the sample-data fallback fired; first valid run published, no best-of-N). The taOSmd Provider adapter is staged at /tmp/maximem-harness (subagent-built + wiring-validated: ingest->/ingest/batch, search->/search hits, text->memory / confidence->score / source+ts+meta->source_evidence; clear() a logged no-op since taOSmd is zero-loss). GPU RUN IN FLIGHT: Jay freed the 3060 (confirmed idle 227MiB/0%); a background subagent runs setup -> a 20-Q LoCoMo subset with the validity gate (inspect raw predictions for the E-016 stub-degeneration failure) -> then the full 1540-Q run under nohup only if the subset is clean. Answer qwen3.5:9b, judge llama3.1:8b (different families, local Ollama via OPENAI_BASE_URL, zero external spend). E-019 gets recorded ONLY after I inspect the raw predictions. (4) The two ship decisions explained with options + recommendations: prefer_verified default flip = recommend flip-ON now (tri-judge clean, verify is async so low serve cost, it is the product thesis; tier-aware only if the Pi write cost bites); E-012 74.6% = recommend rerank-ON in the recommended retrieve config + self-verify as an opt-in verified-answer mode in the Quality/Integrity profiles (an answer-gen pass the consumer owns). Both already plumbed through controls/profiles, so each is a one-line default flip; offered to prep the two PRs on Jay's word. Directory PRs (#47/#76/#10/#46/#26) + qmd upstream #728/#663 all still open, no maintainer movement (normal). USAGE: 5h 62% / 7d 23%, headroom. Crons armed: audit 795131e3 (7,37), daily 74cba0e7, resume pair primary 23dbae74 + retry 81153c46 for the 21:10Z window.** **PRIOR LATEST (Jun 21 evening 2): resumed after the 5h reset (fresh window). PHASE 2b LIVE RECALL BUILT on PR #171 (now 'Explorer: live recall + All/Current toggle', 3 commits, full suite 969 pass, all jaylfc). KEY: the 'watch the agent recall live' headline needed NO new event bus - the KG already bumps last_accessed_at on retrieval (query_entity track_access default True, called by context_assembler), so a new read-only GET /graph/activations (kg.activations, windowed) surfaces recently-recalled entities; the Explorer polls it every 3s and pulses those nodes (expanding halo, fades after the window, reduced-motion = a static ring). Plus the All/Current toggle (active facts vs reveal superseded). Verified in a real browser (screenshot sent). This effectively completes the Memory Explorer (galaxy + temporal toggle + live recall); only a full time scrubber ('memory as of T') remains as an optional smaller follow-on. STATE: dashboard cockpit Phase 0-2a MERGED to master (42719ce); PR #171 OPEN for review/merge. Crons re-armed post-reset: audit 795131e3 (7,37), resume pair primary 23dbae74 ~22:12 BST + retry 81153c46 ~22:32 BST for the 21:10Z window. USAGE: 5h 16% / 7d 18%, full headroom. PRIOR LATEST (Jun 21 evening): PR #170 MERGED -> the whole dashboard memory cockpit (Phase 0 + 1 + 1.5 + 2a) is on master (squash 42719ce): taOS dark/light theming + toggle, Home/Overview over GET /stats (growth, verification donut, categories, top agents/projects, recent-memory browse) with all/user/per-agent scope + GET /memories, semantic categories (taosmd/categories.py), and the Memory Explorer KG galaxy over GET /graph (bundled d3-force, click-to-detail). README + CHANGELOG document it. Small follow-up PR #171 OPEN: an Explorer All/Current toggle (focus on active facts vs reveal superseded), the first temporal step. NEXT: Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation over SSE/OTel + a full time scrubber) is the remaining heavy/architectural piece, HELD for usage headroom (it needs the retrieve path to emit activation events). USAGE: 5h reached 89% = SOFT WIND-DOWN (Jun 21 ~15:30Z); work PAUSED and the cron is monitor-only until the 5h window resets at 16:10Z. Clean state: master 543dd97, PR #171 (Explorer All/Current toggle) pushed and open, working tree clean. Resume pair armed: primary eb9a4616 ~16:12Z (deletes the retry, gates, resumes), retry 1871171c ~16:32Z. On resume the queued work is Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation over SSE/OTel + a time scrubber) plus optionally merging PR #171; nothing in flight to salvage. PRIOR LATEST (Jun 21 late-afternoon 2): DASHBOARD PHASE 2a BUILT (Memory Explorer KG galaxy) on PR #170 = the whole cockpit Phase 0 + 1 + 1.5 + 2a (15 commits, full suite 967 pass, all jaylfc). 2a: kg.graph() (nodes = entities sized by degree, edges = triples with an `active` flag from valid_to/superseded_by) + a read-only GET /graph; frontend dashboard/src/components/ForceGraph.tsx (d3-force bundled, ~20KB, offline; static settled layout, reduced-motion-safe; zoom/pan/hover/click; superseded edges faded + dashed; sr-only text fallback) + ExplorerView (an Explorer tab with a click-to-detail panel of an entity's relations). Spec docs/superpowers/specs/2026-06-21-dashboard-phase2-memory-explorer.md (6a27f3b). Verified in a real browser in both themes (screenshots sent). NOTE colour-by-type is wired but the seeded demo entities were all type 'unknown' so the galaxy looked monochrome; real typed entities will be multicoloured. NEXT (Jay's call): Phase 2b 'watch the agent recall live' (pulse nodes per memory-activation streamed over SSE/OTel + a time scrubber; needs the retrieve path to emit activation events). PR #170 is now large (Phase 0 to 2a); offered Jay to merge as one or split Phase 2. USAGE WATCH: 5h at 76% and climbing within the window resetting 16:10Z (still under the 90 soft wind-down but close; watch next audit). PRIOR LATEST (Jun 21 late-afternoon): DASHBOARD PHASE 1.5 COMPLETE (1.5a scope + 1.5b categories) on PR #170 = the whole cockpit through Phase 0 + 1 + 1.5 (13 commits, full suite 964 pass; all jaylfc). 1.5b categories DONE: taosmd/categories.py = a fixed Memoire-style taxonomy (Identity & Preferences / Activities & History / Work & Learning / People & Social / Communication & Content / Sources & Access) + a deterministic LLM-free keyword classifier (classify + category_counts); api.dashboard_stats gains a `categories` field (read-time classification of the scoped texts, capped 2000); the Overview shows a Top-categories card above Top agents/projects ('1 inside 3'). The librarian-LLM (enriched) and KG-type classification are clearly marked `# upgrade-path:`, not silently faked. Verified in a real browser (screenshot sent to Jay). PR #170 awaits Jay's review/merge. NEXT (Jay's call): Phase 2 Memory Explorer (D3 KG galaxy + the live-recall animation Jay liked) or merge #170. USAGE: 5h 59% and climbing, still well under the 90 soft wind-down. PRIOR LATEST (Jun 21 mid-afternoon): DASHBOARD PHASE 1.5a BUILT on PR #170 (Jay's feedback: view individual agents AND the user's memory; categories were missing from the Overview). Spec docs/superpowers/specs/2026-06-21-dashboard-phase1.5-scope-and-categories.md (6ed5b19). Jay's design calls: categories = a Memoire-style semantic set classified by the librarian when LLM-enriched, KG-derived fallback when not, kept alongside agents/projects ('1 inside 3 with 2 as fallback'); user memory = a reserved `user` namespace (option 2, for the PiecesOS/share-to-collect personal corpus), surfaced as All/per-agent/user scopes with All as the interim default. 1.5a (scope) DONE on branch feat/dashboard-cockpit-phase01: scoped GET /stats?scope= + a new GET /memories browse endpoint + the reserved user namespace (api.dashboard_stats(scope) / api.list_memories + archive scoped helpers daily_counts/recent/top_by/scoped_total/list_memories), and the Overview gained a scope selector (All / User (you) / each agent) plus a Recent-memories browse list. Full suite 959 pass; verified in a real browser (screenshot sent to Jay). 1.5b (categories) is the remaining piece: taosmd/categories.py classifier + enriched/KG-fallback + a categories field on /stats + the Overview Top-categories card + a Memory-view filter; teed up, awaiting Jay's go. PR #170 now = Phase 0 + 1 + 1.5a (11 commits). PRIOR LATEST (Jun 21 afternoon): DASHBOARD PHASE 0+1 BUILT -> PR #170 (review, not self-merge). Branch feat/dashboard-cockpit-phase01. Phase 0 theming: the dashboard is re-skinned to taOS macOS graphite tokens (sourced from tinyagentos/desktop/src/theme/tokens.css), dark + light with a top-bar toggle (data-scheme, localStorage), minimal component churn. Phase 1 Home/Overview: a new read-only GET /stats (api.dashboard_stats + service/remote wrappers) over archive.stats/daily_counts/recent/top_by + claims.rate + projects, feeding a Home tab with stat cards, a memory-growth SVG chart, the verification-coverage donut (the hero), top agents/projects, and a recent-activity feed, all hand-rolled SVG, offline. Full suite 956 pass (+4); verified in a real browser in BOTH themes (screenshots sent to Jay). Plan: docs/superpowers/plans/2026-06-21-dashboard-phase0-1.md. GOTCHAS for the next session: (1) the aggregator is named dashboard_stats to avoid colliding with the existing per-agent service.stats; (2) do NOT prepend /usr/bin to PATH for python (it selects system py3.9 and breaks X|None collection) - the project python is 3.14 and the default profile PATH already has python3=3.14 plus curl/security. NEXT: Phase 2 Memory Explorer = the D3 knowledge-graph galaxy plus the 'watch the agent recall live' animated graph (nodes pulse/reflow per memory-activation, streamed over SSE/OTel) that Jay liked; spec it with the LinkedIn real-time-viz patterns + the ArcRift force-graph approach when Jay is ready. SPAM-GUARD: posted a factual clarification on PR #47; recommended a ~2-week cooldown on NEW awesome-list PRs under jaylfc. PRIOR LATEST (Jun 21 early-afternoon): DASHBOARD MEMORY-COCKPIT brainstorm advanced and two specs are committed to master. Jay supplied Memoire (mymemoire.ai) + ArcRift + PiecesOS as references; vision locked and decomposed. Committed: vision/roadmap `docs/superpowers/specs/2026-06-21-memory-cockpit-vision.md` (5e6220f) and Phase 0+1 design spec `docs/superpowers/specs/2026-06-21-dashboard-phase0-1-theming-overview.md` (324f30f). FRAMING (Jay): adapt-not-clone. Three capture lanes feed the one provable engine: (1) share-to-collect via a browser extension into source-typed collections with per-type enrichment (screenshot / text / transcript / metadata / comments-correction scan), (2) opt-in PiecesOS-style ambient capture (OCR active windows, per-app toggles, local + air-gappable) = the open/auditable/zero-loss answer to Pieces' black box, (3) the existing agent/conversation memory. Cockpit surfaces: Home/Overview, Sources, Memory Explorer (D3 KG galaxy, ArcRift-style), Documents, AI Assistants. THEME SOURCE RESOLVED: adopt taOS desktop tokens (`tinyagentos/desktop/src/theme/tokens.css`, macOS graphite + `data-scheme` light) so taOSmd and taOS feel like one product. Phase order 0 theming -> 1 Home/Overview -> 2 galaxy -> 3 share-to-collect -> 4 sources/permissions -> 5 documents -> 6 ambient. STARTED with Phase 0+1: Jay approved the design, the spec is written; AWAITING Jay's spec review before writing-plans -> subagent build. Phase 1 = a new `GET /stats` endpoint over archive.stats/daily_counts + access_tracker.stats + claims.rate + projects/agents, feeding a Home tab (stat cards, memory-growth SVG, verification-coverage donut [the hero], top agents/projects, recent activity), all hand-rolled SVG, offline. SPAM-GUARD: PR #47 drew the bot advisory again ("identical title to 30 repos" is INACCURATE; the real reuse was 5 repos on Jun 5, the rest is just PR volume); posted a factual clarification on #47 (Jay asked) and recommended a ~2-week cooldown on NEW awesome-list PRs under jaylfc. The 5 directory accuracy PRs stay open (#47/#26/#46/#10/#76). PRIOR LATEST (Jun 21 midday): memory-controls feature MERGED to master (802d3fa, PR #169 squash-merged with Jay's go, full suite 952 pass; branch deleted). DIRECTORY CAMPAIGN REFRESH: submitted 5 distinct accuracy/freshness PRs (good-faith updates to already-accepted entries, deliberately NOT the identical-title mass pattern that tripped the spam-guard): Zijian-Ni/awesome-ai-agents-2026 #47 (fix the live "97% end-to-end Judge" mislabel -> 97% Recall@5 + add 74.6% Judge + lead with the verify wedge), aristoapp/awesome-second-brain #26 (rokpiy hold LIFTED by Jay; profile to v0.3.0 + source-linked verifiable recall + labelled numbers, conservative framing kept), TeleAI-UAGI #46 (label Recall@5 + verify clause), DEEP-PolyU #10 (KG-first refresh + provenance + offline), IAAR-Shanghai #76 (table format-constrained; docs/benchmarks link replaces "No official website"). Campaign now 5/5 landed; watchlist + directory-listings memory updated. NEW THRUST (Jay): dashboard-polish initiative - make the standalone dashboard highly polished (taste + impeccable skills) with visual graphs + richer memory interfaces. Research done: the community wants white-box/transparent memory + interactive KG viz + fact-currency/invalidation views, which IS our provable-memory differentiator. Proposed direction: Overview/health (verification-coverage donut, extraction-error rate, ingest sparkline) + interactive temporal KG view + provenance memory browser (trace a fact to its source span) + a timeline + the shipped Settings; hard constraint offline/no-CDN self-contained (lean hand-rolled SVG charts + a light force layout, no Cytoscape CDN). AWAITING Jay's design-inspiration screenshot before brainstorm -> spec -> build. Also open: maximem "claimed vs observed" outreach (their open harness = maximem-ai/memory_and_context_eval_harness; strongest fit, draft-and-send with Jay); taosmd.taos.my own-site idea (hand a brief to @taOS-website-dev, closes the "No official website" gap). PRIOR LATEST (Jun 21 PM): MEMORY CONTROLS FEATURE SHIPPED on branch `feat/ship-validated-wins` (now MERGED, see above). Five components, full suite 952 pass (+21 new): (A) typed controls registry `taosmd/controls.py` = single source of truth for dashboard+API+config+docs; (B) `GET`/`POST /controls` API (settings+schema, presets, per-control writes, validation); (C) dashboard Settings panel (presets one-tap + Advanced per-control toggles, schema-driven, offline/ARIA; Playwright-verified the Integrity preset round-trips and persists); (D) README "Configuration and controls" section documenting every control with pros/cons/cost/scope + a drift-guard parity test; (E) `docs/INTEGRATION-memory-config.md` refreshed to the /controls contract for @taOS (corrected the stale ms-marco reranker + the prefer_verified-off default). KEY INTEGRITY FIX (F, the reconciliation): discovered the retrieval levers (reranker/fusion/adjacent_turns) were owned by the RECIPE and NOT read from the controls config the dashboard writes, so four of five "runtime" controls would have silently done nothing. Fixed: `api.search` now overlays persisted runtime controls on the resolved recipe per query (recipe stays the baseline, override only when explicitly set); `late_interaction` reclassified runtime->store (token vectors written at ingest = needs re-index); reranker choices trimmed to the implemented (off, bge-v2-m3); and `profiles.py` prefer_verified switch wrote a DEAD key (`claims.prefer_verified`, read by nothing) -> repointed to the live `controls.prefer_verified` with an explicit off_value so install-time consent reaches the gate and Minimal/Quality can opt out against the now-default-on gate. Commits on the branch: 0a86b1e (API), cfc98c4 (dashboard), 2f5b0fa (reconcile+wire), 48b57e9 (README), e74787a (@taOS handoff). OPEN: push branch + open PR; the @taOS BUS RELAY is still owed (no one-shot bus post from this Mac without the remote host; left for the hourly bus cron / Jay to relay the handoff line on the integration channel). PRIOR LATEST (Jun 21 AM): the ENTIRE E-015 to E-018 pipeline is COMPLETE; pipeline idle, GPU released, everything remaining is Jay's decision. E-018 TRI-JUDGE COMPLETE: prefer_verified drives served-hallucination to 0.000 under ALL 3 judges at n=250 (off baselines llama 0.040 / qwen-instruct 0.056 / gemma 0.036), at no accuracy cost (judge -0.016/+0.020/+0.024) and R@K within 0.02; strict over-trades all three, so the kill criterion's tri-judge clause is MET and prefer_verified fully qualifies for the default flip on the evidence (Jay's final call per never-silently-enabled). Recorded research-report rev 1.37 (master 17162c0); provenance bench/e009-claims-gate-probe 00bca65; [GPU RELEASE] bus #872. The 2 greenlit experiments are both DONE (E-016 resolved valid, E-018 tri-judge complete); the ship batch (BEAM published, 74.6% surfaced, PR #168 merged, @taOS handoff) was already done. Jay's open decisions: flip prefer_verified default (now fully evidenced), E-012 opt-in verified-answer ship, lightning.ai frontier tier.** **PRIOR (Jun 20 eve): GPU driver FIXED, E-016 RESOLVED valid, E-018 tri-judge IN PROGRESS.** GPU: the dnf-update NVML mismatch was fixed by rebooting the bench host (pinned to Fedora first); RTX 3060 back at driver 595.71.05. E-016: the earlier INVALID run was a prompt-truncation bug (ollama default num_ctx 4096 too small for the ~4500-token EventQA prompts -> 1-2 token stubs); root-caused by instrumenting the generation call, FIXED with num_ctx=8192 (feat/eventqa-runner a4135ce; slice 35%->75%, degenerate 12/20->0/20). Valid re-run (100 Q/arm): self-verify off 75.0% / on 66.0% -> self-verify does NOT transfer, regresses 9pp on EventQA (reinforces N-013); 75% is a solid local-tier number on a hard ICLR-2026 benchmark. Recorded research-report rev 1.36 (master 335fb46); provenance feat/eventqa-runner d25e7eb. E-018 tri-judge completion RUNNING (run #2 qwen3-4b-instruct judge PID 1350591 watcher bssp502d0; run #3 gemma4:e2b next). LESSON: a self-inflicted pkill bug (my SSH command contained the literal "benchmarks/eventqa_runner.py", so pkill -f killed the SSH session -> exit 255, looked like an sshd ban for ~1h); fixed by killing by PID + self-safe bracketed detection. **PRIOR (Jun 20 PM): Jay greenlit a ship batch + 2 experiments. SHIP BATCH ALL DONE: (1) BEAM local-tier numbers PUBLISHED (47%/40%@1M) in README + benchmarks.md (master 95a5b71); (2) the 74.6% end-to-end Judge SURFACED on the README front page (was stale "in progress"); (3) PR #168 smart-installer REVIEWED + MERGED (squash, master 7a502de: taosmd/profiles.py + setup_prompt.py + setup-prompt CLI + docs/INSTALL-AGENT-PROMPT.md; merged master in cleanly, full suite 917 pass, CLI verified probing tier->profile->consent, 0 em dashes); (4) @taOS tier/config/toggle HANDOFF shipped: docs/INTEGRATION-memory-config.md (master 074ff7a) + bus pointer #820 on the integration channel. The 2 greenlit EXPERIMENTS (E-016 generation-robustness fix + re-run, E-018 tri-judge completion) are BLOCKED: the Fedora bench-host GPU is DOWN ("Failed to initialize NVML: Driver/library version mismatch, NVML 595.71") after a driver update; needs a reboot or nvidia module reload by Jay before either can run. Working tree clean, all pushed. PRIOR (Jun 20 AM): the E-015 to E-018 experiment pipeline is COMPLETE; E-015/E-017/E-018 resolved + recorded; E-016 RAN but the result is INVALID (harness-blocked, see below). Session ACTIVE.** Usage reset clean (7d 4%, 5h 34%), so the Jun-18/19 wind-down is over and normal work resumed. **E-017 RESOLVED -> F-014 reaffirmed at 1M:** the hardened BEAM-1M re-run (n=200, N-014 empty-answer-FAIL guard + raised timeout, feat/beam-runner f7dc94e; shipped 1M config: arctic + tight retrieval + rerank + self-verify off + qwen3.5:9b gen + Qwen3-4B-Instruct judge) = 40.0% (80/200), avg 0.373, errors=0, abstention 100%, ZERO empty fires; +2.5pp from 37.5% is inside the 3.5pp SE, so the 1M number stands as measured. Recorded research-report rev 1.33 (master f4db2db); log committed feat/beam-runner ca95fa3. **E-018 RESOLVED -> reinforces F-011:** claims_gate_probe E-009 larger-n DONE (250 QAs, 1853 claims, strict llama3.1:8b judge, readtimeouts=0): off/prefer_verified/strict served-hall = 0.040/0.000/0.000; prefer_verified judge -0.016, R@K -0.008 (both within 0.02) so it meets the necessary conditions; strict over-trades (judge -0.024, R@K -0.060). Reproduces F-011 at n=250; only the llama judge ran so the tri-judge clause stays open and the prefer_verified default flip stays Jay's call. Recorded research-report rev 1.34 (master 3755c22); provenance committed bench/e009-claims-gate-probe 23dc643. **E-016 RAN but INVALID (harness-blocked):** the MemoryAgentBench EventQA two-arm run (feat/eventqa-runner, qwen3.5:9b + arctic + rerank, self-verify off vs on, 100 Q/arm) completed (off 19.0% / on 16.0%) but the numbers are not published: about half the generations (51/100 off) degenerated to 1-2 token stubs on the long contexts, so both arms sit at/below the 16.7% six-way-MC chance floor and measure a generation artifact, not the stack or the self-verify transfer. Controlled probes ruled out num_ctx, the option format, and the think:false param, so the trigger is in the real retrieved-context interaction and needs the runner instrumented to replay a degenerate prompt. Recorded research-report rev 1.35 (master 17e03d4); provenance feat/eventqa-runner 9809430. The self-verify transfer question stays open (N-013 remains the evidence it is task-specific); a generation-robustness fix + re-run is pre-registered. GPU RELEASED (qwen3.5:9b unloaded, 3060 free, bus #775). No benches queued; pipeline idle. **E-015 RESOLVED -> N-015** (tau-sweep, research-report rev 1.31; quantified the inert gate, FTS-source half deferred). **E-016 feasibility gate** (rev 1.32): BiTempQA phantom, TEMPO not memory-shaped, retargeted to MemoryAgentBench EventQA. Everything from here (publish BEAM, merge PR #168, E-012 opt-in ship, E-009 prefer_verified flip) stays Jay's decision. **PRIOR (Jun 19 ~19:15Z): E-014 (MemX R1 reject-gate) RESOLVED -> N-015.** Judge-free R@K ablation on LoCoMo (200 QAs, gate off vs on, tau 0.50, arctic): R@K identical 0.73/0.73 (F1/BLEU byte-identical too) - the gate NEVER FIRES because arctic cosines clear 0.50, so it is inert at the default tau on this embedder. Formally clears the 0.02 kill-criterion floor (0.00 drop) but the pass is hollow: safe, zero recall cost, but no measurable benefit on answerable LoCoMo; tau needs per-embedder calibration and the gate value needs an unanswerable-query benchmark (cf. E-005). Ships default-off opt-in. Recorded research-report rev 1.29 (master 573182d), N-015; logs committed feat/memx-levers c7c1a43; GPU freed + models unloaded. This closes the MemX experiments Jay asked about (BEAM already done; SuperLocalMemory was recon; BiTempQA/BEC/MemoryArena remain surveyed-not-run external benchmarks). **PRIOR: E-013 RESOLVED, F-014 CONFIRMED.** Jay rebooted the bench host back into Fedora (GPU online). Built the N-014 fix on feat/beam-runner (f7dc94e: empty-answer=FAIL guard in judge_prediction + configurable TAOSMD_BEAM_LLM_TIMEOUT default 120; 23 beam tests pass), ran the hardened BEAM-100K re-confirm = 49.0% / avg 0.455 / errors=0, abstention 100%, empty-answer guard fired ZERO times. Per the pre-registered kill criterion F-014 STANDS as measured and N-014 is confined to slow-CPU generation (no empty inflation on GPU; the 100% abstention is genuine). Full 1M re-run judged redundant (zero empties on GPU is tier-independent), not run. Recorded research-report rev 1.28 (master 7511753); [GPU RELEASE] posted + models unloaded, 3060 free. **Company-name / branding scrub (convention relayed by @taOS-dev, Jun 18): COMPLETE.** The attribution change (pyproject author + README footer to 'the taOS project', convention 2) is landed (ac832ab). The loud first scrub commit, whose subject named the former company, was reworded to 'chore: update license and packaging metadata' via filter-branch (message-only, content byte-identical) and force-pushed after Jay greenlit briefly lifting master branch protection (allow_force_pushes flipped true, pushed, restored to false, verified identical to the captured backup). master is now 02d916e; the former company name appears NOWHERE in the tracked tree or in any recent commit message. The 3 deep historical commits (about 180 to 429 back) were intentionally left untouched, since rewriting them would be a huge, conspicuous history rewrite counter to keeping this quiet. Parked reword branch + filter-branch backup ref cleaned up. **Earlier (Jun 18), now resolved:** (1) **E-009 tri-judge confirm DONE + recorded (research-report rev 1.27, master 2820165).** The lenient gemma4:e2b arm is NO-HARM (prefer_verified identical to off: judge 0.490, R@K 0.615, served-hall 0.000) but did NOT reproduce the elimination benefit, because its off baseline already had 0.000 served-hallucination (vs 0.040 in F-011), so the gate had nothing to remove; the difference tracks a config difference (vector-only, no reranker) and is undiagnosed, so the benefit is config-dependent. strict over-trades (R@K 0.615 to 0.455). The prefer_verified default flip still rests on the F-011 strict-llama run + Jay sign-off; the lenient arm only adds no-cost corroboration. Numbers captured in-session (result JSON e009_gemma_20260618_102802.json is on the Fedora disk, now OFFLINE because Jay rebooted the bench host into WINDOWS). (2) **VPS 30B-CPU BEAM run DONE and it surfaced a real harness confound -> N-014:** the runner's hard 60s LLM timeout stores EMPTY answers on slow generation, and the ported mem0 nugget judge PASSES an empty answer on abstention/negative rubrics, so blanks inflate the score. qwen3:30b-a3b on the CPU-only VPS read 55% with 10/20 empties, then 20% honest at a 240s timeout (0 empties, n=20); code-confirmed in beam_runner.py. So a frontier model on CPU did NOT beat the tuned local 9B. F-014's GPU 9B numbers (47% / 37.5%@1M, errors=0) probably stand but cannot be cleared from existing logs (no per-answer lengths recorded; abstention sits at 100%, the most exposed category), so **E-013 is pre-registered: a hardened BEAM re-confirm** (empty-answer=FAIL guard + raised timeout) with a kill criterion written before the run, QUEUED for the next GPU window after E-009. Recorded research-report rev 1.26 (N-014 + E-013 + F-014 caveat + abstract clause), pushed to master **7562327**. (3) **MemX R1 reject-gate BUILT** on feat/memx-levers (96b7725): apply_reject_gate(tau=0.50), env-gated TAOSMD_REJECT_GATE/_TAU default-off, 11+8 tests; the LoCoMo --strategy full ablation + a BEAM wire-in are PENDING the GPU (gate fires via retrieve() only, BEAM's retrieve_memories() needs an explicit call). MemX Lever 2 (freq/importance rerank) STUBBED honestly: no per-memory access-count tracking exists yet (upgrade-path TODO). **USAGE 7d 93% (SOFT WIND-DOWN + WEEKLY GATE, Jun 18 ~15:00Z): monitor-only, no new heavy work until the 7d window resets Jun 21 02:00Z; HARD STOP at 98%. 5h reset to 0% at 14:40Z (next 5h resets 19:40Z). GPU: the Fedora 3060 is OFFLINE (Jay took the box and had me reboot it into Windows via rebootwin); E-009 was collected first and released cleanly. No GPU work (E-013 hardened BEAM re-confirm + anything else needing the card) until the host is back in Fedora. Weekly job-pack refresh (docs/agent-jobs/: job-001 em-dash sweep, job-002 cross-encoder path fix, job-003 http-server dead import + README) is DUE before the Jun 21 dark period.** 5h 57% (resets 14:40Z). Resume pair armed (primary f7ac3a47 14:42Z, retry c720509d 15:02Z); audit a0855fff + daily sweep 74cba0e7 stay monitor-only. **feat/memx-levers (96b7725) and feat/beam-runner (a6006e5) both PUSHED** (owed PRs to master, deferred past wind-down). **Smart-installer PR #168 awaits Jay merge.** The publish-BEAM decision stays Jay's and now waits on the E-013 hardened number (publishing 47-49% before the empty-answer audit would be premature). (SUPERSEDED Jun 16 pause: Jay is rebooting the Mac mini, so this session + its session-only crons [audit de5c15ff, resume pair 30223ec8/30224b4d, daily sweep 4ef41aff] DIE; disk/git/memory persist; on the new session re-arm them per "On arrival" below, and the Fedora 3060 is JAY'S until he frees it [check bus + nvidia-smi before any re-claim]. BEAM is the active thrust, config + retrieval-recall sweeps DONE (47-49% plateau confirmed by BOTH; no config or retrieval lever beats it; wider/lexical retrieval REGRESS; self-verify is BEAM-neutral); 3060 RELEASED + unloaded. Runner BUILT on branch feat/beam-runner [benchmarks/beam_runner.py, ports mem0 own nugget-judge methodology + 10 categories, 13 tests]. FIRST RESULT: BEAM-100K validation [5 convs, 100 Qs, full stack arctic+rerank+self-verify+qwen3.5:9b+Qwen3-4B-Instruct judge] = **31% pass / avg_score 0.278** [results benchmarks/results/beam_100K_*.json scp'd to /tmp/beam_100k_result.json]. DIAGNOSIS [corrected, NOT over-abstention]: the bottleneck is RETRIEVAL PRECISION on long-context needles - retrieval pulls ~19 chunks/Q [zero zero-retrieval] but the exact-fact chunk is often missing; PASS vs FAIL retrieved_count ~equal [18 vs 20] so MORE depth alone will not fix it; only 6-8 abstentions, most failures are genuinely-wrong answers. IMPLICATION: the judge panel [Jay's idea] is a final-number firm-up, NOT the lever [wrong answers fail under any judge] - so score-up retrieval/gen FIRST, judge-panel on the best config. STRATEGY: BEAM rewards FRONTIER generation [mem0 64-70% @1M with GPT-4-class]; our local 9B lands honestly behind, so the play is a TRANSPARENCY one [early, fully-provenanced, methodology-pinned local-tier number] not a score-win. CONTEXT-AMOUNT SWEEP DONE (BEAM-100K, 5 convs, same stack, only the retrieval-depth env changed): rich [40 chunks / rerank15 / 24K ctx] = 9%, baseline [12 / rerank8 / 16K] = 31%, **TIGHT [6 / rerank4 / 6K] = 47%** [best]. CLEAR LEVER: intelligent context release - LESS, cleaner context wins big on the 9B [+16pp baseline->tight; more context BURIES the small model]. This is on-thesis (same as E-012). xtight [4 / rerank3 / 4K] = 47% (no gain over tight, so 47% is the BEAM-100K plateau under the 9B). BEAM-1M n=200 head-to-head MEASURED = **37.5% (75/200, avg_score 0.357)**; mem0 frontier 64-70% @1M, so local-9B lands honestly behind as predicted (per-category: abstention 100% / preference 80% strong; event_ordering 0% / knowledge_update 10% weak). qwen3.6:35b-a3b LOCAL generator arm TRIED + ABANDONED: it CPU-offloads on the 12GB 3060 [0/100 in ~40min, GPU starved at 35% util], so **9B is our practical local ceiling, not 35b** [[feedback_gpu_contention]]. CONFIG SWEEP DONE Jun 17 ~00:50Z (Jay freed the 3060 by waking it from LAN + rebooting Windows->Fedora; ran the 5 remaining arms, [GPU RELEASE]+unload posted). RESULT: NO config lever beats the 47% tight-retrieval plateau beyond n=100 noise. All 7 arms (pass% / avg_score): base_chunk100 47.0/0.436, chunk50 47.0/0.417, chunk200 49.0/0.425, chunk300 44.0/0.394, sentences 47.0/0.425, chunk100_nosv 49.0/0.455, chunk100_sv_decomp 47.0/0.436. Best nominal chunk200 + chunk100_nosv at 49% (+2pp, within noise of base). KEY FINDING: self-verify gives NO BEAM benefit (chunk100_nosv ties the top pass-rate AND leads avg_score 0.455), unlike LongMemEval F-013 - BEAM failures are wrong answers, not abstentions, so the CoVe verify/abstain pass cannot help here. Sweep was benchmarks/beam_campaign.sh (chunk size 50/100/200/300, sentence-mode, self-verify off, decompose on; branch feat/beam-runner, 9B gen + Qwen3-4B-Instruct judge). ARMS DONE: base_chunk100_sv **47.0%** (avg 0.436), chunk50_sv **47.0%** (avg 0.417, smaller chunks no gain); arm3 chunk200_sv interrupted. ARMS PENDING on resume: chunk200, chunk300, sentences, chunk100_nosv, chunk100_sv_decomp. Methodology + provenance: benchmarks/beam_campaign_resume.sh + log benchmarks/results/beam_campaign_resume_20260616_221030.log (branch feat/beam-runner). NO clear winner to carry to 1M (all within noise), so BEAM-1M stays 37.5% (n=200); OPTIONAL follow-up = a larger-n (n>=500) confirm of chunk100_nosv vs base, since self-verify-off is faster AND nominally >= on BEAM, if Jay wants a firmer read. NET: the score-up thesis is confirmed - local-9B plateaus at 47% on BEAM-100K (mem0 frontier 64-70% @1M), so the honest play is the transparent, fully-provenanced local-tier number, NOT a config-tuned score win. RECALL SWEEP DONE (Jun 17, Jay's "push for more"): tight retrieval 49% beats wider (recall_wide 43, recall_wider 46) and lexical-heavy (fts_heavy 41), so the plateau is NOT a retrieval-recall gap either - confirmed across TWO independent sweeps (config + recall), recorded research-report rev 1.24 + benchmarks/results/beam_recall_20260617_130445.log. The PUBLISH DECISION (publish the honest 47-49% / 37.5%@1M now vs keep pushing) is now ripe for Jay. ALSO DONE THIS SESSION: smart-installer BUILT -> PR #168 (profiles registry + setup-prompt CLI + static prompt, 17 tests, awaits Jay merge); SuperLocalMemory competitor dig DONE (first local-first competitor; their numbers are vendor-graded best-of-5 single-lenient-judge, zero-loss is the clean fault line; recs in reference_memory_landscape_jun2026). QUEUED: E-009 tri-judge confirm (next GPU window, mind the qwen3:14b co-resident deadlock - use the small judges) before the prefer_verified flip; E-012 opt-in/config ship. NEXT after that: record a BEAM finding in the research report once the series settles. RECORD a BEAM finding in the research report once the series settles. Jay's call publish-baseline-now vs score-up-first [my lean: score-up first; 47%/37.5% is already a far better, fully-provenanced story than 31%]. E-012 DONE [F-013 74.6%]; ship=opt-in verified+rerank QUEUED; PRs merged; smart-installer SPEC written+committed [docs/superpowers/specs/2026-06-16-smart-installer-design.md] AT THE REVIEW GATE awaiting Jay's OK before writing-plans.)

## State as of Jun 13 (historical)

- **master** is healthy: about 886 tests passing. Shipped today (Jun 13): **PR #162 arctic-embed-s as the low-tier dense default** (ships F-010 -- R@K +0.157, judged +0.0565 full-1540, same 384-dim and latency; existing installs keep MiniLM via the config default, only fresh setups fetch arctic) and **PR #163 the v2 claims layer "Provable Memory"** (default-off: `taosmd/claims/` -- zero-loss ClaimStore, cross-family fail-closed entailment verifier, demote-not-delete recall gate, archive-span provenance, async verify-pass, `taosmd verify`/`claims status` CLI). Earlier this cycle (Jun 12): temporal date-range lever (PR #157), pause/resume checkpointing for the LoCoMo runner (PR #158), compact mode on `GET /a2a/messages`, the grants-gate auth fix (`faaaf3c`), project-scoped grants (#151), task-graph (#150), batch ingest + bm25 mode (#149), TTL filter + three-number summaries (#155), admin surface (#153), research report first edition (#156).
- **taOS #744 is CLOSED, e2e-verified 3/3** against the real registry feeds (live grant pass with claim-binding, expired-grant 403, revoked 403). The exercise found and fixed the gate-bypass bug above, the argument for e2e over unit coverage.
- **INTEGRITY CORRECTION (Jun 14, research report rev 1.19): the LongMemEval-S "97.0%" headline was Recall@5, mislabelled "end-to-end Judge accuracy" since an April docs reframe.** Proof: benchmarks/results/enhanced_20260413_133215.json (query_expand, top_k=5) is the source of the published per-category table and is a Recall@5 hits metric; no Judge run ever produced 97.0% (the Judge harness reproduces ~22.6%). Corrected on every public surface (README, benchmarks.md, research-report F-001, AGENTS.md, CHANGELOG) to **"97.0% Recall@5"** with provenance; Jay approved. Recall@5 still beats MemPalace 96.6% head-to-head, so the corrected claim is strong. **E-012 pre-registered** to measure a genuine end-to-end Judge number. The data was never lost (public benchmark + valid local copies).
- **Positioning (Jun 14): README now CO-HEADLINES "Provable, auditable memory" alongside the Recall@5 number.** An independent Letta/Mem0/Graphiti/Cognee comparison found provenance + zero-loss + audit trails unaddressed across the field, so that gap is our lead (the zero-loss archive + F-009/F-011 claims layer answer it). Copy propagated to @taOS for their README section + the website. Landscape intel + the open-ground confirmation are in memory [[reference_memory_landscape_jun2026]]. Research tooling set up: agent-reach (pipx) + no-auth methods (Reddit RSS, HN Algolia, GitHub, Jina) [[reference_research_methods_noauth]]; daily landscape-monitor cron armed. mem0's Apr-2026 numbers (94.4 LongMemEval / 92.5 LoCoMo) have ZERO disclosed judge methodology, a direct contrast to our provenance.
- **Other headline: LoCoMo leader 0.748/0.394/0.659 (tri-judge), unchanged and correctly labelled.** Earlier pre-registered negatives stand: N-008 (projected ColBERT no upgrade), N-009 (surprise-boundary chunking a coverage artifact), N-010 (write-skip under the shipping floor), N-011 (surprisal worst retention signal).

## In flight as of Jun 13 (historical)

- **E-012 SCORE-UP -- CONFIRMED + judge-robust (F-013), GPU released.** SUPERSEDED by N-017/PR #176 (2026-06-23): the judge verdict parser counted INCORRECT as a pass, inflating every Judge score below; corrected full-500 is 42.8% (qwen3.5:9b) / 51.2% (llama3.1:8b). Full-500: tuned-depth baseline 56.8% (284/500), winner rerank+self-verify **74.6% (373/500)**. The cross-family llama3.1:8b firm-up reproduced BOTH numbers exactly (identical per-category counts), so the +17.8pp gain is not judge-specific. Recorded F-013 (research-report rev 1.22, firm-up appended) + provenance benchmarks/results/e012_ablation_results.md, all pushed to master. [GPU RELEASE] posted (bus #474). Levers on PR #165 (bench/e012-judge-harness): rerank (TAOSMD_RERANK), self-verify (TAOSMD_SELF_VERIFY), decompose (TAOSMD_DECOMPOSE, a DUD - drop), sampling (TAOSMD_SAMPLE_SEED). OPTIONAL/LATER: lenient gemma4:e2b re-judge (needs a prediction-cache to dodge the VRAM deadlock); qwen3.6:35b-a3b bigger-gen arm (23GB, CPU offload); full-distractor-set run (pre-registered). **SHIP DECISION = JAY's** (never-silently-enabled): self-verify is general + model-agnostic; proposal is an opt-in verified-answer mode + rerank in the recommended retrieve config, NOT a silent core default (taOSmd serves memory; answer-gen is the consumer path). Also pending: PR #165 review/merge (+ #164/#166/#167). Harness env: TAOSMD_OLLAMA_MODEL=qwen3.5:9b, TAOSMD_JUDGE_MODEL=hf.co/unsloth/Qwen3-4B-Instruct-2507-GGUF:Q4_K_M, TAOSMD_DECOMPOSE_MODEL=qwen3.5:9b.
- **E-009 RESOLVED to F-011: the prefer_verified claims gate PASSES its kill criterion.** Full 10-conv LoCoMo sweep (200 QAs, 1853 claims, llama3.1:8b judge, `benchmarks/results/e009.json`): served-hallucination 0.040 -> 0.000 (eliminated), judged accuracy 0.425 -> 0.445 (no drop, +0.020 within noise at n=200), R@K 0.615 -> 0.610 (-0.005, inside the 0.02 bound). strict FAILS (judge -0.065, R@K -0.065, trades accuracy for purity) and stays opt-in. Robust claim: **prefer_verified eliminates served-hallucination at NO measured accuracy cost** (the +0.02 is noise; do not claim a gain). Recorded report rev 1.18 + F-011. **REMAINING before the default flip: Jay sign-off + a tri-judge larger-n confirm** (per "never silently enabled" + ship-the-win); until then the gate stays default-off with the validated trade documented. Harness PR'd: **PR #164** (bench/e009-claims-gate-probe). NOTE: first launch with qwen3:14b judge hung on a 12GB VRAM model-swap deadlock; relaunched with llama3.1:8b co-resident (see [[feedback_gpu_contention]]).
- **E-007 arctic-embed SHIPPED (F-010), PR #162 MERGED.** R@K +0.157, judged +0.0565 full-1540 (arctic-s 0.7305 vs MiniLM 0.6740), same 384 dim and latency (report rev 1.15). Enablers #160/#161 also merged. Existing installs keep MiniLM (config default); fresh setups fetch arctic via the tested HF path (Pi 13ms/embed). **OPEN FOLLOW-UP (not a blocker): @taOS owes arctic store-registration + verified OS-store download path** (integration msg 417; bus 422 reframed it as a follow-up). A gentle Pi-CPU speed sample is the remaining nice-to-have.
- **E-008 RESOLVED to N-011: surprisal is the WORST retention signal; length wins.** The v2 surprisal pillar is dead across retrieval (N-009/N-010) and consolidation (N-011); the v2 spine is the provenance/claims layer (anchored by F-009, 18.8 percent extraction-hallucination, unmatched). **Claims layer (Provable Memory) MERGED, PR #163** (default-off). E-009 (above) is the validation that decides whether `prefer_verified` ever flips default-on. Spec/plan private at ~/tinyagentos-private/{specs,plans}/2026-06-13-v2-claims-layer-*. Side-finding: length is a strong free retention heuristic (own pre-registration deferred).
- **qmd fork exit path:** upstream PR tobi/qmd#728 open (tsc cleanup 51 to 3, zero behavior change); #663 (brettdavies, continues our #511 with credit, includes /search + /vsearch) has two ready branches linked from us (`dim-guard-on-663`, `fix-663-test-types`). When #663 merges and a release cuts, our fork can be dropped. Watch both for maintainer replies.

## Queued next as of Jun 13 (historical)

1. **E-012 BASELINE DONE (F-012: 47.2% end-to-end Judge), score-up phase ACTIVE (Jay: "use all our knowledge to get scores up").** SUPERSEDED by N-017/PR #176 (2026-06-23): the F-012 47.2% was measured under the buggy judge parser (INCORRECT counted as pass); the corrected full-500 shipped-config baseline is 42.8% (qwen3.5:9b) / 51.2% (llama3.1:8b). Honest LongMemEval-S oracle Judge number: 236/500 = 47.2% under a strict local judge (qwen3.5:9b gen + Qwen3-4B-Instruct judge + MiniLM), branch bench/e012-judge-harness, log e012_full500.log. DIAGNOSIS: retrieval is solved (97% Recall@5), REASONING is the bottleneck (temporal 17.3%, multi-session 28.6%; single-session/knowledge 71-89%); the generator was evidence-STARVED (assembler 2000 tok / 5 chunks / 3 FTS). Recorded report rev 1.20 / F-012. SCORE-UP LEVERS (each measured vs 47.2%): (a) DONE - depth knobs tunable (TAOSMD_ASSEMBLE_TOKENS=4000/RETRIEVE_LIMIT=12/FTS_LIMIT=5/CONTEXT_CHARS=16000, commit a23ea26); (b) NEXT + highest-leverage - "intelligent context release" (Jay's idea, validated): retrieve MORE then RERANK/PRUNE to a clean subset before generation (bge-v2-m3 already in stack; evaluate Provence pruner [[reference_context_pruning_research]]); the claims layer already drops INCORRECT, pruning adds the REDUNDANT half; (c) arctic embedder + query-expansion; (d) lenient/tri-judge pass for a comparable number + the full-distractor set. GPU runs COORDINATED with @taOS via the lease protocol [[feedback_gpu_lease_protocol]] (their demo priority; CLAIM/RELEASE on the integration channel). **APPROVED + BUILT (Jay "let's go", Jun 14):** spec docs/superpowers/specs/2026-06-14-e012-score-up-ablation-design.md + plan docs/superpowers/plans/2026-06-14-e012-score-up-ablation.md. All four levers now exist on bench/e012-judge-harness (PR #165): (a) depth knobs + (b) rerank context-release (TAOSMD_RERANK) were already built; decomposition (TAOSMD_DECOMPOSE, ported from locomo) and self-verification (TAOSMD_SELF_VERIFY, fresh CoVe-style) added TDD with 7 green tests; default-off so the baseline arm is unchanged. Ablation runner benchmarks/e012_ablation.sh. RUN IN PROGRESS on the leased 3060 (lease msg #465; gen qwen3.5:9b, judge Qwen3-4B-Instruct-2507, decompose reuses the 9B so co-resident ~9GB, no deadlock). n=20 wiring validation PASSED (55% temporal, judge sane). METHODOLOGY NOTE the validation surfaced: the tuned-depth defaults already lift temporal well above F-012's 17.3%, so the screen adds an explicit STARVED anchor arm (2000/5/3) to re-tie to 47.2% and isolate the depth-tuning gain. NEXT: 9-arm screen at n=100, then full-500 + tri-judge firm-up on the winner; record F/N either way.
2. **E-009 default-flip decision (Jay).** Claims gate resolved to F-011: prefer_verified PASSES (zero served-hallucination at no accuracy cost), strict opt-in. Flip the `prefer_verified` default to on only after Jay sign-off + a tri-judge larger-n confirm (n=200 single-judge validates "no cost", not enough to flip a shipped default). Interim option: document prefer_verified as a recommended opt-in integrity mode now. Also: PR the harness branch `bench/e009-claims-gate-probe`.
3. **E-010 DONE -> N-012: no embedder beats arctic-embed-s; arctic retained, F-010 reaffirmed.** Judge-free LoCoMo R@K on the VPS: arctic-s 0.823, e5-small 0.808, bge-small 0.773, granite-r2 0.742, gte-small 0.727; none clears the +0.02 margin (granite, the top MTEB candidate, did not transfer). Recorded report rev 1.21 / N-012. Shipped reusable: env-configurable onnx loader (TAOSMD_ONNX_POOLING/_QUERY_PREFIX/_DOC_PREFIX) + benchmarks/e010_bakeoff.sh. Branch bench/e010-embedding-bakeoff (the loader change is worth a PR to master).
4. **@taOS coordination (their side, no taOSmd work owed):** (a) @taOS is installing an SD image backend on the Fedora 3060 themselves (Jay granted the window; I touch nothing outside taOSmd); (b) they owe arctic store-registration (Snowflake/snowflake-arctic-embed-s with a verified OS-store path); (c) they owe a README correction on their side (taOSmd section should say "97.0% Recall@5", not Judge). RESOLVED Jun 24 via directory PR #51 (merged): the directory entry now cites 97.0% Recall@5 and the bug-tainted 74.6% figure is gone. Their tool-call leaderboard is DONE (zero flow failures down to 4B).
5. **Interim job pack** at `docs/agent-jobs/` (three low-risk jobs) for weaker agents; review any PRs against each job's verification section, never auto-merge.
6. **Reproducibility hardening (F-001 follow-up): ADDRESSED, PR #167 (chore/reproduce-longmemeval).** Root cause was that the repo could not reproduce its own headline: the dataset was untracked and the recipe undocumented. Fix: committed the previously-untracked proof file (enhanced_20260413_133215.json, query_expand 485/500 = 97.0% Recall@5), pinned the exact recipe in benchmarks/REPRODUCE-longmemeval.md (runner, command, config, embedder, metric, expected per-category table), sha256-pinned the dataset via a tracked benchmarks/data/README.md (gitignore negation; the 265 MiB file stays ignored), and deprecated the stale longmemeval_recall.py. An independent CPU re-run from the pinned recipe is in flight on the bench host; its confirmation line gets appended to the doc's reproduction log before #167 merges. Recall@5 is deterministic so it reproduces host-independently.

A GATE CAN BE CORRECT, PASS TEN TESTS OF ITS OWN, AND BE WIRED TO NOTHING, AND THE ONLY WAY TO TELL IS TO COMMIT THE DEFECT AND WATCH THE SUITE STAY GREEN. The trailing-newline gate carded last session arrived as a clean script with ten passing fixture tests, and the script genuinely works: dropping a real fragment whose last byte is 0x65 into the tree and running it directly produces a named failure and a non-zero exit. Nothing in the repository invokes it. A search across every workflow, config and shell file returns no reference, while the three sibling gates each ship their own workflow, so the convention existed and was not followed. What settles it is not the search but the consequence: with that offending fragment still sitting in the changelog directory the full suite returned sixteen hundred and twenty seven passed, which is the baseline plus exactly the ten new tests, so every test written to catch the defect ran green while the defect sat untouched a few directories away. The tests only ever examine temporary fixtures and not one of them looks at the real repository. The generalisation is that a gate has two separate properties, whether it can detect the defect and whether anything ever asks it to, and a passing test suite only ever demonstrates the first. The card was the root cause rather than the lane: it specified the script and its fixture tests in precise detail, never said how the gate gets run, and additionally warned against asserting on the real tree, so the implementation delivered exactly what was asked.

A TEST NAMED FOR A BEHAVIOUR CAN TEST A HELPER INSTEAD, AND THE NAME IS WHAT EVERY LATER READER TRUSTS. A helper-path guard landed that refuses to emit crontab lines from two kinds of ephemeral location, a temporary directory and a linked git worktree, and both refusals demonstrably fire against the real command line. Only one of them is covered. Disabling the whole guard fails one test, which looks like coverage until the mutant is made precise: removing only the worktree refusal and leaving the detector function untouched, so the existing test still has something to call, leaves all nine tests passing while the real command resumes emitting four crontab lines that pin a path which vanishes. The test responsible is named for refusing, never calls the entry point and never asserts a refusal; it calls the path-inspection function directly and checks that it returns a string. Testing the detector is not testing the decision that consumes it, and a name that promises the decision is worse than no test, because it retires the question for everyone who reads the file afterwards. The wider rule is that a mutant aimed at the whole feature can be absorbed by one honest test while a second, vacuous one hides behind it, so the mutant has to be aimed at each branch of the behaviour separately.

A CHANGELOG WRITTEN FROM THE AUTHOR'S WORKING CONTEXT ANNOUNCES REPAIRS TO BUGS NO RELEASE EVER HAD, AND THAT IS NOW TWICE IN TWO SESSIONS. A large and genuinely good implementation restored a receipts subsystem and described itself as fixing endpoints that had been returning 500 after being dropped by an earlier pull request. Neither half is true of the shipped product. The module has never existed on master and the route has never existed on master, so those endpoints returned 404 because there was nothing there, and nothing was dropped from any release because the feature had never landed in one. The same applies to its claims about de-duplicating a routing table and correcting a unit conversion, both of which describe the closed predecessor branch rather than the tree being merged, and the merged diff adds all of it fresh with zero deletions. Filed under Fixed, it read as an admission that the product had shipped broken endpoints. The check that catches this costs one command, asking the history whether the file or the route was ever on master, and it has to be run because the author's frame of reference is the branch they have been living in while the reader's is the release. The correction went straight to master as Added, exactly as the previous instance did, since a fragment is permanent and the code itself was sound.

A REVIEWER'S OWN CARD IS SUBJECT TO EVERY RULE THE REVIEWER ENFORCES, INCLUDING THE ONE ABOUT CONTRADICTING ANOTHER CARD. A revision card was filed and then closed and rewritten within minutes of its own creation, because it instructed the next implementer to assert on the real repository tree while the original card in the same lineage had explicitly forbidden exactly that as vacuous. Two cards in one lineage giving opposite instructions is the precise failure flagged in other lanes every week, and the only reason it was caught was reading the original card's full body while closing it rather than trusting the summary carried forward in a checkpoint. The replacement states the reconciliation outright, that the missing workflow is mandatory and a real-tree test is optional, and explains why the original objection was half right, because such a test breaks only when a fragment is added badly, which is the intended behaviour rather than a flaw. Two probes in the same session also announced themselves as results while measuring nothing: a comparison of two command invocations used an argument that is invalid for both of them, so an error that looked like one form failing was the tool rejecting the flag, and a command line run without its required timestamp exited non-zero from both the positive and the negative location, which is the tell, since two locations that should differ returning the same status means the code under test was never reached.

## Working agreements

- Durable state lives in three places: GitHub issues (tasks), this file (snapshot), and the A2A bus (the taOSmd-progress channel is the running log).
- Small doc fixes go straight to master. Features, refactors, and redesigns go through a branch and PR.
- Benchmark policy: real datasets, external judges, publish methodology, record negative results too.

## On arrival (incoming agent or contributor)

1. Read this file top to bottom.
2. ARM THE SESSION INFRASTRUCTURE (project rule, every session, no exceptions): (a) the 30-minute audit cron using the canonical prompt in `.claude/audit-cron-prompt.md` (schedule 7,37 * * * *), covering rate-limit recovery, docs freshness, the research report, memory, and the bus; (b) the A2A connection per `.claude/a2a-session-setup.md`: live watcher, durable poll floor verified, history caught up, identity rules observed.
2. `git fetch origin` and review recent commits on master plus any open branches.
3. Check open GitHub issues and PRs.
4. Tail the A2A bus: taOSmd-progress, general, integration channels.
5. Read the project memory index if you have one.
Then take the top unblocked item, or continue whatever "In flight" points at.

## On rate limit or handoff (the moment you see the warning)

1. Commit and push WIP on a branch. Never leave uncommitted work.
2. Update this file: move your task to "In flight" with the branch name, exactly where you stopped, and the next concrete step.
3. Post one line to taOSmd-progress: finished X, mid-flight Y on branch Z, next step W.
The next agent recovers from last push + open PRs + this file + issues.
